#!/bin/bash
#
# SPDX-License-Identifier: AGPL-3.0-or-later
# Copyright © 2026 Enrico Weigelt, metux IT consult
#
# Full build of all packages (in solution order) for xserver-master.
# The install prefix is ephemeral — deleted after the build.
#

set -e

. cf/xserver-master/config.sh

$MPBT -solution $SOLUTION -workdir "$WORKDIR" build

rm -Rf "$WORKDIR"/install
