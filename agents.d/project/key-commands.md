---
title: "Key commands"
order: 40
---

## Key commands

| command | purpose |
|---------|---------|
| `./mpbt-workspace-bootstrap [--shell-hook] [--build] [--all-releases]` | **one-shot fresh-clone setup** — see "Bootstrapping a fresh clone" above; idempotent, safe to re-run |
| `./install-mpbt` | `go install github.com/metux/mpbt/cmd/mpbt-builder@latest` |
| `./run-flagship [--detach]` | start a Claude Code **flagship** session (`AGENT_ID=Enterprise`, no `XLIBRE_RELEASE`); `--detach` launches it in a named tmux session via `agent-run` so it survives terminal close |
| `scripts/ship-names assign [flagship]` \| `release <name>` \| `list` \| `gc` | **Star Trek ship name registry** — each agent instance gets a unique ship name as its `AGENT_ID`. `assign` picks the next unused name (50 names, flock-serialized); `assign flagship` reserves `Enterprise` for the control/flagship session; `release` frees a name; `gc` reaps reservations with no live agent-bus heartbeat. Names shown in the shell prompt (PS1), Claude Code status line (⚓), and tmux session title. `Enterprise` is the reserved flagship — never auto-assigned to worker sessions |
| `./run-ship [--release <rel>] [--name <id>]` | start a plain Claude Code session with an auto-assigned, board-visible `AGENT_ID` (`<rel>-$$` or `ws-$$`); `--release` sources that project's `cf/<rel>/config.sh` first (accepts the same short/full/non-xserver forms as `run-opencode.*`). The explicit-launcher counterpart to `scripts/agent-bus-auto-id.sh` (which does the same thing implicitly, via `~/.bashrc`, for a plain `claude` started from a shell already inside the workspace tree) |
| `scripts/agent-bus-monitor-loop` | the `Monitor`-tool `command` that watches this session's own agent-bus inbox and prints one line per new/unacked directive; identical invocation every session (reads `$STARFLEET_SHIP_ID` from the environment, no session-specific args), so it's pre-authorized via a bare `"Monitor"` allow entry in `.claude/settings.json` — arming it needs no confirmation prompt |
| `.bin/starfleetctl hook claude monitor-hint` | Claude Code `SessionStart` hook helper: emits `hookSpecificOutput.additionalContext` JSON telling the assistant to unconditionally arm Monitor-tool watchers on its agent-bus inbox (and, for `AGENT_ID=Enterprise`, `fleet-watch` too). Wired as a `SessionStart` hook in `.claude/settings.json`. Quiet no-op when `$STARFLEET_SHIP_ID` is unset |
| `.bin/starfleetctl agent-bus fleet-watch` | `Monitor`-tool `command` for a control/flagship session: watches `_WORK_/agent-bus/status/` and prints one line whenever a ship's heartbeat *epoch* changes — "New ship online: …" for a never-seen `AGENT_ID`, "Ship update: …" for a restart of a known one (seeds its known-set from the board at arm time, so only later changes are reported) — covered by the bare `"Monitor"` allow. Auto-armed alongside `agent-bus monitor-loop` for `AGENT_ID=Enterprise` sessions via `starfleetctl hook claude monitor-hint` |
| `./run-fetch.xserver-<release>` | clone/fetch all sources for a release line |
| `./run-build.xserver-<release>` | full build of all packages in order, then **deletes** `_WORK_/<release>/install` |
| `./run-opencode.xserver-<release>` | start opencode session for a release line (sets `XLIBRE_RELEASE`) |
| `scripts/xx-make-pr.sh [--branch <name>] <commits...>` | create PR from commits on the incubator branch |
| `.bin/starfleetctl mk-agent-clone <release> [name]` | create/refresh an agent-owned clone for backport work (object-shared, isolated from your clone) |
| `scripts/worktree add <repo> [name] [--from <ref>\|--branch <existing>]` \| `list [repo]` \| `remove <repo> <name> [--force] [--keep-branch]` \| `prune [repo]` | generic **worktree** helper for temporary/per-session/per-task isolation of **any** existing repo (not just xserver release lines — use `mk-agent-clone` for those, see why in its header). `add` defaults to a fresh throwaway branch `wt/<name>` off `origin/HEAD` (or the repo's current branch); `--branch <existing>` checks out an existing branch instead (git's normal one-worktree-per-branch guard still applies). All worktrees live under **this** workspace's `_WORK_/worktrees/<reponame>/<name>/` regardless of which repo they're for — one well-known, already-gitignored location. `remove` deletes the throwaway `wt/<name>` branch too, unless `--keep-branch` or it was checked out via `--branch`. Pre-authorized in `.claude/settings.json` |
| `scripts/backport-commit <release> <commit-ish\|PR#> [name]` | **one-shot backport**: refresh agent clone → `cherry-pick -x` → `xx-make-pr.sh`; opens the PR vs `release/<release>` |
| `.bin/starfleetctl show-branch-file <ref> <path> [symbol]` | print a repo file (or symbol region) at any ref via the GitHub API — for backport applicability checks; auto-handles the `Xext/<ext>/` ↔ `<ext>/` reorg |
| `.bin/starfleetctl backport-applies <master-path> <grep-ERE> [release ...]` | run the applicability grep across all release lines at once (wraps `show-branch-file`) — classify each branch vulnerable / already-fixed / N-A in one command |
| `scripts/pr-set-body <pr#> <body-file>` | set a PR body via REST API (works around the broken `gh pr edit`); for backport cross-linking |
| `.bin/starfleetctl pr-view <pr#> [json-fields]` | print PR metadata via `gh pr view --json <fields>` as one canonical, allowlistable command (default fields `number,title,state`) — exists so agents don't chain it after an unrelated `echo`/`export` (breaks `Bash(gh *)` prefix matching, see the AGENT_ID-export gotcha below); also covers the backport workflow's step-0 "already backported?" body check |
| `scripts/pr-append-body <pr#> <text-file>` | fetch a PR's current body, append the given text, write it back via `pr-set-body` — for backport-dashboard tables / back-links without manual fetch-then-set |
| `scripts/pr-comment <pr#> <body-file> [--bot-review]` | post a PR comment; `--bot-review` prepends the exact mandated bot-disclosure banner (see the `bot-review` skill) so the wording can't drift |
| `scripts/pr-label <pr#> add\|remove\|set-review <label...>` | add/remove PR labels via REST API (same `gh pr edit` workaround as `pr-set-body`); `set-review passed\|changes-requested` swaps the two `bot-review-*` outcome labels in one call |
| `scripts/pr-request-reviewers <pr#> <login...>` | request PR reviewers via REST API — wraps the HW-domain-routing recipe from the `bot-review` skill |
| `.bin/starfleetctl pr-ci <pr#\|URL>` \| `--json` | **quick CI status of a PR** — one canonical command (give it a PR number *or* full PR URL, nothing else) so agents don't hand-assemble `gh pr checks`/`gh pr view --json statusCheckRollup` combos. Classifies every check **by conclusion, not count** (fail-fast means a big red number is usually a few `FAILURE` + many collateral `CANCELLED`): prints header + bucket counts (pass/FAIL/cancelled/pending/skip), lists the real failures + pending checks, a one-line verdict, and a known-flake hint (BSD/Solaris VM, xephyr-glamor timeout, go-xts) when the reds match. For the failure *logs*, use `pr-job-logs`. Pre-authorized in `.claude/settings.json` |
| `scripts/pr-job-logs <pr#>` \| `--job <id>` \| `<pr#> --all` | **fetch raw CI job logs + failure summary** for a PR's failing jobs (or one job by id). Wraps the reliable `repos/<repo>/actions/jobs/<id>/logs` endpoint (since `gh run view --log` returns nothing here) and greps the real cause across **build/link** (`FAILED:`, `: error:`, `undefined reference`), **configure** (meson `ERROR:`, `Dependency "x" not found`), **dep-install** (Gentoo `emerge`/`!!!`, Alpine `apk`, Debian `E:`, Arch, Fedora), and **test-phase** (`Summary of Failures`, `Fail: N`, `Caught signal`/segfault) failures; falls back to the log tail when no marker matches. Writes to `$OUTDIR` (default `mktemp -d`) |
| `scripts/pr-checkout <pr#> [name]` | **repair an open master PR**: isolated agent clone + check out the PR's head branch, ready to edit (prints the clone dir) — pairs with `pr-amend-push` |
| `scripts/pr-amend-push <clone-dir> [files...]` | fold edits into the PR's commit (`--amend --no-edit`, keeps message + `Signed-off-by`) and `--force-with-lease` back to the PR branch |
| `scripts/pr-claim <pr#> ["what"]` \| `--list` \| `--release <pr#>` \| `--steal <pr#>` | **advisory cross-agent PR lock + work log** so multiple agents don't collide on the same PR (each has its own clone, but they share one GitHub PR branch). Claim a PR before mutating it; `--list` is the shared "who's working on what" log. Keyed by PR#, `flock`-serialized, stale after `$CLAIM_TTL` (1h). Set a unique `$STARFLEET_SHIP_ID` per agent. See Concurrency |
| `.bin/starfleetctl agent-bus status <state> ["note"]` \| `board` \| `inbox` \| `ack <id>` \| `tell <agent> <text>` \| `broadcast <text>` | **cross-session control plane** (status heartbeats + directives) so one control agent ("1st officer") can see and steer many independent sessions without terminal-hopping. Workers `status`/`inbox`/`ack`; the controller reads `board` and posts `tell`/`broadcast`. File-based + `flock`-serialized under `_WORK_/agent-bus/` (gitignored), heartbeats stale after `$BUS_TTL` (15m). Set a unique `$STARFLEET_SHIP_ID`; `$XLIBRE_RELEASE` is the default project column; `$AGENT_HANDLE` (set by `agent-run`) is the board's ATTACH column. Sibling of `pr-claim` (that = PR-branch ownership; this = who's-doing-what + steering). See Concurrency |
| `scripts/agent-run <release> [--client claude\|opencode\|shell] [--name <id>] [--tier <tier>] [--supervisor <name>] [--permission-mode <mode>] [-- <args>]` \| `--list` \| `--stop <id>` | **launch a session DETACHED in a named tmux session** so it survives terminal close / SSH disconnect and any number of controllers can attach. Now a thin wrapper around `.bin/starfleetctl session run` (Go backend). Works for any CLI (not just Claude). Registers the tmux session as the agent-bus `$AGENT_HANDLE` so it shows on the `board`. Needs `tmux`. See "Detaching sessions" in Concurrency |
| `scripts/fleet-autoscale need <N> --reason "<text>" [--release <rel>] [--client claude\|opencode] [--max <cap>] [--dry-run]` \| `status` | **on-demand fleet elasticity**: spawn extra ships when more parallelizable work is queued than idle capacity, capped at `--max`/`$FLEET_AUTOSCALE_MAX` (default 6) total non-stale fleet size. Now a thin wrapper around `.bin/starfleetctl session autoscale` (Go backend). Explicit trigger only — no daemon/cron/Monitor-loop wiring, by design (see DASHBOARD.md "Fleet auto-scaling" row); every call (spawn or no-op) is logged to `_WORK_/agent-bus/autoscale-events.log` and a real spawn also `agent-bus broadcast`s, so it's never a silent background action |
| `scripts/agent-attach <id\|session\|partial>` \| `--read-only` \| `--independent` \| `--list` | **connect a controller to a detached agent** (tmux multi-attach — many controllers, same live session). `--read-only` = watch-only; `--independent` = own window size (grouped session). Resolves `<id>` against the board / tmux session names. Detach with `Ctrl-b d` (agent keeps running) |
| `scripts/fetch-nvidia-drivers [version ...]` | download proprietary NVIDIA `.run` installers and extract the X-server modules (no install) for ABI checks |
| `scripts/list-nvidia-versions [--per-branch]` | enumerate NVIDIA driver versions on the mirror (all, or latest-per-branch) |
| `scripts/fetch-all-nvidia-drivers [--every]` | fetch+extract many versions (default: latest of each branch; `--every` = all 500+), pruning `.run`s |
| `scripts/nvidia-abi-check SYM ...` | classify symbols against extracted blobs: link-import (`nm`) ∪ runtime lookup (string) |
| `scripts/driver-tracker` \| `--update <issue#>` | cross-repo dashboard for the `xf86-input-*`/`xf86-video-*` driver forks (org-wide, dynamically discovered) — prints open PRs/issues per repo, or pushes the report into a GitHub tracking issue (`--update`, default target `X11Libre/xserver#3280`). No GitHub view spans all driver repos at once otherwise. Ongoing/recurring — see its DASHBOARD theme for refresh cadence |
| `scripts/nvidia-undefined-symbols [version ...]` | raw dump of imported (UND) symbols (subset of nvidia-abi-check; prefer the latter) |
| `scripts/json validate <file>...` \| `pretty [file]` \| `get <expr> [file]` | JSON helper so agents stop reaching for ad-hoc `python3 -c "import json…"` one-liners (each triggers a fresh auth prompt, since arbitrary `python3 -c` can't be safely wildcard-allowlisted). Reads a file arg or stdin (`gh api … \| scripts/json get …`). `validate` = parse + non-zero exit on bad JSON; `get` evals a Python expr against the parsed doc (bound to `data`). Pre-authorized in `.claude/settings.json` |
| `scripts/confirm-log-hook` | **`PreToolUse`/`Bash` hook, pure telemetry — not meant to be run by hand.** Reimplements `permissions.allow`/`deny`/`ask` matching (glob/`:*`/word-boundary rules, `&&`/`\|\|`/`;`/`\|`/`\|&`/`&`/newline splitting with quote- and `2>&1`-redirect-awareness, `timeout`/`time`/`nice`/`nohup`/`stdbuf`/bare-`xargs` wrapper stripping) and, for each Bash sub-command that isn't auto-allowed, appends one JSON line to the fleet-shared `_WORK_/agent-bus/tooling-gaps/events.jsonl` (`flock`-serialized). **Guaranteed neutral:** always exits 0 with empty stdout — no `hookSpecificOutput`/`permissionDecision` field ever, so it cannot alter the real allow/deny/ask outcome (verified live: wiring it in and running both already-allowed and deliberately-unlisted commands produced zero change in prompting behavior). Skips only `permission_mode: bypassPermissions` (nothing there ever prompts, so logging would be pure noise); every other mode is logged **with its mode recorded** rather than assumed — live testing found a fleet-worker session running `acceptEdits` executes an unlisted Bash command with no visible prompt at all, so hardcoding "only default mode counts" would have silently under-collected. See DASHBOARD.md "Permission-confirmation telemetry" row (directive m0050) for the full writeup and coordination with the m0051/m0052 active-escalation effort (shares the same `tooling-gaps/` directory name, distinguished by a `source: hook\|escalation` field, once that side is built) |
| `scripts/confirm-log-report [--top N] [--words N] [--category no-match\|ask-match\|deny-match]` | aggregator for the above: ranks the most common non-matched command prefixes (grouped by first N words, default 2) from `_WORK_/agent-bus/tooling-gaps/events.jsonl` — the "what to build a wrapper for next" report. Pre-authorized in `.claude/settings.json` |
| `scripts/with-clone-lock <cmd...>` | run a mutating command holding an exclusive per-working-tree lock (see Concurrency) |
| `scripts/ws-commit -m <msg> <path...>` | atomically commit (+push) to **this** workspace repo under the `with-clone-lock` mutex — the safe way for agents to mutate the shared workspace checkout (`-a` = all tracked, `--no-push` = commit only). Use this instead of raw `git commit` here, so concurrent agent sessions don't race the index/HEAD (which produced bundled "1" commits) |
| `scripts/dashboard pull \| show \| write <file\|-> \| commit -m <msg> [--no-push]` | dedicated read/write/commit/push cycle for the **whole** `.starfleet-ai/DASHBOARD.md` file — still useful for a fresh session's first orientation ("give me everything"), but for editing one theme prefer `.bin/starfleetctl dashboard theme <cmd>`/`reindex` (see that row) since 2026-07-06's per-theme-file restructuring. `pull`/`show` sync via `git pull --rebase --autostash` under `with-clone-lock` first (so you're editing fresh content, not a stale local copy); `write` replaces the file's content for scripted/non-interactive updates (does not commit); `commit` wraps `ws-commit -m <msg> DASHBOARD.md`. Pre-authorized in `.claude/settings.json` |
| `.bin/starfleetctl show-pr-conflict` | list all open PRs with merge conflicts. **Bash original removed 2026-07-06** — no remaining `scripts/*` dependents, parity already proven |
| `.bin/starfleetctl <agent-bus\|dashboard\|hook [claude ...]\|session\|pr-claim\|ws-commit\|ship-names\|with-clone-lock\|pr-view\|pr-ci\|show-branch-file\|backport-applies\|show-pr-conflict\|pr-checkout\|pr-amend-push\|pr-comment\|pr-label\|pr-request-reviewers\|pr-set-body\|pr-append-body\|backport-commit\|xx-make-pr\|mk-agent-clone> [args…]` | thin exec wrapper for the `mpbt-hq/starfleetctl` Go CLI (own repo, mpbt solution — see "starfleetctl" theme in `DASHBOARD.md`). Rebuilds the binary automatically when its source is newer, then runs it with the resolved workspace root. **Cut over to production: `dashboard`, `pr-claim`, `ws-commit`, `ship-names` (2026-07-03); `agent-bus` (2026-07-06, praetor go-ahead once parity was re-confirmed live); `with-clone-lock` (2026-07-06, Phase 1 of the starfleetctl roadmap — the one fleet-management gap in the previously-covered set); `pr-view`, `pr-ci`, `show-branch-file`, `backport-applies`, `show-pr-conflict` (2026-07-06, Phase 2's read-only GitHub-interaction subset — no separate go-ahead sought since all five are read-only, matching the "read-only ones are lower risk" carve-out already noted for this phase in `DASHBOARD.md`); `pr-checkout`, `pr-amend-push`, `pr-comment`, `pr-label`, `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `backport-commit`, `xx-make-pr` (2026-07-06, Phase 2's mutating GitHub-interaction subset, `mpbt-hq/starfleetctl@e77934c` — praetor go-ahead via Enterprise, directive m0060, after Constellation's risk-scaled parity verification: byte-identical live checks for the low-blast-radius label/body/comment commands, a real end-to-end `pr-checkout` run against an open fork PR, scratch-repo runs for `pr-amend-push`/`xx-make-pr`, and new Go tests for `backport-commit`'s path-remap fallback — see the `DASHBOARD.md` "starfleetctl" row for the full verification breakdown); `dashboard theme list\|show\|write\|new\|commit` + `dashboard reindex` (2026-07-06, DASHBOARD.md restructuring per directive m0048/m0073 — one-file-per-theme editing + a mechanically-regenerated thin index, `reindex` verified idempotent; see the "Working practices" DASHBOARD.md note above for the full workflow); `mk-agent-clone` (ported 2026-07-06, `starfleetctl@221cabd`; bash original deleted 2026-07-07 once `pr-checkout`/`backport-commit` — its only two callers — were confirmed to call the Go `EnsureAgentClone()` directly rather than shelling out, and a shallow-clone `--reference` regression the Go port had inherited was fixed first, `mpbt-hq/starfleetctl@68b4cf7`, mirroring the same fix already applied to the bash original in `mtx/agent-config@a530435`).** Each is a full Go reimplementation of its bash namesake sharing the exact same on-disk file format/lock file, so Go and bash callers interoperate transparently: `dashboard`/`ws-commit`/`with-clone-lock` share `<gitdir>/mpbt-clone.lock` (verified in both directions: a Go holder blocks a bash `with-clone-lock`/vice versa, reporting the correct holder PID, and `pull --rebase` cleanly interleaves commits from both sides); `pr-claim` shares `_WORK_/agent-claims/`; `ship-names` shares `_WORK_/agent-bus/ships/` + its `.assign.lock`; `agent-bus` shares `_WORK_/agent-bus/{status,inbox,...}` (`board` output deterministically sorted — same alphabetical-by-filename order as bash's glob, confirmed byte-identical in parity tests; a one-off live-bus row-order difference observed during the initial spot-check was almost certainly two sequential invocations racing a constantly-changing board, not a Go defect — an earlier note here mis-attributed it to "map iteration", which the implementation doesn't use for this path). **`board`/`inbox`/`msgs`/`asks` (agent-bus), `--list` (pr-claim), and `list` (ship-names) all take a `--json` flag** (2026-07-06, Phase 1) that prints a JSON array instead of the human table — for `.bin/starfleetctl agents` to consume directly, no `grep`/`awk`/`jq`/`sed` needed; text output is unchanged when `--json` is omitted. `with-clone-lock` is deliberately generic like its bash namesake: it operates on the caller's cwd's git working tree, not just mpbt-workspace, so it skips the usual workspace-root discovery. **The five Phase-2 GitHub-interaction subcommands (`pr-view`/`pr-ci`/`show-branch-file`/`backport-applies`/`show-pr-conflict`) still shell out to the real `gh` CLI for auth/transport** (re-implementing GitHub API auth in Go wasn't the point of this phase) **but replace jq/grep/sed post-processing with native `encoding/json`/`regexp`** — verified byte-for-byte identical output against the bash originals, including `pr-ci`'s `--json`/PR-URL-argument forms and `show-branch-file`'s multi-hit `grep -A` semantics (merged/adjacent context blocks, `--` separators between non-adjacent ones). One real bug found+fixed in the process: bash `show-pr-conflict` was missing `--repo`, so it silently queried whatever repo `gh` auto-detected from the caller's cwd instead of always `X11Libre/xserver` — fixed in both the bash original and (by construction) the Go port. Prefer `.bin/starfleetctl <name> …` over the bash `scripts/<name>` going forward for all twenty: **it's pre-authorized as one blanket entry** (`Bash(.bin/starfleetctl)`/`Bash(.bin/starfleetctl *)`), so every current and future subcommand this tool gains is automatically covered without a new allowlist entry each time — the explicit trade-off (less granular; a bug in one subcommand isn't scoped out from the others) the praetor already accepted when starfleetctl was planned. **2026-07-06 (praetor + Enterprise): bash originals removed once genuinely dependency-free, not just "cut over."** `pr-view`/`pr-ci`/`show-pr-conflict`/`backport-applies` deleted — audited first (`grep -rl` across `scripts/*`, `.claude/settings.json`, and the Monitor/hook scripts) and confirmed zero remaining callers of the bash form. **The other six stay on disk for now, for concrete reasons, not just caution:** `ship-names` is still exec'd directly by the `SessionStart`/`SessionEnd` hooks (the bash `agent-bus` CLI has since been removed, so those hooks now call `.bin/starfleetctl agent-bus` instead) and by `agent-bus-auto-id.sh`/`agent-run`/`fleet-autoscale`; `agent-bus-watch` is the `agent-bus watch` subcommand. Farragut's live test (directive m0047) found the Go `agent-bus-monitor-loop`/`fleet-watch` port reliably misses messages that arrive *after* the Claude-Code `Monitor` tool starts it (backlog-only detection; root cause not yet found across 3 isolated repro attempts), so that cutover is presently blocked by a real bug, not deferred out of caution. `dashboard`→`ws-commit`→`with-clone-lock` is a live `exec` chain (`scripts/dashboard`'s commit path literally `exec`s `"$HERE/ws-commit"`); `pr-claim`→`with-clone-lock` is exec'd by `scripts/pr-checkout`; `show-branch-file` is exec'd by `scripts/backport-commit`. **Phase 2's mutating GitHub-interaction subset (`pr-checkout`, `pr-amend-push`, `pr-comment`, `pr-label`, `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `backport-commit`, `xx-make-pr`) is now cut over too** (2026-07-06, praetor go-ahead via Enterprise, m0060) — but their bash originals are **not** deleted yet, same "stays on disk until genuinely dependency-free" reasoning as the rest of this list: `scripts/pr-checkout` (soft-)checks `scripts/pr-claim --who`; `scripts/backport-commit` calls bash `scripts/xx-make-pr.sh`; nothing in `scripts/*`/`.claude/settings.json`/the Monitor/hook scripts calls any of these nine *as the caller* (verified via the same `grep -rl` dependency audit used for the four already-deleted ones). **Update, 2026-07-07 (Pegasus, directive m0138):** `pr-checkout`/`backport-commit` already called the Go `EnsureAgentClone()` directly rather than shelling out to bash `scripts/mk-agent-clone` (see the `mk-agent-clone` bullet above) — that was the only remaining bash dependency blocking it, so once its inherited `--reference`-on-shallow-repo regression was fixed (`mpbt-hq/starfleetctl@68b4cf7`), `scripts/mk-agent-clone` (bash) itself was confirmed dependency-free and deleted. **Before deleting any further bash original: re-run the same dependency audit** (`grep -rl '\bscripts/<name>\b' scripts/ .claude/settings.json` minus the file's own self-reference and `starfleetctl`-prefixed hits) — a script can be individually "cut over and parity-proven" while still being load-bearing for a sibling that isn't. **The `agent-bus-monitor-loop`/`agent-bus-fleet-watch` Monitor scripts remain the only `Monitor`-tool-safe implementation** — they read the inbox/board files directly rather than shelling out to the `agent-bus` wrapper. The `SessionStart`/`SessionEnd` hooks now call `.bin/starfleetctl agent-bus` (`status`/`clear`/`watch`), since the bash `agent-bus` CLI was removed. **Concurrent starfleetctl development note:** the shared `_WORK_/starfleetctl/sources/xlibre/starfleetctl` checkout is used by every ship on this host — building directly in it while another ship is mid-edit can collide (seen 2026-07-06 twice: two ships editing `cmd/starfleetctl/main.go` at once, and separately a stale untracked `internal/withclonelock/` colliding with a fast-forward pull once that package landed on origin via a different ship's isolated worktree). Use `scripts/worktree add _WORK_/starfleetctl/sources/xlibre/starfleetctl <name>` to isolate your own change, then push straight to `origin master` from the worktree (`git push origin HEAD:master`, since a fresh worktree branch isn't named `master`) once it's tested; if you must edit in the shared checkout directly, `git stash`/rebase your changes before any `git pull` rather than assuming a fast-forward will be clean |
| `scripts/cancel-stale-ci [--cancel] [--branch N] [--workflow N]` | cancel still-running CI runs whose branch moved on since they started (compares each run's head SHA to the branch's current tip; dry-run by default, `--cancel`/`-y` to act). Paginates all active runs; caches branch-tip lookups |
| `scripts/prune-stale-ci [--delete] [--keep-gone] [-v] [--branch N] [--workflow N]` | **delete** completed/outdated workflow runs whose branch moved on (head SHA ≠ current tip) **or whose branch was deleted** (clean 404). Dry-run by default (per-branch summary), `--delete` to act — **irreversible**. "Branch moved" deletes only when the tip is positively resolved and differs; gone-branch runs are deleted too unless `--keep-gone`; aborts on any non-404 tip-lookup error. Sibling of `cancel-stale-ci` (that one cancels *running* stale runs; this deletes *finished* ones) |
| `cf/xserver-master/packages/xlibre/update-generic.sh` | re-generate symlinks from the driver template for all generic drivers |
