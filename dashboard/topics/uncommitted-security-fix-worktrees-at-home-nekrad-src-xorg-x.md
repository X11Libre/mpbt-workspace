---
slug: uncommitted-security-fix-worktrees-at-home-nekrad-src-xorg-x
title: "**Uncommitted security-fix worktrees at `/home/nekrad/src/xorg/xserver-rel-25.0` and `-25.1`** — outside mpbt-workspace, never tracked in agent-bus/DASHBOARD"
category: parked
noted_by: "Enterprise, 2026-07-03"
since: "2026-07-03"
migrated_from: 8cf8692b9f0057ffe44e793b35bcf7329da83d3f
---

Found while checking "who uses these dirs" for the praetor. Both are **linked git worktrees** of `/home/nekrad/src/xorg/xserver` (main clone, on `master`), on **unborn** branches `rfc/security-fixes-release-25.0`/`-25.1` (no commits, but full tree staged — classic `git checkout --orphan` artifact). No untracked files beyond `build/`, no reflog, no running process/tmux session touches them. **Real, unpublished work confirmed** — do NOT delete without extracting first: both worktrees carry, identically, (1) an integer-overflow/negative-size guard added to `render/filter.c`'s `convolutionFilterValidateParams` (rejects `w<0`||`h<0`, uses `int64_t` for the product) and (2) an OOB-read guard added to `Xi/xiselectev.c`'s `ProcXISelectEvents` (bounds-checks before dereferencing `evmask->mask_len`) — **neither exists anywhere on `master`** (checked both at the current path and the `Xext/render/`|`Xext/xinput/` reorg path), so this looks like original hardening authored directly on the release lines, never upstreamed. **Needs scrutiny before landing:** both worktrees also re-add a `xorgGlxCreateVendor();` call in `glamor/glamor_egl.c` — the *inverse* of PR #3174 (which removed that exact call on master to fix the `libglx.so`/`libglamoregl.so` duplicate-symbol issue, see AGENTS.md); could reintroduce that bug, verify before use. The `25.1` worktree's base is stale (meson.build shows 25.1.7/2026-06-14 vs current `origin/release/25.1` at 25.1.8/2026-06-21) — its extra-looking diffs in `Xext/panoramiX.h` (macro `__LAMBDA__`→`__VA_ARGS__`), `Xi/xiproperty.c` (switch bracing) and `hw/xfree86/common/xf86DGA.c` (reverts PR #3173's `x_rpcbuf_write_string_0t_pad` fix) are **just staleness noise**, not deliberate edits — confirmed those fixes are already on current `origin/release/25.1`, so a rebase onto the current tip drops them cleanly. `25.0`'s worktree base already matches current `origin/release/25.0` exactly (only the 3 genuine files differ) — no staleness issue there. **Next step:** rebase 25.1's staged tree onto current `origin/release/25.1`, re-verify the `glamor_egl.c` hunk, then commit + run through the normal backport-workflow (`mk-agent-clone` + PR) instead of leaving it in an ad-hoc worktree outside the fleet's tracking. **Update 2026-07-03 (Potemkin):** confirmed **both** vulnerable patterns exist verbatim on current `master` too (`Xext/render/filter.c` still has plain `w * h > nparams`; `Xext/xinput/xiselectev.c`'s non-swapped path still reads `evmask->mask_len` before bounds-checking it — the swapped path right above already has the check, so it's a real asymmetry) — filed as a **master PR first, per normal workflow: #3244** (`render/Xi: fix integer-overflow and OOB-read input-validation bugs`, 2 commits, build-verified via `ninja hw/vfb/Xvfb`), CI running. **`glamor_egl.c` hunk: discarded, not carried into #3244 or any backport.** Verification: `nm` on the currently-built `libglamoregl.so`/`libglx.so` (from `_WORK_/xserver-master/target/…`) shows `xorgGlxCreateVendor` already present as a **local** symbol in *both* modules even on unmodified current master (`Xext/render/meson.build`'s `glamoregl` target still `link_with: [glamor, libxserver_glx]`) — so #3174's fix (removing the explicit call) didn't fully eliminate cross-module duplication at the object-file level, and the actual PR #3174 concern was specifically about the *duplicated static-guard flag* (`vendor_initialized` et al.) firing per-module-copy, not the mere symbol existing twice (harmless for `static`/local symbols by itself). Given that ambiguity plus the task's explicit "im Zweifel verwerfen", **not** re-adding the explicit `xorgGlxCreateVendor()` call is the safe call — it's exactly the line #3174 removed on purpose, and nothing in this batch of work depends on it. **Next step:** once #3244 is merged, backport both commits to `release/25.2`/`25.1`/`25.0` via `scripts/backport-commit`, then delete the two ad-hoc worktrees (only after the backport PRs exist). **DONE 2026-07-06 (Potemkin) — fully landed, closing this row:** #3244 merged to master as `6a97f6d38e` (filter.c) + `bf6b62648d` (xiselectev.c). Backported to all three release lines via `scripts/backport-commit` from an isolated `mk-agent-clone master security-fixes` clone: `release/25.2` → **#3245**/**#3246**, `release/25.1` → **#3247**/**#3248**, `release/25.0` → **#3249**/**#3250** (all 🔄 open, awaiting praetor's manual release-line merge per the standing no-auto-merge-on-release rule). Backport dashboard table added to #3244's body; each backport PR links back to #3244. Before deleting, re-verified both ad-hoc worktrees file-by-file against their release tips (`git diff --stat origin/release/<rel> -- .`): `25.0` differed only in the 3 known files; `25.1` additionally showed `panoramiX.h`/`xiproperty.c`/`xf86DGA.c`/`meson.build` diffs, all confirmed to be pure staleness (worktree predates upstream changes already merged, e.g. still had the pre-#3173 unpadded `xf86DGA.c` write) — no real extra work lost. Both worktrees + their unborn branches removed (`git worktree remove --force`).
