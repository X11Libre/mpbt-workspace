---
slug: starfleetctl-consolidate-the-flock-race-prone-agent-scripts
title: "`starfleetctl` (renamed from `mpbtctl`): consolidate the flock/race-prone agent-scripts (`agent-bus`, `pr-claim`, `ws-commit` first) into one Go CLI"
category: active
status: "**In progress 2026-07-03, assigned to Farragut, successive/incremental — `agent-bus` subcommand ported, parity-verified, now its own repo + mpbt solution**"
doc_ref: "**own repo `mpbt-hq/starfleetctl`** (private, `master`, commit `77009c2`; moved from `metux/starfleetctl` 2026-07-06, see the Phase 5 entry below), wired as an mpbt-workspace **solution** like go-x11proto/flyingtux: `cf/starfleetctl/{config.sh,solutions/default.yaml,packages/xlibre/starfleetctl.yaml}` + `run-fetch.starfleetctl`/`run-build.starfleetctl`, clone lives at `_WORK_/starfleetctl/sources/xlibre/starfleetctl`"
migrated_from: 8cf8692b9f0057ffe44e793b35bcf7329da83d3f
---

Motivation from chat with praetor: (1) one `Bash(starfleetctl)`/`Bash(starfleetctl *)` allowlist entry replaces ~30 per-script pairs in `.claude/settings.json`, and every future subcommand is covered automatically (tradeoff: also less granular — a new subcommand inherits full trust with no separate review step); (2) Go's `encoding/json` + `os/exec` arg-arrays eliminate a real class of bash bugs (quoting/word-splitting, brittle jq/sed JSON parsing, `set -e` silently swallowed in pipelines) — but CI/VM flakes, `gh` CLI quirks (e.g. broken `gh pr edit`), and the Claude-harness command-prefix-matching gotcha are external and would need re-encoding in Go just the same, not eliminated by the rewrite. **Not** a big-bang rewrite of all ~30 `scripts/*` — start with the highest-value/highest-race-risk ones (agent-bus/pr-claim/ws-commit), leave single-purpose scripts (e.g. `show-branch-file`) as bash. **Critical constraint: `scripts/agent-bus` is live infrastructure the whole fleet depends on right now** — build the Go equivalent alongside the existing bash version against the same `_WORK_/agent-bus/` file format, validate parity, and only cut over once proven equivalent; never let a WIP `starfleetctl` break the running fleet's coordination. Licensing: AGPL-3.0-or-later (workspace tooling, not an xserver/driver deliverable — see AGENTS.md "Licensing policy"), SPDX headers per file. Update this row every session that touches it, per AGENTS.md's theme-tracking rule — this is exactly the kind of multi-session initiative that gets lost otherwise. **2026-07-03, part 1 (Farragut):** built `agent-bus` subcommand (all 12: status/clear/inbox/ack/board/tell/broadcast/ask/reply/asks/msgs/events/prune), stdlib-only Go, `syscall.Flock` for the same mutex the bash `flock 9` uses, byte-for-byte TSV file format match with `scripts/agent-bus`. **Verified parity** against a scratch `BUS_DIR` with mixed bash-writes/go-reads and vice versa (status/board, tell/broadcast/inbox/ack/msgs/events, staleness+`[STALE]`+prune, a cross-impl ask→reply round trip, matching exit codes) — landed first on branch `mtx/mpbtctl` in mpbt-workspace (based on `mtx/agent-config`, not `master` — `AGENTS.md`/`DASHBOARD.md`/`LICENSE`/`scripts/agent-bus` only exist there). **2026-07-03, part 2 (Farragut), same session:** praetor decided — instead of living inside mpbt-workspace (or being folded into the `mpbt`/`mpbt-builder` Go repo, the other option discussed), it should be its own sister project **exactly like go-x11proto/FlyingTux**: own repo, cloned+built via an mpbt solution. Renamed `mpbtctl`→**`starfleetctl`** (fits the existing ship-name/fleet theme), created `metux/starfleetctl` (private GitHub repo), migrated the code there (module path + `cmd/` dir renamed to match), added a `Makefile` (`go build` via mpbt's `exec` builder, same shape as go-x11proto's), wired `cf/starfleetctl/` + the two `run-*.starfleetctl` wrappers on `mtx/agent-config`. **Verified end-to-end**: `./run-fetch.starfleetctl` clones cleanly, `./run-build.starfleetctl` builds via the real `mpbt-builder` (not just `go build` by hand), and the resulting binary's `agent-bus` subcommand still interoperates with `scripts/agent-bus` on a shared scratch bus. Old `mtx/mpbtctl` branch in mpbt-workspace is now **superseded** — worktree removed, branch left pushed but unmaintained (not force-deleted; praetor's call whether to delete it). **Next session:** `pr-claim` subcommand in `metux/starfleetctl`; wire a `Bash(starfleetctl)`/`Bash(starfleetctl *)` allowlist entry once there's an actual cutover plan — still bash-only in production for now. **TODO flagged by praetor, 2026-07-03 (Pegasus):** `starfleetctl` still needs a **self-bootstrap command** to set itself up from scratch in a brand-new, completely empty workspace — in particular tool-authorization (the `.claude/settings.json` allowlist entries this same row already discusses) and creating whatever files/dirs it expects to find (currently all hand-wired by mpbt-workspace's own scripts/config, not something `starfleetctl` can do for itself yet). Not scoped/designed yet — just parked here so it isn't lost before the `pr-claim` subcommand work above. **2026-07-03 (Endeavour): first subcommand actually cut over to production — `dashboard`.** Praetor asked to give `starfleetctl` a high-level DASHBOARD.md access function so dashboard operations "go through it" without needing separate explicit authorization each time. Built `internal/dashboard` (Go port of `scripts/dashboard`: `pull`/`show`/`write`/`commit -m <msg> [--no-push]`), committed+pushed to `metux/starfleetctl` (`bb96557`). Deliberately shares the *exact same* `<gitdir>/mpbt-clone.lock` file `scripts/with-clone-lock`/`scripts/ws-commit` use (not a separate lock), so a Go and a bash actor mutating this clone at once still serialize instead of racing — **verified genuinely interoperable**, not just parallel-implemented: in a scratch bare+clone repo, a Go `dashboard commit` timed out (reported the correct holder PID) while a concurrent bash `with-clone-lock sleep` held the lock, then succeeded immediately once released. Also verified: pull/show/write/commit round trip, `show`'s stdout/stderr split (file content only on stdout, git chatter on stderr, matching the bash version exactly), the nothing-staged no-op, and `--no-push`. **Wired the actual cutover**, not just the code: added `scripts/starfleetctl` (thin exec wrapper — rebuilds the binary when source is newer, execs with `MPBT_WORKSPACE_ROOT` resolved, so it's invoked the same relative way every other fleet script is) and pre-authorized it in `.claude/settings.json` as `Bash(scripts/starfleetctl)`/`Bash(scripts/starfleetctl *)` — the first real instance of the "one blanket entry auto-covers every subcommand" trade-off this row already flagged as the motivation, now actually exercised (`agent-bus` subcommand rides along under the same grant, but is **not** yet the production path — only `dashboard` has real callers switched over; `scripts/agent-bus` bash remains authoritative for the fleet bus). `scripts/dashboard` (bash) is left in place, not removed — both implementations share the lock and the file format, so they coexist safely; `scripts/starfleetctl dashboard …` is now the **preferred** path per `AGENTS.md`'s updated Key-commands entry. **Next:** migrate the `pr-claim` subcommand next (per Farragut's prior note), then reassess whether `scripts/dashboard`/`scripts/agent-bus` bash wrappers can be deprecated once every caller (including this file's own maintenance instructions) references `starfleetctl` instead. **2026-07-03 (Farragut): worked the rest of the original TODO autonomously per Enterprise directive (m0024) — `pr-claim`, `ws-commit`, plus a self-identified extra, `ship-names`.** All three follow the established pattern (stdlib-only Go, same file formats/lock files as the bash originals) and are pushed to `metux/starfleetctl` (`d715ecc` pr-claim+ws-commit, `4806c9a` ship-names). `pr-claim`: full command set (claim/refresh/steal/release/release-all/list/who), verified against a shared `CLAIM_DIR` with mixed bash/Go callers including exit codes and the events.log format. `ws-commit`: shares the *exact same* `<gitdir>/mpbt-clone.lock` as `scripts/with-clone-lock`/`scripts/ws-commit`/`internal/dashboard` — verified with a scratch bare+clone pair that a Go commit genuinely blocks on a bash-held lock (reported the real holder PID) and that bash/Go commits interleave cleanly via `pull --rebase`; also verified `--no-push` and `-a`/`--all`. `ship-names` (not originally requested — self-identified via `grep -l flock scripts/*` per the directive's "check independently for further candidates" instruction, same judgment call as `dashboard` earlier): covers `assign` (general + flagship), `release`, `list`, `gc`, `flagship`, sharing `_WORK_/agent-bus/ships/` and the `.assign.lock` (fixed 10s wait); verified byte-for-byte identical `list` output and that `assign` genuinely blocks on a concurrently-held lock (measured ~2s against an externally-held flock). **Still bash-only in production** for all three (unlike `dashboard`, which already has `scripts/starfleetctl` + the settings.json cutover) — no `.claude/settings.json`/AGENTS.md changes made for these, per the standing constraint that a cutover needs proven parity **and** the praetor's go-ahead, which hasn't been sought yet for these three. **No more flock-based `scripts/*` candidates found** after `ship-names` (checked via `grep -l flock scripts/*`, only `with-clone-lock`/`ws-commit`/`pr-claim`/`agent-bus`/`ship-names` match, all now covered — `dashboard` doesn't itself use `flock` directly, it shells out to `with-clone-lock`'s lock file). **Next:** praetor decision on cutting any of pr-claim/ws-commit/ship-names over to production the way `dashboard` was; the parked self-bootstrap-command TODO above is still open too. **TODO flagged by praetor, 2026-07-03 (Pegasus):** move the `starfleetctl` repo from the personal `metux/starfleetctl` account into an `mpbt` GitHub organisation (not yet created/confirmed to exist — needs checking/setting up). Not scoped yet: whether the org already exists, whether it'd go public or stay private, and everything that references the current `metux/starfleetctl` path today (`cf/starfleetctl/*`, `run-fetch.starfleetctl`/`run-build.starfleetctl`, `scripts/starfleetctl`, this DASHBOARD.md row, any local clone remotes) would need updating to the new org path. Parked here alongside the other open starfleetctl TODOs, not yet actioned. **2026-07-03 (Farragut): praetor gave the go-ahead ("wenn alles gut genug funktioniert, können wir umschalten") — cut `pr-claim`, `ws-commit`, and `ship-names` over to production, same treatment as `dashboard`.** Updated `AGENTS.md`'s Key-commands entry for `scripts/starfleetctl` to list all four as preferred over their bash namesakes (no new `.claude/settings.json` entry needed — the existing blanket `Bash(scripts/starfleetctl)`/`Bash(scripts/starfleetctl *)` grant already covers every subcommand). Bash originals left in place, unchanged, coexisting via the shared file/lock formats — nothing forces any caller to switch. **`agent-bus` explicitly excluded from this cutover** — it's fleet-wide live infrastructure (every session's heartbeat/inbox), a fundamentally different risk class from the other four's single-session/advisory scope, so it stays bash-authoritative until separately, explicitly approved. Verified the cutover itself is real, not just documented: the AGENTS.md commit for this change (`12305f7`) was made *with* `scripts/starfleetctl ws-commit` instead of `scripts/ws-commit` — a genuine production use, not a scratch-dir test. **2026-07-06 (Enterprise): `agent-bus` itself cut over to production too** — praetor go-ahead after a live parity spot-check (`board` output byte-identical to bash modulo row order, cosmetic only — Go map iteration doesn't preserve the bash version's file-listing order). `AGENTS.md` updated (`bf29a11`) to list `agent-bus` alongside the other four as preferred via `scripts/starfleetctl`. `SessionStart`/`SessionEnd` hooks and the `agent-bus-monitor-loop`/`agent-bus-fleet-watch` Monitor scripts deliberately kept on bash (hooks run outside the permission system, so switching them buys nothing; the Monitor scripts read the inbox/board files directly, not through the `agent-bus` wrapper). **2026-07-06 (praetor): 5-phase roadmap for the rest of this initiative, dispatched while the praetor is away (a few hours) — fleet to work as autonomously as possible, self-coordinating via `pr-claim`/this row, only pinging for genuine blockers or real design decisions.** Phases as given (no phase 4 was specified — treat the numbering gap as intentional/reserved, don't invent content for it):

  - **Phase 1 — full fleet-management coverage + zero-post-processing output. ✅ DONE 2026-07-06 (Farragut), directive `m0029`.** Audited `scripts/*`/`AGENTS.md` for pipe-chains after agent-bus/pr-claim/ws-commit/ship-names/dashboard — found **none currently in use** (these are read as human tables today), so this was forward-looking rather than replacing an existing bad pattern. **Coverage gap found and closed:** `with-clone-lock` had no Go equivalent (`mpbt-hq/starfleetctl` commit `1429074`) — the generic "serialize mutating work in one clone" primitive every other lock (`ws-commit`/`dashboard`) builds on. Deliberately generic like the bash original (operates on the caller's cwd's git tree, skips workspace-root discovery); verified interop both directions against a scratch repo (Go blocks on a bash-held lock and reports the correct holder, and vice versa, ~2s measured not a race). **Zero-post-processing output added** (commit `1d28004`): `--json` on `agent-bus board/inbox/msgs/asks`, `pr-claim --list`, and `ship-names list` — structured JSON array instead of the human table, field names mirroring the text columns (age as an `age_seconds` integer, not the display string), always a valid `[]` even when empty. Existing text output is unchanged (same code path when `--json` is absent). Verified all four with `python -m json.tool` plus manual field-value checks against scratch bus/claim/ship-registry dirs. **Correction to the `agent-bus` cutover note above:** its claim that `board`'s live spot-check row-order difference was due to "Go's map iteration" was a misdiagnosis — this implementation sorts deterministically (same alphabetical-by-filename order as bash's glob, confirmed byte-identical in this session's own earlier parity tests); the observed difference was almost certainly two sequential invocations racing a constantly-changing live board, not a Go defect. Fixed the wording in `AGENTS.md`'s Key-commands row (kept this DASHBOARD row's own history as-is, corrected only the still-current doc). **Process incident, worth flagging for the other phases:** found a live collision in the shared `_WORK_/starfleetctl/sources/xlibre/starfleetctl` checkout — a concurrent Phase-2 edit (`internal/ghpr`) was mid-write in the same tree while this work was in progress (main.go merged cleanly by luck; the tree was briefly non-buildable). Isolated the rest of this work in `scripts/worktree add _WORK_/starfleetctl/sources/xlibre/starfleetctl <name>` per-change worktrees instead, pushed clean from there each time (`git push origin HEAD:master`, since a fresh worktree branch isn't `master`), broadcast a heads-up (`m0043`) recommending the same for whoever's on Phase 2/3/5. `AGENTS.md`'s starfleetctl row now carries this recommendation. Both commits (`1429074`, `1d28004`) reviewed clean (`go build`/`go vet`/`gofmt -l`) before push. **`AGENTS.md`/`.claude/settings.json` updated** per the standing "parity first, then docs" rule — `with-clone-lock` added to the cut-over list (same risk class as the other four: single-session/advisory, not fleet-wide-live like `agent-bus`), no new `.claude/settings.json` entry needed (existing `Bash(scripts/starfleetctl)`/`Bash(scripts/starfleetctl *)` blanket grant already covers it).
  - **Phase 2 — GitHub interaction into Go too** (same `starfleetctl` binary or a separate one — implementer's call, but justify the choice in this row). Covers the `pr-*` family (`pr-view`, `pr-ci`, `pr-checkout`, `pr-amend-push`, `pr-comment`, `pr-label`, `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `pr-job-logs`, `show-pr-conflict`, `cancel-stale-ci`, `prune-stale-ci`, `show-branch-file`), `backport-commit`/`backport-applies`, and `xx-make-pr.sh`. Same rules as always: parity-proven-then-praetor-go-ahead before any AGENTS.md/settings.json cutover of a *mutating* command (read-only ones like `pr-view`/`pr-ci` are lower risk); never bypass `pr-claim` semantics; **no auto-merge into `release/*` ever**, only `master` and only when explicitly told. **Claimed by Potemkin, 2026-07-06 — read-only subset DONE, pushed + cut over.** `pr-view`, `pr-ci` (incl. `--json` + PR-URL-argument forms), `show-branch-file` (incl. multi-hit `grep -A` semantics — merged/adjacent context blocks, `--` separators between non-adjacent ones), `backport-applies`, `show-pr-conflict` — all implemented in `internal/ghpr/` (`metux/starfleetctl` → now `mpbt-hq/starfleetctl`, commit `075f264`), wired as new top-level `starfleetctl` subcommands standing alongside `with-clone-lock` (skip `workspaceRoot()`, work from any cwd — they're stateless `gh` wrappers, not fleet-file-coordination tools). Still shell out to the real `gh` CLI for auth/transport (not worth reimplementing GitHub API auth in Go for this phase) but replace jq/grep/sed post-processing with native `encoding/json`/`regexp`. **Verified byte-for-byte identical output against every bash original** on live repo data (PR #3244/#3245, master `Xext/render/filter.c`) before pushing. **One real bug found+fixed along the way:** bash `show-pr-conflict` was missing `--repo`, so it silently used `gh`'s cwd-based repo auto-detection instead of always `X11Libre/xserver` — returned an empty/wrong list depending on where you ran it from (confirmed: empty from `mpbt-workspace` itself, a real xserver PR list from inside an xserver clone). Fixed in the bash original too, not just the Go port. Cut over directly to "preferred" in `AGENTS.md` for these five — no separate praetor go-ahead sought, since all five are read-only (matches this row's own "read-only ones are lower risk" carve-out); bash originals left in place, both coexist. **Collision note (see also the starfleetctl row's own "Concurrent starfleetctl development" callout):** hit the shared-checkout collision this same session — found Farragut's `with-clone-lock` WIP mid-edit in the same tree, then (after they moved to an isolated worktree) a stale untracked `internal/withclonelock/` blocked a plain fast-forward pull. Resolved by removing the stale untracked dir, pulling clean, and re-applying only the `ghpr`-specific hunk to the freshly-pulled `main.go` rather than my own stale copy. **Next:** the mutating subset (`pr-checkout`, `pr-amend-push`, `pr-comment`, `pr-label`, `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `backport-commit`, `xx-make-pr.sh`) — pick up if free, claim the row; each of those needs parity-proof **and** an explicit praetor go-ahead before any cutover, per the standing rule. **Claimed by Constellation, 2026-07-06 (fleet directive m0046) — mutating subset DONE, pushed, cut over 2026-07-06 (m0060).** All nine implemented in `internal/ghpr/` (`mpbt-hq/starfleetctl` commit `e77934c`, rebased cleanly onto `f351823`): `pr-comment` (incl. the `--bot-review` banner prepend, ship name from `$AGENT_ID`), `pr-label` (add/remove/set-review), `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `pr-checkout` (same-repo and fork-PR paths, the advisory `pr-claim --who` cross-agent check, wired through `workspaceRoot()` since it needs `scripts/mk-agent-clone` + `_WORK_/...` paths — unlike the stateless read-only group), `pr-amend-push`, `backport-commit` (incl. the path-remap fallback for the `Xext/<ext>/` <-> `<ext>/` reorg), and `xx-make-pr` (both single- and multi-commit paths). Used an isolated `scripts/worktree add` throughout, not the shared checkout, per the Phase-1/Phase-2 collision notes above — zero collisions this time. **Verification approach, deliberately risk-scaled to each command's blast radius** (no real spurious PRs/pings created against `X11Libre/xserver`): `pr-label`/`pr-set-body`/`pr-append-body`/`pr-comment` verified byte-identical to their bash originals against **live** repo data (idempotent `set-review passed` re-apply on #3257; round-tripped **and exactly restored** PR #3103's body via append-then-set-body, diffed byte-for-byte after restore; bot-review banner text diffed directly against the bash version's output, both via a stubbed `gh`); `pr-request-reviewers` verified via a stubbed `gh` only (its real effect — pinging humans — isn't something to trigger just to test); `pr-checkout` run **live** end-to-end against real open fork PR #3253 (correctly detected the fork, wired the `fork` remote, checked out tracking `fork/<branch>`); `pr-amend-push` exercised against a scratch bare+clone repo (amend preserved Signed-off-by, force-with-lease pushed correctly, both the detached-HEAD and nothing-staged rejections fire); `xx-make-pr` run end-to-end against a scratch repo with a stubbed `gh`, both single- and multi-commit paths — confirmed it **faithfully reproduces** the documented `[PR #N]`-prefix-leak-onto-the-pushed-branch bug (`AGENTS.md` "PR workflow") rather than silently fixing it, since this is a parity port, not a redesign; `backport-commit`'s path-remap fallback covered by two new Go tests (`TestApplyViaPathRemap`, simulating the real `Xext/dpms/` -> `dpms/` reorg shape, and `_NotAReorg` for the genuine-conflict bail path) rather than a live run against `mk-agent-clone` (out of scope to fire a real cherry-pick/PR-create cycle just to test). One disclosed, deliberate divergence: the bash original's diff-path remap uses `sed` (so a `.` in a path is a BRE wildcard); the Go port uses a literal `strings.ReplaceAll` instead — behaviourally identical for every real path in this tree, intentionally not reproducing that edge case. **Cut over 2026-07-06 (Constellation, praetor go-ahead via Enterprise m0060):** `AGENTS.md`'s Key-commands row updated (subcommand list + a new dated bullet in the "Cut over to production" summary, `starfleetctl@e77934c`) to list all nine as preferred, same treatment as every prior round. **Bash originals intentionally NOT deleted** — `scripts/pr-checkout`/`scripts/backport-commit` still call bash `scripts/mk-agent-clone` (no Go port of that one yet) and `scripts/backport-commit` also calls bash `scripts/xx-make-pr.sh` internally; per the just-established "delete only once genuinely dependency-free" precedent (the `pr-view`/`pr-ci`/`show-pr-conflict`/`backport-applies` cleanup earlier this session), removal is a separate later step gated on `mk-agent-clone` itself getting a Go port. **`mk-agent-clone` ported, 2026-07-06 (Constellation, directive m0064).** `EnsureAgentClone()` in `internal/ghpr/mkagentclone.go` (`starfleetctl@221cabd`) — same config-section-copy approach as the bash original (drop+re-add `make-pr.*`/`remote.*` verbatim from the reference clone) and a new `mk-agent-clone` subcommand wrapping it. **`pr-checkout`/`backport-commit` now call it directly as a Go function — no more bash `scripts/mk-agent-clone` shell-out from either.** `backport-commit` already called `ghpr.RunXXMakePR` directly (a Go function call, not a shell-out) rather than bash `scripts/xx-make-pr.sh` — so with this change **both `pr-checkout` and `backport-commit` have zero remaining bash dependencies**. Verified against live workspace data: bash vs. Go clone of the same release (fresh + idempotent refresh) produce byte-identical git config and matching HEAD/branch state; `pr-checkout`'s stdout-contract (clone dir only, all progress on stderr) still holds after threading an `io.Writer` through `EnsureAgentClone` so callers can redirect the whole subtree's progress output like the bash original's differing `>&2` usage per call site. **Not added to `AGENTS.md`'s "preferred" list as a bare subcommand** — it's git-mutating (creates/fetches/checks-out local clones), same risk class as `dashboard`/`ws-commit` which both waited for an explicit go-ahead before their own cutover, so leaving that call to the praetor rather than assuming the m0064 go-ahead (which was for the port itself) extends to it; its logic is already in real production use today via the two already-cut-over callers. **Next:** `scripts/mk-agent-clone` (bash) can likely be deleted once the dependency audit confirms nothing else still calls it directly (only `pr-checkout`/`backport-commit` ever did, and both are now ported) — someone should double check with the same `grep -rl` audit used for the read-only cleanup before removing it.
  - **Phase 3 — fully automatic bootstrap, both fresh-project and re-init-after-clone.** Builds on the existing `./bootstrap` script and the already-parked "starfleetctl self-bootstrap command" TODO above (tool-authorization + file/dir setup from nothing). Must also handle **re-initializing an existing, previously-bootstrapped checkout after a fresh `git clone`** (i.e. idempotent from either a truly empty or a git-fresh-but-not-yet-`_WORK_`-populated state) — not just first-time setup. **Claimed by Farragut, 2026-07-06** (picked up autonomously per the standing "grab an open DASHBOARD theme" invitation, m0065). **Mostly DONE — one part genuinely blocked, flagged below rather than decided.** Built `starfleetctl bootstrap [--fix]` (`mpbt-hq/starfleetctl` commit `73ae87e`): idempotent self-check/self-repair of the fleet-management-specific one-time setup (`_WORK_/agent-bus`/`_WORK_/agent-claims` dir tree, the `starfleetctl` allowlist entries in `.claude/settings.json`) — deliberately narrow, doesn't duplicate `./bootstrap`'s broader mpbt-build-system job. The `.claude/settings.json` fix is a targeted text insertion after the `"allow": [` marker (not a full JSON re-marshal, which would reformat the whole shared/hand-edited file into a noisy diff), validated to still parse as JSON before writing. Verified against a mock fresh checkout: report-only correctly flags everything missing; `--fix` repairs cleanly; re-running either mode afterward is a true no-op (including a second `--fix` producing no duplicate settings.json entries); the specific "re-init after fresh clone" case (wipe just the gitignored `_WORK_`, leave tracked `settings.json` alone) detected and repaired correctly without re-touching the already-fine file. Ran `--fix` for real against this live checkout too — found and closed one genuine gap (`_WORK_/agent-bus/notify/.popup-once` hadn't been created). Wired into `./bootstrap` as a new step 4a, right after starfleetctl itself is built (commit `0b8b1ee`) — `./bootstrap` re-run end to end afterward, confirmed the new step fires and passes. **Side finding while testing this (documented in `AGENTS.md`'s starfleetctl section, not just here): `./run-fetch.starfleetctl` doesn't fast-forward the local checkout to new upstream commits** — only updates the remote-tracking ref, so `./run-build.starfleetctl` can silently build stale code right after a fetch (`git branch -vv` shows e.g. "2 behind"). Not root-caused (generic mpbt behavior vs. this solution's specific config — unconfirmed), just flagged as a gotcha; worked around locally with `git merge --ff-only origin/master` in the shared clone before rebuilding. **The genuinely blocked part, NOT decided here — needs the praetor's call:** the roadmap's premise of "fully automatic bootstrap from a truly fresh clone" runs into a real structural wall — confirmed via `gh repo view --json defaultBranchRef` that `master` is this repo's actual default branch (what a plain `git clone` checks out), and confirmed via `git cat-file -e origin/master:<path>` that `master` is **missing** `./bootstrap`, `AGENTS.md`, `DASHBOARD.md`, `LICENSE`, `scripts/agent-bus`, `scripts/starfleetctl`, and (per an earlier session's finding) the rest of the fleet tooling — all of it lives only on `mtx/agent-config`. `master`'s own `.claude/settings.json` is also a stale, much smaller snapshot (no fleet/agent-bus/starfleetctl entries, no hooks, no ship-name `statusLine`). So a genuinely fresh `git clone` with no prior knowledge has *nothing* to bootstrap with — not even `./bootstrap` itself exists there. Fixing this crosses the explicit, repeatedly-stated `AGENTS.md` "Licensing policy" line that `mtx/*` → `master` promotion is **"a deliberate, separate, later decision the [praetor] makes per item — don't propose or perform that promotion unprompted."** Did not act on this. Options for the praetor to choose from, not chosen here: (a) generalize some minimal subset onto `master` (e.g. just enough of a pointer/instructions to `git checkout mtx/agent-config`); (b) leave it as tribal knowledge / document it only in a human-facing onboarding message; (c) something else entirely. Everything this session's `bootstrap` subcommand does assumes you're already on `mtx/agent-config` — it solves the *re-init* half of Phase 3 completely, not the *branch-discoverability* half.
    **2026-07-06 (Farragut), continuing Phase 3 — `AGENTS.md` split into `agents.d/` fragments + `starfleetctl` `agents` subcommand.** Praetor proposal (chat, not a formal directive): keep root `AGENTS.md` **permanently fixed** (a short notice + one `@agents.d/index.md` import), move all actual content into `agents.d/<slug>.md` fragments, auto-generate the index — direct precedent from this same file's own `dashboard/themes/*.md` split (see the `split-dashboard-md-into-per-theme-files-…` theme). Confirmed technically sound first (subagent research against `code.claude.com/docs/en/memory.md`: Claude Code's `@import` resolves nested imports up to 4 hops, so root → index → fragment is well within range; no glob/directory-import exists, so an explicit generated index listing every `@agents.d/<slug>.md` file is required, not optional). Built `internal/agents` in `mpbt-hq/starfleetctl` (list/show/write/new/reindex/commit — same shape as `internal/dashboard`'s theme commands, including the shared `<gitdir>/mpbt-clone.lock`). **Migrated the real `AGENTS.md`** (was 1504 lines) into 23 fragment files, one per former top-level `##` section, each independently byte-verified against a pre-migration backup (not just trusting the migration script's own exit code) — zero content loss. Root `AGENTS.md` is now the fixed stub; `agents.d/index.md` is generated. Commit `5930dbc`/pushed `d358a76`.
    **Follow-on the same session, per praetor directive relayed via Enterprise (m0089):** `starfleetctl` should carry **its own** AGENTS.md-fragment content (the CLI usage instructions), versioned with itself, instead of that content being separately hand-maintained inside every consuming workspace's `agents.d/` — so a `starfleetctl` update that changes behavior and updates its own `README.md` carries the instruction update along automatically, no doc-sync step needed per consumer. Mechanism (`starfleetctl@15bd3f9`): a root-level `package starfleetctl` (`doc.go`, `//go:embed README.md` — embed can't reach outside its own file's directory, hence the small root package) exposing `Readme string`; new `agents install-self [--order <n>]` (default order 900) writes/overwrites `agents.d/starfleetctl.md` from that embedded README, tagged `owner: "starfleetctl"` in its frontmatter (unlike `agents new`/`write`, this command *always* clobbers — the fragment is tool-owned); a 6th `bootstrap` check (`verifySelfFragment`/`fixSelfFragment`) that distinguishes **missing** from **stale** (present but byte-different from what the current binary would install) so `bootstrap --fix` after any `starfleetctl` update also refreshes the instructions it carries. Verified end-to-end against scratch mock repos (fresh install, staleness correctly detected+repaired, idempotent re-run reports all-ok) before applying live. **Applied to this real checkout** (`mpbt-workspace@df93679`): `agents.d/starfleetctl.md` now exists, `agents.d/index.md` regenerated to include it, `scripts/starfleetctl bootstrap` reports all 6 checks green. **Next:** the deeper editorial follow-on m0089 also implied — trimming the now-partially-redundant starfleetctl-specific prose still embedded in some of the 23 migrated fragments (esp. `agents.d/concurrency-isolation-multiple-sessions-manual-work.md`, which deeply interleaves generic agent-bus/starfleetctl mechanics with XLibre-specific examples) — is **editorial judgment, not mechanical**, and was deliberately deferred rather than rushed; flagged here so it isn't lost.
    **2026-07-06 (Farragut): `genesis-init` subcommand — stand up the whole fleet from just a built `starfleetctl` binary, nothing else.** Directly answers the praetor's "kann man das 1:1 in ein neues Projekt kopieren" feasibility question and turns out to be **prerequisite 2** of the separately-tracked `starfleet-upstream-master-prep` theme's end goal (own praetor directive, relayed via Enterprise m0089/m0098: the whole fleet must be standable-up from a checkout of the new `wip/starfleet-upstream` branch alone, once prerequisite 1 — retiring the remaining bash originals, m0087/m0088 — lands; **this was built on the `starfleetctl` side only, deliberately not yet ported there, per Enterprise's explicit "don't get ahead of prerequisite 1" instruction**). `starfleetctl@452a3df`: `genesis-init [dir]` writes `cf/starfleetctl/{config.sh,solutions/default.yaml,packages/xlibre/starfleetctl.yaml}`, `run-fetch.starfleetctl`, `run-build.starfleetctl`, `scripts/starfleetctl`, and `scripts/ship-names.txt` from embedded templates (`internal/genesis/templates/`, `go:embed`) — verified none reference any project-specific detail (diffed against mpbt-workspace's own copies, only the "not part of the xserver build" comment line was genericized) — never overwrites an existing file, then runs every `bootstrap` check's `Fix` in sequence (same set `bootstrap --fix` runs: AGENTS.md/agents.d, DASHBOARD.md, `_WORK_` dirs, `.claude/settings.json` allowlist, the starfleetctl self-fragment via `install-self`). Wired as a standalone top-level subcommand (same group as `with-clone-lock` — skips `workspaceRoot()`'s AGENTS.md+scripts/ discovery, since by definition neither exists yet; takes an explicit target dir instead, default cwd). **Real gap found+fixed along the way:** `fixSettingsAllowlist` assumed `.claude/settings.json` already existed (every prior caller was an already-partially-set-up checkout) and errored outright from true genesis — now creates a minimal `{"permissions":{"allow":[]}}` skeleton first when the file is completely missing. **Verified against three scratch scenarios:** a truly empty git repo (init + one empty commit, nothing else) → `genesis-init` produces a fully operational `AGENTS.md`/`agents.d`/`DASHBOARD.md`/`.claude/settings.json`/`_WORK_` tree, `bootstrap` reports all 6 checks green afterward; re-running `genesis-init` against its own output correctly no-ops; a repo with a pre-existing non-empty `.claude/settings.json` gets the two starfleetctl entries merged in alongside its existing unrelated ones, still valid JSON. Also verified live against this real checkout (fast-forwarded the shared clone to `452a3df`, rebuilt, `scripts/starfleetctl bootstrap` still reports all-green — the settings.json fix didn't disturb the already-correct file). **Not yet ported to `wip/starfleet-upstream`** — waiting on prerequisite 1 per instruction; flagged as the next step once that lands. An earlier, narrower local `genesis` git branch (commit `80b852e`, mpbt-workspace, in a now-removed worktree, never pushed) predates this broader design and is **superseded** by it — that branch's bridge-`master`-to-`mtx/agent-config` idea is a different, narrower problem (this repo's own branch split) than `genesis-init`'s "any project, from absolute zero" scope; left as an unpushed local branch, not deleted, in case any of its README wording is still useful when `wip/starfleet-upstream` content actually gets drafted.
    **Same session, immediately after: closed the remaining gap — `genesis-setup` (`starfleetctl@61f6118`), the literal "one small script" from the praetor's original question.** `genesis-init` above still assumes a built `starfleetctl` binary already exists; getting there required a human to know the clone URL, run `make`, and remember to call `genesis-init` by hand. `genesis-setup` (repo root, executable, `git`/`go`/`make` only) is exactly that missing first step: clone-or-fetch starfleetctl, `make` it, exec `starfleetctl genesis-init <target-dir>`. Deliberately lives at the **starfleetctl repo root**, not inside mpbt-workspace — a brand-new project has no reason to know about mpbt-workspace at all, only about starfleetctl. **Verified end-to-end** against a truly fresh scratch git repo (`init` + one empty commit, nothing else) via a local `file://` clone source (`STARFLEETCTL_REPO_URL` override): one copied file + one run produces a fully `bootstrap`-green checkout; re-run afterward correctly no-ops at every step (fetch+ff-merge instead of re-clone, `genesis-init` reports nothing to do). **This is now the complete, tested answer to "kann man das 1:1 in ein neues Projekt kopieren, anstarten, und danach zieht sich der Rest automatisch selbst hoch"** — yes, `genesis-setup` is that one script. Still gated the same way as `genesis-init` on **not** porting anything onto `wip/starfleet-upstream` until prerequisite 1 (m0087/m0088) lands.
  - **Phase 5 — move `metux/starfleetctl` to the `mpbt` GitHub org** (https://github.com/mpbt-hq — praetor-provided URL, check it's the right org/that it exists first). Update every reference once moved: `cf/starfleetctl/*`, `run-fetch.starfleetctl`/`run-build.starfleetctl`, `scripts/starfleetctl`, this row, any local clone remotes. This is a real, visible GitHub repo-transfer action — explicitly authorized by the praetor in this same directive, so no separate confirmation needed, but do it carefully and document the before/after state here. **Claimed by Endeavour, 2026-07-06** — confirmed `mpbt-hq` org exists and `metux` (the `gh` identity) has admin role there, so the transfer is executable; proceeding. **Done, 2026-07-06 (Endeavour):** transferred via `gh api --method POST repos/metux/starfleetctl/transfer -f new_owner=mpbt-hq` (before: `metux/starfleetctl`, private; after: `mpbt-hq/starfleetctl`, private, same `master` branch/history — verified `gh api repos/mpbt-hq/starfleetctl` resolves post-transfer). Updated every reference: local clone's `origin` remote (`_WORK_/starfleetctl/sources/xlibre/starfleetctl`), `cf/starfleetctl/solutions/default.yaml`'s `xlibre_git:` base URL, the repo-path mentions in `cf/starfleetctl/config.sh` and `scripts/starfleetctl`'s comments, and `AGENTS.md`'s starfleetctl section + Key-commands row. GitHub redirects the old `metux/starfleetctl` URL transparently, but nothing in this workspace still points at it directly. Phase 5 complete. **Standing design constraint, praetor 2026-07-06: everything under the `mpbt-hq` GitHub org (starfleetctl and any future sister project placed there) must stay fully independent of XLibre/xserver specifics.** XLibre/mpbt-workspace is only the **first** major application of this tooling, not something it's allowed to be coupled to — a second, unrelated one ("sonicde") is already planned. Concretely: no hardcoded `X11Libre/xserver` repo slug, no assumptions baked in about xserver's release-line naming or directory layout, etc. — anything project-specific must stay a configurable default (env var / flag / config file), never a literal in the Go code. Worth a quick audit next time someone touches `internal/ghpr/` or similar: check for stray hardcoded `X11Libre`/`xserver`/`xlibre` strings that should instead be parameters. Not urgent enough to interrupt current in-flight work for — fold into the next natural touch of that code.
  **Also per this directive: other open themes in this file (Aktive Themen + Parkplatz) may be picked up autonomously by any idle ship while the praetor is away** — normal judgment call on what's worth doing, still bound by every other standing constraint in `AGENTS.md` (isolation via `mk-agent-clone`/`pr-claim`, `Signed-off-by`-only commits, no release-branch automerge, HW-domain-expert routing, NVIDIA-ABI caution, etc.). |
