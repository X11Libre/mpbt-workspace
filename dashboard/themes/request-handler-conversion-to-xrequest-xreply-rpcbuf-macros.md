---
slug: request-handler-conversion-to-xrequest-xreply-rpcbuf-macros
title: "Request-handler conversion to `X_REQUEST_*()`/`X_REPLY_*()`/rpcbuf macros"
category: active
status: "**Xinerama family DONE (2 PRs open); broader dix/Xext sweep pending praetor OK on scope/granularity**"
doc_ref: "PR #3229 (`panoramiXprocs.c`, 51 handlers), PR #3230 (`rrxinerama.c` + `pseudoramiX.c`); agent clone `_WORK_/xserver-master/agent/xreq/xserver`, branch `wip/xreq-panoramix`"
migrated_from: 8cf8692b9f0057ffe44e793b35bcf7329da83d3f
---

Praetor request 2026-07-02: convert **all** request handlers to the `dix/request_priv.h` macros (`X_REQUEST_HEAD_STRUCT`/`_AT_LEAST`/`_NO_CHECK`, `X_REQUEST_FIELD_*`, `X_REPLY_FIELD_*`, `X_SEND_REPLY_*`/rpcbuf), **especially Xinerama**; "erstmal nur PRs" (open only, don't merge). **Done:** the whole Xinerama family. `panoramiXprocs.c` was the big one (51 handlers) — those hook only `ProcVector[]` and do no request-field swapping (core dispatch's `SwappedProcVector` handles that), so it's a pure `REQUEST()`+`REQUEST_SIZE_MATCH/AT_LEAST` → `X_REQUEST_HEAD_*` conversion, swap blocks untouched; `-141/+51` lines. `rrxinerama.c`/`pseudoramiX.c` are the extension's *own* handlers (they swap their own req/reply fields) so they got the full HEAD+FIELD+REPLY_FIELD treatment, mirroring the already-converted `panoramiX.c`. Both build-verified (Xvfb/Xnest link; `pseudoramiX.c` is macOS-only → syntax-checked with real build flags). Dispatch demuxes (`Proc*Dispatch`, raw `REQUEST(xReq)` to read the minor opcode) correctly left as-is. **Gotcha hit:** `xx-make-pr.sh` on the 2nd PR exceeded the 120s foreground Bash timeout mid-run — it had already pushed the branch (clean, no `[PR #…]` marker since the rewrite step never ran) but hadn't done `gh pr create`; recovered by creating #3230 manually with `gh pr create`. **Remaining sweep (NOT started — needs praetor go-ahead on how granular/which files, and dix/dispatch.c is sensitive):** `git grep -c REQUEST_SIZE_MATCH|REQUEST_AT_LEAST_SIZE dix Xext` ranks the rest — `dix/dispatch.c` (~70, core protocol, most sensitive), `dix/swapreq.c` (~47, these are the *swap* side — different pattern), `Xext/glx/*` (glxcmds/vnd/glxcmdsswap, complex generated dispatch), `dix/events.c` (16), `dri2ext.c` (14), `dix/devices.c` (13), `present_request.c` (12), `randr/*` (rrcrtc 9, rrscreen/rrproperty/… 3-6 each), `record.c` (8), plus smaller ones. Recommend doing the clean extension files first (present, dri2, randr/*, record) as per-file PRs, and treating `dix/dispatch.c` + GLX as their own carefully-reviewed passes.
