---
slug: pr-review-backlog
title: "PR review backlog (`/bot-review`)"
category: active
status: "Ongoing, recurring — large backlog"
doc_ref: "`gh pr list --repo X11Libre/xserver --state open`"
migrated_from: 8cf8692b9f0057ffe44e793b35bcf7329da83d3f
---

2026-07-01 snapshot: **84 of 140** open PRs carry no `bot-review-*` label yet (32 of those are drafts). No dedicated tracking doc — this row *is* the tracker; re-check the count periodically rather than listing PRs individually. **2026-07-06 (Constellation, self-assigned during maintainer-away autonomy window, m0030):** re-checked count — still >100 unlabeled open PRs (many old/community/drive-by). Claiming a batch of non-backport, substantive master PRs to review this session: **#3257** (hurd OS-support+kbd driver), **#3253** (kdrive/fbdev refresh-rate), **#3228** (glamor CopyArea clamp, workaround for issue #3136 already tracked above), **#3216** (xfree86 ddc HDR metadata parsing), **#3199** (os: drop Linux abstract-socket support). Posting review comments + pass/changes-requested labels only, per the normal bot-review process — no merging (not the maintainer's own PRs, not master-auto-merge-authorized). Other free ships: pick a different slice to avoid duplicate reviews. **Batch done, all 5 `bot-review-passed`:** #3257 (advisory: no CI yet on fork, sequencing note vs #3194), #3253 (advisory: still draft, recommend @cepelinas9000 second look on shared `kmode.c`), #3228 (**flagged as a backport candidate** — client-triggerable OOB-read crash/DoS via fullscreen Present, applies once merged; still draft pending reporter confirmation), #3216 (deep-dove the tail-appended `xf86Monitor::hdr` field per its own ABI note — confirmed safe, all instances server-side `calloc`'d, never embedded by value), #3199 (new `unix_socket_is_live()` liveness check is a genuine hardening improvement beyond the abstract-socket removal itself; not clearly backport-worthy — feature removal, not a vuln fix, flagged as maintainer's judgment call). **Second batch done, all 3 `bot-review-passed`:** #3218 (verified the new `HAVE_SYS_*_H` conf_data keys need no template wiring — `configure_file` with no `input:` auto-generates defines); #3213 (**build-verified, not just CI-trusted** — checked out, built `libglx.so`+`libglamoregl.so`, confirmed via `nm -D` the previously-duplicated GLX symbols are now defined once in `libglx.so` and correctly `U`/dynamic in `libglamoregl.so`; this completes the incomplete #3174 fix `AGENTS.md`'s glx/glamor duplicate-symbol note already flagged); #3210 (traced `WalkTree`/`TraverseTree` to confirm the root window is visited too, not just descendants; `dix_priv.h` confirmed not in any SDK/install header list).
