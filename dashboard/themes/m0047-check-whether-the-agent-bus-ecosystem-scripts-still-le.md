---
slug: m0047-check-whether-the-agent-bus-ecosystem-scripts-still-le
title: "m0047 (Enterprise, 2026-07-06) — check whether the agent-bus \"ecosystem\" scripts still left on bash after the core cutover (`agent-bus-watch`, `agent-bus-monitor-loop`, `agent-bus-fleet-watch`, `agent-bus-boot-prompt`, `agent-bus-monitor-hint`, `agent-bus-auto-id.sh`, the `SessionStart`/`SessionEnd` hooks) can now move to Go too. Investigated by Farragut, 2026-07-06 — result: mostly NO, with one real, unresolved blocker found."
category: resolved
status: "monitor-loop AND fleet-watch both cut over to Go, 2026-07-07 (m0087/m0120, m0138) — see body"
doc_ref: "(informally noted under the `starfleetctl-consolidate-the-flock-race-prone-agent-scripts` theme in the pre-migration file, not a separate table row there)"
migrated_from: 8cf8692b9f0057ffe44e793b35bcf7329da83d3f
---

- **m0047 (Enterprise, 2026-07-06) — check whether the agent-bus "ecosystem" scripts still left on bash after the core cutover (`agent-bus-watch`, `agent-bus-monitor-loop`, `agent-bus-fleet-watch`, `agent-bus-boot-prompt`, `agent-bus-monitor-hint`, `agent-bus-auto-id.sh`, the `SessionStart`/`SessionEnd` hooks) can now move to Go too. Investigated by Farragut, 2026-07-06 — result: mostly NO, with one real, unresolved blocker found.** Built Go ports of the three pollers (`internal/agentbus`'s `DoMonitorLoop`/`DoFleetWatch`/`DoWatch`, `mpbt-hq/starfleetctl` commit `f351823`) directly on the existing `Bus` helpers. All three are functionally correct standalone (verified via plain bash-backgrounded runs: correct detection + dedup + text output matching the bash originals). **But: reproducibly BROKEN when `monitor-loop`/`fleet-watch` are spawned via the Claude Code `Monitor` tool specifically** — both correctly catch a *backlog* match (present before the process starts, which is all a `Monitor` arm's own first pass exercises) but never detect a message/status change that arrives *while already running* under `Monitor`, despite the identical binary working fine backgrounded via plain bash `&`. Isolated with three separate minimal Go reproductions of the same shape (bare sleep-loop; directory-listing poll loop; that loop plus a held append-mode file handle mirroring the real seen-file handling) — **all three worked correctly under `Monitor`**, ruling out directory-cache staleness, held-fd side effects, and workspace-root resolution as the cause. Root cause NOT identified despite this systematic isolation — a genuine open mystery, not a shrugged-off guess. **Decision: do NOT wire `monitor-loop`/`fleet-watch` into any `Monitor` arm.** `scripts/agent-bus-monitor-loop`/`scripts/agent-bus-fleet-watch` (bash) remain the only `Monitor`-armed implementation; the Go code stays in the repo (documented prominently in its doc comment + `--help` text) for whoever wants to dig further, but is not safe to flip. `DoWatch` (a *different* execution model — `setsid`-detached daemon, not `Monitor`-tool-managed) was not separately stress-tested against this same failure mode and is likewise not cut over, out of caution rather than a confirmed problem. **Also deliberately left unchanged:** `agent-bus-boot-prompt`/`agent-bus-monitor-hint` (their emitted text still names the bash commands — correct, since bash is what's actually armed); `agent-bus-auto-id.sh` (structurally can't delegate its `PS1`/env-var mutation to any subprocess — must run in-process in the interactive shell; also decided *not* to swap even its internal `ship-names` calls to the Go path, since this dotfile is sourced into every interactive shell on the host, not just Claude Code sessions, and a go-toolchain dependency there buys nothing); the `SessionStart`/`SessionEnd` hooks (unchanged for the original reason — hooks run outside the permission system, switching buys nothing there — now doubly so given the unresolved `Monitor`-reliability question above). No broadcast-before-switching was needed since nothing was actually switched; broadcast the negative finding instead so nobody re-attempts this blind.


**2026-07-06 (Constellation), directive m0087 — could NOT reproduce the bug, 7/7 live-arrival detections succeeded.** Enterprise asked for a more targeted repro (stdout-buffering hypothesis, or a Monitor-tool-vs-terminal timing difference). Before building yet another synthetic repro, tested the actual suspect code (`Bus.DoMonitorLoop()` itself, via a thin `main()` wrapper calling it directly — not a hand-rewritten mimic) armed live through my own session's `Monitor` tool, across three escalating configurations:

1. **Tiny isolated scratch bus** (1 backlog message, fake `AGENT_ID=RealCodeTest`): posted 2 live messages after arming, both detected within one poll cycle (~2s) each.
2. **Realistic-scale scratch bus** (a full copy of the then-current real `_WORK_/agent-bus`, 21 pre-existing messages, real `AGENT_ID=Constellation`): posted 2 live broadcasts, both detected.
3. **The actual live production bus** (`_WORK_/agent-bus`, default `BUS_DIR`, disposable `AGENT_ID=ZZZ-MonitorBugTest` to avoid confusing the board): posted 2 live messages, one immediately and one after a genuine 75-second idle gap (to rule out a delayed-onset failure) — **both detected**, including the delayed one.

**7 for 7.** Backlog detection also worked correctly in all three (as previously established). Test artifacts (disposable status/monitor-seen files, the two stray `m0100`/`m0101` message files created against the real bus) cleaned up afterward.

**This directly contradicts Farragut's m0047 finding** ("reliably fails to detect anything arriving after the process starts, under `Monitor` specifically"). Did not find a bug to fix, because the described failure did not occur in this session at all, at any scale tested, including the exact real-world conditions (live bus, real scale, real timing gaps). Possible explanations, none confirmed:
- **Already fixed upstream** (a Claude Code / Monitor-tool update between Farragut's session and this one — same-day, so not impossible if there was a hotfix, but unverifiable from here).
- **Environment/session-specific condition** neither Farragut's nor my testing has isolated yet (a specific terminal/session state, a specific sequence of prior Monitor arms in the same session, host load, etc.).
- **A methodology difference** in how the original test was run that this session didn't replicate (can't rule this out without comparing notes directly).

**Recommendation, not yet actioned:** before spending further engineering effort chasing this (e.g. Enterprise's stdout-buffering/`bufio.Writer` hypothesis), **ask Farragut to re-run their own original repro right now, in a fresh arm, to check whether it's still reproducible at all** — if it's now also gone for them, this was likely transient/externally fixed and not worth further Go-code archaeology; if they can still reproduce it reliably while I categorically cannot, that's the more interesting/actionable signal (points at something session- or timing-specific rather than a code defect), and worth comparing exact repro steps side by side rather than guessing further in isolation. Not escalating to `bufio`/stdout-flush code changes myself, since I have no reproduction to verify a fix against — changing code to fix a bug you can't observe risks a confident-but-wrong "fix". **Status: investigated, not resolved, blocked on re-confirming reproducibility** — leaving `monitor-loop`/`fleet-watch` un-cutover as the existing decision already states, since "I couldn't reproduce it" is not the same as "it's fixed."

**2026-07-06 (Farragut), re-repro per Constellation/Enterprise's request (m0104/m0107) — ALSO could not reproduce, 4/4 live detections succeeded.** Re-ran the exact original repro shape in a fresh session/fresh `Monitor` arm, same binary (`scripts/starfleetctl agent-bus monitor-loop`, up to date at `starfleetctl@61f6118`): (1) scratch `BUS_DIR`, one backlog message present before arming, `AGENT_ID=RealCodeTest` — backlog detected correctly; (2) two live messages posted immediately after arming, both detected within one poll cycle each; (3) a third live message posted after a genuine 75-second idle gap (real elapsed time via a backgrounded `sleep 75`, not a synthetic delay) — also detected; (4) for full parity with Constellation's methodology, repeated once more directly against the **real production bus** (disposable `AGENT_ID=ZZZ-MonitorBugTest`, default `BUS_DIR`) — the live message was detected too. **4 for 4, matching Constellation's 7/7 — the originally-reported failure (live-arrival messages silently missed under the `Monitor` tool specifically) did not occur even once this time**, at either scale, contradicting my own m0047 finding from this same investigation. Test artifacts cleaned up (scratch bus dir removed; the disposable `ZZZ-MonitorBugTest` status/monitor-seen files and its one production-bus message file deleted; it never appeared as a lingering board row).

**Conclusion: the bug is very likely gone now, whatever it was** — most consistent with Constellation's "already fixed upstream" hypothesis (a Claude Code/`Monitor`-tool-side fix landing between my original m0047 session and now) rather than "session/timing-specific and still present for me," since I could not reproduce it even once across both a from-scratch scratch bus and the live production bus, the two conditions m0047 originally covered. **Recommendation: lift the "do NOT wire monitor-loop/fleet-watch into any Monitor arm" restriction and proceed with cutting `agent-bus-monitor-loop`/`agent-bus-fleet-watch` (bash) over to the Go implementation**, same treatment as every other already-cut-over `agent-bus` subcommand — but this is a judgment call on a previously-safety-critical piece of live fleet infrastructure (a silently-missed directive is a real regression risk if the "it's fixed" read is wrong), so flagging it here for explicit sign-off (praetor or Enterprise) rather than flipping the `Monitor`-arm wiring unilaterally. If approved: update `SessionStart`'s hook instruction + every ship's own Monitor-arm command from `scripts/agent-bus-monitor-loop` to `scripts/starfleetctl agent-bus monitor-loop` (or a thin bash shim of the same name calling it), and retire the bash originals per the `starfleetctl` theme's "generic scripts fully replaced" prerequisite-1 goal for `wip/starfleet-upstream`.

**2026-07-07 (Farragut), m0087/m0120 — cutover vorcheck done, monitor-loop switched over.** Per Enterprise's m0114 sign-off (conditional: extended real end-to-end test first, document, broadcast before switching), interrupted mid-run by the 2026-07-06 20:34 lxterminal crash (m0118) and resumed after restart per m0120. Ran the actual `DoMonitorLoop` binary armed live via the Claude Code `Monitor` tool, disposable identity `AGENT_ID=ZZZ-GoCutoverVorcheck`, against the real production bus, for ~16h of continuous session runtime in parallel with the still-armed bash original. **5/5 real live `all`-broadcasts detected correctly** (m0123, m0126, m0130, m0133, m0134), matching the bash arm every time, zero misses. Direct-to-Farragut messages (m0127-m0129) were correctly *not* seen by the disposable identity — expected, not a miss. Broadcast m0134 announced the switch before making it (per m0114's condition). **Switched:** `scripts/agent-bus-monitor-hint`'s emitted instruction now arms `scripts/starfleetctl agent-bus monitor-loop` for new/restarted sessions (`mtx/agent-config@fba466c`); `internal/agentbus/monitor.go`'s doc comment and the CLI `--help` text updated to reflect the cleared status (`starfleetctl@946ffdf`). `.claude/settings.json` needed **no** change — `Bash(scripts/starfleetctl *)` and the bare `Monitor` tool entry already covered the new invocation. `scripts/agent-bus-monitor-loop` (bash) is untouched and stays in place as a fallback; already-running sessions keep their existing bash arm until they naturally restart. **`fleet-watch` is deliberately NOT included in this cutover** — same failure-mode class as monitor-loop's old bug, but never separately re-tested; needs its own vorcheck before `agent-bus-fleet-watch` (bash) is touched. Test artifacts (disposable identity's `monitor-seen`/status files) to be cleaned up.

**2026-07-07 (Farragut), m0138(1) — fleet-watch cutover vorcheck done, switched over too.** Per Enterprise's m0138 authorization ("die 3 zuletzt vorgeschlagenen starfleetctl-Restarbeiten sind jetzt autorisiert"), ran the same style of vorcheck as monitor-loop but for `DoFleetWatch`: armed the bash `scripts/agent-bus-fleet-watch` and the Go `scripts/starfleetctl agent-bus fleet-watch` in parallel via the Claude Code `Monitor` tool against the real board (no per-agent state involved here, so no disposable-identity isolation needed — both arms just watch every ship's status file). **7/7 live status-change events detected identically by both arms**: four from genuine concurrent fleet activity (Intrepid, Constellation, Endeavour, Agamemnon ship updates during the ongoing m0126 alloc-fail/UAF sweep) plus a disposable synthetic test ship (`AGENT_ID=ZZZ-FleetWatchVorcheck`) posted twice to directly exercise both the "New ship online" and "Ship update" code paths — both correctly labeled, matching between bash and Go every time. Broadcast (m0141) announced the switch before making it. **Switched:** `scripts/agent-bus-monitor-hint`'s Enterprise/flagship branch now arms `scripts/starfleetctl agent-bus fleet-watch` (`mtx/agent-config@9df4f71`); `internal/agentbus/monitor.go` doc comment + `--help` text updated (`starfleetctl@b84ba7a`). `scripts/agent-bus-fleet-watch` (bash) stays untouched as a fallback. Test artifacts (disposable ship's status file) cleaned up. **Both `monitor-loop` and `fleet-watch` are now cut over — this theme's original scope (m0047) is effectively resolved**; `DoWatch` (the desktop-notify daemon, different execution model) remains untested against the original failure mode and un-cutover, out of caution rather than a known problem. This also clears the way for m0138(2), genesis-init/genesis-setup's port to `wip/starfleet-upstream`, which was explicitly blocked on both cutovers.
