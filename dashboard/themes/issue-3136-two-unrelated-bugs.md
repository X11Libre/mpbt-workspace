---
slug: issue-3136-two-unrelated-bugs
title: "Issue #3136 (veloren fullscreen crash) — **two unrelated bugs**"
category: active
status: "Bug1 workaround PR open (draft); Bug2 investigating + 2 side-leaks fixed"
doc_ref: "issue #3136; **PR #3228** (draft, glamor CopyArea OOB workaround); leak PRs **#3226** (glx — **MERGED** `f045ee09`; backports #3236/#3237/#3238) + **#3227** (panoramiX — **⇒ REBASE DUE**, #3231 merged) + **#3231** (xinerama macros→variadic, **MERGED**); agent clones `.../glamor-wa`, `.../glamor-bisect`, `.../leakfix`, `.../xinerama-macro`"
migrated_from: 8cf8692b9f0057ffe44e793b35bcf7329da83d3f
---

**Bug 1 — glamor OOB source read → crash (the issue's subject):** `glamor_upload_boxes` back-projects dest-clamped box coords into the source buffer without bounding to the source allocation; fullscreen Present geometry reads past `bits` → SIGSEGV in mesa. **Bisect finding:** the unclamped-source design is **pre-21.1** (not the regression); the real regression suspect is the 2023 upstream depth-24-on-32 alpha-fixup path (`5371690891`+`3e044b1e64`, absent in 21.1) with a wrongly-sized `tmp_bits` — but reporter's crash used the *plain* path (bits==src), so needs runtime gdb data (metux requested it) to pin. **PR #3228** = provably-safe defensive clamp in the plain CPU-copy path (crash→glitch), **draft/RFC pending reporter confirmation + HW review**; does NOT cover the tmp_bits path. **Backport-applicable to 25.2/25.1/25.0** (identical unclamped call at `glamor_copy.c:272` on all three; reporter saw all crash) — flagged only, not opened (draft + release-merge-manual policy). **Bug 2 — uninitialised memory → client (info-leak), unrelated:** valgrind flagged uninit bytes written via `__write_reply_hdr_and_rpcbuf`. Audited all ~14 non-zeroing `x_rpcbuf_reserve` callers → **all fully fill** (arrays lückenlos, padding-free structs, or compound-literal assigns that zero padding); rpcbuf is likely NOT the source. Two genuine *side* leaks found instead (bare reply struct + field-by-field fill via `X_SEND_REPLY_SIMPLE`): **glx `dispatch_GLXQueryVersion`** (→ **PR #3226**, designated-init) + **panoramiX `PanoramiXAllocColor`** (→ **PR #3227**, designated-init). The enabling `XINERAMA_FOR_EACH_SCREEN_*`→variadic change was split into **PR #3231 — now MERGED**. **NEXT ACTION (pending):** rebase #3227 onto current `origin/master` (`pr-checkout 3227` → `git rebase origin/master` → the now-duplicate `panoramiX.h` hunk drops as already-applied, leaving only the `panoramiXprocs.c` reply-init → `pr-amend-push`/force-push). #3227 still carries both files (built standalone); cross-linked via a comment. Backport: **glx leak MERGED to master (#3226) + backports opened 2026-07-02** → **#3236** (25.2) / **#3237** (25.1) / **#3238** (25.0, adapted to the older `SetReplyHeader`/`WriteToClient` idiom; 25.2/25.1 applied via `backport-commit`), all build-verified, cross-linked (dashboard on #3226 + back-links), **not merged** (release-merge-manual); panoramiX leak on **25.2/25.1** (on 25.0 `PanoramiXAllocColor` delegates to the core handler, no reply built → N/A) — flagged, not opened. Reporter's exact WITH_RPCBUF leak still unpinned → asked reporter for `valgrind --track-origins=yes`. Note: `= {…}` zeroes omitted *fields* (std) + padding (gcc/clang, not std-guaranteed). **panoramiX leak backported, 2026-07-02:** praetor asked to check #3227 for backport-worthiness — confirmed (real client-triggerable info disclosure) and independently re-verified applicability by reading the actual code on each branch (25.2/25.1 both still have the bare `xAllocColorReply reply;` + field-by-field fill; 25.0 confirmed delegates to `SavedProcVector[X_AllocColor]`, no reply built there → N/A, matching the earlier assessment). Backported **both** #3227 and its prerequisite #3231 to each applicable release (in dependency order, macro-enabler first): **25.2** → PR #3232 (macro) + PR #3233 (fix, depends on #3232); **25.1** → PR #3234 (macro) + PR #3235 (fix, depends on #3234). All 4 build-verified (`meson setup` + `ninja hw/vfb/Xvfb hw/xnest/Xnest`, clean). Cherry-picks applied via 3-way merge with no manual conflict resolution needed on either branch (git's merge correctly dropped the redundant macro-header hunk when applying the fix commit after the macro commit was already present). All 6 PRs (#3227, #3231 masters + #3232-3235 backports) cross-linked (backport dashboards on the masters, back-links + depends-on notes on the backports). Not merged — release-line merges are manual/praetor-only. **Side finding, not mine to resolve:** while creating the 25.2 and 25.1 agent clones, their local `rfc/backport-25.2`/`rfc/backport-25.1` incubators carried extra commits beyond what's pushed to `origin` (e.g. a DGA QueryMode padding fix, a `glamor_egl.c`/`xorgGlxCreateVendor` fix on 25.1) that aren't on `origin/rfc/backport-<rel>` — looks like other in-flight session(s)' pending work on those incubators, not pushed yet. Left untouched (reset my own clones to `origin`'s state before cherry-picking, to avoid clobbering it) — flagging in case those sessions lost track of it. **Macro-enablers merged + fixes rebased, 2026-07-02:** praetor asked to merge the macro-enabler PRs; flagged the AGENTS.md "release merges are manual-praetor-only, agents stop after opening" rule first (master auto-merge-on-request is fine, release-branch auto-merge is not) — praetor merged **#3232** (25.2) and **#3234** (25.1) themselves. (**#3231** on master was already merged earlier.) With all three macro-enablers landed, rebased the three dependent fix PRs onto their now-updated targets: **#3227** (master, via `pr-claim`+`pr-checkout`) and **#3233**/**#3235** (25.2/25.1, in the existing backport clones) — each rebase cleanly dropped the now-redundant macro hunk (git recognized it as already-applied), leaving each PR as a single-file, ~15-line `panoramiXprocs.c` diff. All three re-verified (`meson setup` + `ninja hw/vfb/Xvfb hw/xnest/Xnest`, clean) and force-pushed. All 6 PR bodies updated to reflect the merged/rebased state. **#3227/#3233/#3235 are now ready for review/merge** — master (#3227) could be merged on request; the two release PRs still need the praetor's manual merge per policy.
