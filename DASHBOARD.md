# DASHBOARD.md

Living index of **themes/initiatives** — not individual PRs (GitHub already tracks those; don't
duplicate `gh pr list` here). Purpose: let parallel agents (and the praetor) see at a glance
what's actively being worked, and stop half-started ideas from getting lost when a session ends.

Two sections:

- **Aktive Themen** — anything with real state (a branch, a doc, an open decision) that isn't
  finished yet. One row per theme, link to its detail doc/branch/PR.
- **Parkplatz** — noticed-but-not-started, or started-then-set-aside. Short note + date, so it can
  be picked up later instead of re-discovered from scratch.

**Maintenance rule** (see `AGENTS.md` "Working practices"): when you start, pause, or finish a
theme, update this file in the same session — don't rely on memory or chat history surviving.
Ephemeral live-status (who's online right now) stays in `agent-bus`/`pr-claim`; this file is for
the theme itself, so it stays useful after those TTLs expire.

---

## Aktive Themen

| Thema | Status | Doc / Branch / PR | Notiz |
|---|---|---|---|
| Long-term fleet architecture vision: bridge command frontend, multi-workspace federation, decentralized/isolated execution | **Vision/direction from the praetor, 2026-07-06 — not a concrete task queue yet**, recorded here so it isn't lost between sessions. Concrete near-term work is still the starfleetctl 5-phase roadmap (see that row); this row is the longer horizon it's evolving toward | — no branch/PR yet | Triggered by an Enterprise research request into **Paperclip AI** (`github.com/paperclipai/paperclip`, MIT, self-hosted agent-orchestration platform — Node/React/Postgres, org-chart/governance, atomic task checkout, budget/token tracking, git-based workspaces, web dashboard, explicit Claude Code adapter). **Praetor's explicit intent: do NOT adopt/integrate Paperclip itself** — instead treat it as a reference architecture to learn from, and evolve `starfleetctl` incrementally toward equivalent capabilities natively (budget/cost tracking and structured task/goal tracking are the concrete gaps Paperclip's docs made obvious — currently `DASHBOARD.md` + `agent-bus`/`pr-claim` cover this ad hoc, with no cost accounting at all). **Command frontend is a deliberately different, non-Paperclip direction: a literal Star-Trek-bridge-style command center** — large monitor/TV + voice interface + interactive on-screen display, not a browser dashboard tab. Fits the existing ship-name/fleet theme (see "Ship names for fleet identity" in `AGENTS.md`) rather than importing Paperclip's web UI wholesale. **Next horizon after the current 5 phases — meta-commandeering across possibly-separate workspaces**, not just coordination within one `mpbt-workspace` checkout: goal is **security isolation** — individual agents/fleets working in separated, decentralized execution areas (e.g. workers on different machines, or provisioned on-demand in a k8s cluster), rather than everything running on one host sharing one `_WORK_/agent-bus/` directory as today. Two concrete next-step options raised by the praetor (**not yet decided/committed, no ship assigned**): (1) a **real network interface** for cross-host communication/coordination — supersedes the current file-based polling model (`_WORK_/agent-bus/`, `flock`-serialized, single-host by construction) with something that actually works across machines; revisits the earlier "would pipes/sockets beat polling" discussion, but now motivated by the multi-host goal, not just local efficiency. (2) **autonomous background agents that don't need an interactive Claude Code client in the loop at all** — likely means driving agents via the Claude Agent SDK/API directly for always-on headless workers, instead of today's model of orchestrating real `claude` CLI sessions inside `tmux` (`agent-run`/`agent-attach`). Both are genuinely new architecture, not incremental script work — do not start implementation without the praetor picking a direction first. |
| `starfleetctl` (renamed from `mpbtctl`): consolidate the flock/race-prone agent-scripts (`agent-bus`, `pr-claim`, `ws-commit` first) into one Go CLI | **In progress 2026-07-03, assigned to Farragut, successive/incremental — `agent-bus` subcommand ported, parity-verified, now its own repo + mpbt solution** | **own repo `mpbt-hq/starfleetctl`** (private, `master`, commit `77009c2`; moved from `metux/starfleetctl` 2026-07-06, see the Phase 5 entry below), wired as an mpbt-workspace **solution** like go-x11proto/flyingtux: `cf/starfleetctl/{config.sh,solutions/default.yaml,packages/xlibre/starfleetctl.yaml}` + `run-fetch.starfleetctl`/`run-build.starfleetctl`, clone lives at `_WORK_/starfleetctl/sources/xlibre/starfleetctl` | Motivation from chat with praetor: (1) one `Bash(starfleetctl)`/`Bash(starfleetctl *)` allowlist entry replaces ~30 per-script pairs in `.claude/settings.json`, and every future subcommand is covered automatically (tradeoff: also less granular — a new subcommand inherits full trust with no separate review step); (2) Go's `encoding/json` + `os/exec` arg-arrays eliminate a real class of bash bugs (quoting/word-splitting, brittle jq/sed JSON parsing, `set -e` silently swallowed in pipelines) — but CI/VM flakes, `gh` CLI quirks (e.g. broken `gh pr edit`), and the Claude-harness command-prefix-matching gotcha are external and would need re-encoding in Go just the same, not eliminated by the rewrite. **Not** a big-bang rewrite of all ~30 `scripts/*` — start with the highest-value/highest-race-risk ones (agent-bus/pr-claim/ws-commit), leave single-purpose scripts (e.g. `show-branch-file`) as bash. **Critical constraint: `scripts/agent-bus` is live infrastructure the whole fleet depends on right now** — build the Go equivalent alongside the existing bash version against the same `_WORK_/agent-bus/` file format, validate parity, and only cut over once proven equivalent; never let a WIP `starfleetctl` break the running fleet's coordination. Licensing: AGPL-3.0-or-later (workspace tooling, not an xserver/driver deliverable — see AGENTS.md "Licensing policy"), SPDX headers per file. Update this row every session that touches it, per AGENTS.md's theme-tracking rule — this is exactly the kind of multi-session initiative that gets lost otherwise. **2026-07-03, part 1 (Farragut):** built `agent-bus` subcommand (all 12: status/clear/inbox/ack/board/tell/broadcast/ask/reply/asks/msgs/events/prune), stdlib-only Go, `syscall.Flock` for the same mutex the bash `flock 9` uses, byte-for-byte TSV file format match with `scripts/agent-bus`. **Verified parity** against a scratch `BUS_DIR` with mixed bash-writes/go-reads and vice versa (status/board, tell/broadcast/inbox/ack/msgs/events, staleness+`[STALE]`+prune, a cross-impl ask→reply round trip, matching exit codes) — landed first on branch `mtx/mpbtctl` in mpbt-workspace (based on `mtx/agent-config`, not `master` — `AGENTS.md`/`DASHBOARD.md`/`LICENSE`/`scripts/agent-bus` only exist there). **2026-07-03, part 2 (Farragut), same session:** praetor decided — instead of living inside mpbt-workspace (or being folded into the `mpbt`/`mpbt-builder` Go repo, the other option discussed), it should be its own sister project **exactly like go-x11proto/FlyingTux**: own repo, cloned+built via an mpbt solution. Renamed `mpbtctl`→**`starfleetctl`** (fits the existing ship-name/fleet theme), created `metux/starfleetctl` (private GitHub repo), migrated the code there (module path + `cmd/` dir renamed to match), added a `Makefile` (`go build` via mpbt's `exec` builder, same shape as go-x11proto's), wired `cf/starfleetctl/` + the two `run-*.starfleetctl` wrappers on `mtx/agent-config`. **Verified end-to-end**: `./run-fetch.starfleetctl` clones cleanly, `./run-build.starfleetctl` builds via the real `mpbt-builder` (not just `go build` by hand), and the resulting binary's `agent-bus` subcommand still interoperates with `scripts/agent-bus` on a shared scratch bus. Old `mtx/mpbtctl` branch in mpbt-workspace is now **superseded** — worktree removed, branch left pushed but unmaintained (not force-deleted; praetor's call whether to delete it). **Next session:** `pr-claim` subcommand in `metux/starfleetctl`; wire a `Bash(starfleetctl)`/`Bash(starfleetctl *)` allowlist entry once there's an actual cutover plan — still bash-only in production for now. **TODO flagged by praetor, 2026-07-03 (Pegasus):** `starfleetctl` still needs a **self-bootstrap command** to set itself up from scratch in a brand-new, completely empty workspace — in particular tool-authorization (the `.claude/settings.json` allowlist entries this same row already discusses) and creating whatever files/dirs it expects to find (currently all hand-wired by mpbt-workspace's own scripts/config, not something `starfleetctl` can do for itself yet). Not scoped/designed yet — just parked here so it isn't lost before the `pr-claim` subcommand work above. **2026-07-03 (Endeavour): first subcommand actually cut over to production — `dashboard`.** Praetor asked to give `starfleetctl` a high-level DASHBOARD.md access function so dashboard operations "go through it" without needing separate explicit authorization each time. Built `internal/dashboard` (Go port of `scripts/dashboard`: `pull`/`show`/`write`/`commit -m <msg> [--no-push]`), committed+pushed to `metux/starfleetctl` (`bb96557`). Deliberately shares the *exact same* `<gitdir>/mpbt-clone.lock` file `scripts/with-clone-lock`/`scripts/ws-commit` use (not a separate lock), so a Go and a bash actor mutating this clone at once still serialize instead of racing — **verified genuinely interoperable**, not just parallel-implemented: in a scratch bare+clone repo, a Go `dashboard commit` timed out (reported the correct holder PID) while a concurrent bash `with-clone-lock sleep` held the lock, then succeeded immediately once released. Also verified: pull/show/write/commit round trip, `show`'s stdout/stderr split (file content only on stdout, git chatter on stderr, matching the bash version exactly), the nothing-staged no-op, and `--no-push`. **Wired the actual cutover**, not just the code: added `scripts/starfleetctl` (thin exec wrapper — rebuilds the binary when source is newer, execs with `MPBT_WORKSPACE_ROOT` resolved, so it's invoked the same relative way every other fleet script is) and pre-authorized it in `.claude/settings.json` as `Bash(scripts/starfleetctl)`/`Bash(scripts/starfleetctl *)` — the first real instance of the "one blanket entry auto-covers every subcommand" trade-off this row already flagged as the motivation, now actually exercised (`agent-bus` subcommand rides along under the same grant, but is **not** yet the production path — only `dashboard` has real callers switched over; `scripts/agent-bus` bash remains authoritative for the fleet bus). `scripts/dashboard` (bash) is left in place, not removed — both implementations share the lock and the file format, so they coexist safely; `scripts/starfleetctl dashboard …` is now the **preferred** path per `AGENTS.md`'s updated Key-commands entry. **Next:** migrate the `pr-claim` subcommand next (per Farragut's prior note), then reassess whether `scripts/dashboard`/`scripts/agent-bus` bash wrappers can be deprecated once every caller (including this file's own maintenance instructions) references `starfleetctl` instead. **2026-07-03 (Farragut): worked the rest of the original TODO autonomously per Enterprise directive (m0024) — `pr-claim`, `ws-commit`, plus a self-identified extra, `ship-names`.** All three follow the established pattern (stdlib-only Go, same file formats/lock files as the bash originals) and are pushed to `metux/starfleetctl` (`d715ecc` pr-claim+ws-commit, `4806c9a` ship-names). `pr-claim`: full command set (claim/refresh/steal/release/release-all/list/who), verified against a shared `CLAIM_DIR` with mixed bash/Go callers including exit codes and the events.log format. `ws-commit`: shares the *exact same* `<gitdir>/mpbt-clone.lock` as `scripts/with-clone-lock`/`scripts/ws-commit`/`internal/dashboard` — verified with a scratch bare+clone pair that a Go commit genuinely blocks on a bash-held lock (reported the real holder PID) and that bash/Go commits interleave cleanly via `pull --rebase`; also verified `--no-push` and `-a`/`--all`. `ship-names` (not originally requested — self-identified via `grep -l flock scripts/*` per the directive's "check independently for further candidates" instruction, same judgment call as `dashboard` earlier): covers `assign` (general + flagship), `release`, `list`, `gc`, `flagship`, sharing `_WORK_/agent-bus/ships/` and the `.assign.lock` (fixed 10s wait); verified byte-for-byte identical `list` output and that `assign` genuinely blocks on a concurrently-held lock (measured ~2s against an externally-held flock). **Still bash-only in production** for all three (unlike `dashboard`, which already has `scripts/starfleetctl` + the settings.json cutover) — no `.claude/settings.json`/AGENTS.md changes made for these, per the standing constraint that a cutover needs proven parity **and** the praetor's go-ahead, which hasn't been sought yet for these three. **No more flock-based `scripts/*` candidates found** after `ship-names` (checked via `grep -l flock scripts/*`, only `with-clone-lock`/`ws-commit`/`pr-claim`/`agent-bus`/`ship-names` match, all now covered — `dashboard` doesn't itself use `flock` directly, it shells out to `with-clone-lock`'s lock file). **Next:** praetor decision on cutting any of pr-claim/ws-commit/ship-names over to production the way `dashboard` was; the parked self-bootstrap-command TODO above is still open too. **TODO flagged by praetor, 2026-07-03 (Pegasus):** move the `starfleetctl` repo from the personal `metux/starfleetctl` account into an `mpbt` GitHub organisation (not yet created/confirmed to exist — needs checking/setting up). Not scoped yet: whether the org already exists, whether it'd go public or stay private, and everything that references the current `metux/starfleetctl` path today (`cf/starfleetctl/*`, `run-fetch.starfleetctl`/`run-build.starfleetctl`, `scripts/starfleetctl`, this DASHBOARD.md row, any local clone remotes) would need updating to the new org path. Parked here alongside the other open starfleetctl TODOs, not yet actioned. **2026-07-03 (Farragut): praetor gave the go-ahead ("wenn alles gut genug funktioniert, können wir umschalten") — cut `pr-claim`, `ws-commit`, and `ship-names` over to production, same treatment as `dashboard`.** Updated `AGENTS.md`'s Key-commands entry for `scripts/starfleetctl` to list all four as preferred over their bash namesakes (no new `.claude/settings.json` entry needed — the existing blanket `Bash(scripts/starfleetctl)`/`Bash(scripts/starfleetctl *)` grant already covers every subcommand). Bash originals left in place, unchanged, coexisting via the shared file/lock formats — nothing forces any caller to switch. **`agent-bus` explicitly excluded from this cutover** — it's fleet-wide live infrastructure (every session's heartbeat/inbox), a fundamentally different risk class from the other four's single-session/advisory scope, so it stays bash-authoritative until separately, explicitly approved. Verified the cutover itself is real, not just documented: the AGENTS.md commit for this change (`12305f7`) was made *with* `scripts/starfleetctl ws-commit` instead of `scripts/ws-commit` — a genuine production use, not a scratch-dir test. **2026-07-06 (Enterprise): `agent-bus` itself cut over to production too** — praetor go-ahead after a live parity spot-check (`board` output byte-identical to bash modulo row order, cosmetic only — Go map iteration doesn't preserve the bash version's file-listing order). `AGENTS.md` updated (`bf29a11`) to list `agent-bus` alongside the other four as preferred via `scripts/starfleetctl`. `SessionStart`/`SessionEnd` hooks and the `agent-bus-monitor-loop`/`agent-bus-fleet-watch` Monitor scripts deliberately kept on bash (hooks run outside the permission system, so switching them buys nothing; the Monitor scripts read the inbox/board files directly, not through the `agent-bus` wrapper). **2026-07-06 (praetor): 5-phase roadmap for the rest of this initiative, dispatched while the praetor is away (a few hours) — fleet to work as autonomously as possible, self-coordinating via `pr-claim`/this row, only pinging for genuine blockers or real design decisions.** Phases as given (no phase 4 was specified — treat the numbering gap as intentional/reserved, don't invent content for it):
  - **Phase 1 — full fleet-management coverage + zero-post-processing output. ✅ DONE 2026-07-06 (Farragut), directive `m0029`.** Audited `scripts/*`/`AGENTS.md` for pipe-chains after agent-bus/pr-claim/ws-commit/ship-names/dashboard — found **none currently in use** (these are read as human tables today), so this was forward-looking rather than replacing an existing bad pattern. **Coverage gap found and closed:** `with-clone-lock` had no Go equivalent (`mpbt-hq/starfleetctl` commit `1429074`) — the generic "serialize mutating work in one clone" primitive every other lock (`ws-commit`/`dashboard`) builds on. Deliberately generic like the bash original (operates on the caller's cwd's git tree, skips workspace-root discovery); verified interop both directions against a scratch repo (Go blocks on a bash-held lock and reports the correct holder, and vice versa, ~2s measured not a race). **Zero-post-processing output added** (commit `1d28004`): `--json` on `agent-bus board/inbox/msgs/asks`, `pr-claim --list`, and `ship-names list` — structured JSON array instead of the human table, field names mirroring the text columns (age as an `age_seconds` integer, not the display string), always a valid `[]` even when empty. Existing text output is unchanged (same code path when `--json` is absent). Verified all four with `python -m json.tool` plus manual field-value checks against scratch bus/claim/ship-registry dirs. **Correction to the `agent-bus` cutover note above:** its claim that `board`'s live spot-check row-order difference was due to "Go's map iteration" was a misdiagnosis — this implementation sorts deterministically (same alphabetical-by-filename order as bash's glob, confirmed byte-identical in this session's own earlier parity tests); the observed difference was almost certainly two sequential invocations racing a constantly-changing live board, not a Go defect. Fixed the wording in `AGENTS.md`'s Key-commands row (kept this DASHBOARD row's own history as-is, corrected only the still-current doc). **Process incident, worth flagging for the other phases:** found a live collision in the shared `_WORK_/starfleetctl/sources/xlibre/starfleetctl` checkout — a concurrent Phase-2 edit (`internal/ghpr`) was mid-write in the same tree while this work was in progress (main.go merged cleanly by luck; the tree was briefly non-buildable). Isolated the rest of this work in `scripts/worktree add _WORK_/starfleetctl/sources/xlibre/starfleetctl <name>` per-change worktrees instead, pushed clean from there each time (`git push origin HEAD:master`, since a fresh worktree branch isn't `master`), broadcast a heads-up (`m0043`) recommending the same for whoever's on Phase 2/3/5. `AGENTS.md`'s starfleetctl row now carries this recommendation. Both commits (`1429074`, `1d28004`) reviewed clean (`go build`/`go vet`/`gofmt -l`) before push. **`AGENTS.md`/`.claude/settings.json` updated** per the standing "parity first, then docs" rule — `with-clone-lock` added to the cut-over list (same risk class as the other four: single-session/advisory, not fleet-wide-live like `agent-bus`), no new `.claude/settings.json` entry needed (existing `Bash(scripts/starfleetctl)`/`Bash(scripts/starfleetctl *)` blanket grant already covers it).
  - **Phase 2 — GitHub interaction into Go too** (same `starfleetctl` binary or a separate one — implementer's call, but justify the choice in this row). Covers the `pr-*` family (`pr-view`, `pr-ci`, `pr-checkout`, `pr-amend-push`, `pr-comment`, `pr-label`, `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `pr-job-logs`, `show-pr-conflict`, `cancel-stale-ci`, `prune-stale-ci`, `show-branch-file`), `backport-commit`/`backport-applies`, and `xx-make-pr.sh`. Same rules as always: parity-proven-then-praetor-go-ahead before any AGENTS.md/settings.json cutover of a *mutating* command (read-only ones like `pr-view`/`pr-ci` are lower risk); never bypass `pr-claim` semantics; **no auto-merge into `release/*` ever**, only `master` and only when explicitly told. **Claimed by Potemkin, 2026-07-06 — read-only subset DONE, pushed + cut over.** `pr-view`, `pr-ci` (incl. `--json` + PR-URL-argument forms), `show-branch-file` (incl. multi-hit `grep -A` semantics — merged/adjacent context blocks, `--` separators between non-adjacent ones), `backport-applies`, `show-pr-conflict` — all implemented in `internal/ghpr/` (`metux/starfleetctl` → now `mpbt-hq/starfleetctl`, commit `075f264`), wired as new top-level `starfleetctl` subcommands standing alongside `with-clone-lock` (skip `workspaceRoot()`, work from any cwd — they're stateless `gh` wrappers, not fleet-file-coordination tools). Still shell out to the real `gh` CLI for auth/transport (not worth reimplementing GitHub API auth in Go for this phase) but replace jq/grep/sed post-processing with native `encoding/json`/`regexp`. **Verified byte-for-byte identical output against every bash original** on live repo data (PR #3244/#3245, master `Xext/render/filter.c`) before pushing. **One real bug found+fixed along the way:** bash `show-pr-conflict` was missing `--repo`, so it silently used `gh`'s cwd-based repo auto-detection instead of always `X11Libre/xserver` — returned an empty/wrong list depending on where you ran it from (confirmed: empty from `mpbt-workspace` itself, a real xserver PR list from inside an xserver clone). Fixed in the bash original too, not just the Go port. Cut over directly to "preferred" in `AGENTS.md` for these five — no separate praetor go-ahead sought, since all five are read-only (matches this row's own "read-only ones are lower risk" carve-out); bash originals left in place, both coexist. **Collision note (see also the starfleetctl row's own "Concurrent starfleetctl development" callout):** hit the shared-checkout collision this same session — found Farragut's `with-clone-lock` WIP mid-edit in the same tree, then (after they moved to an isolated worktree) a stale untracked `internal/withclonelock/` blocked a plain fast-forward pull. Resolved by removing the stale untracked dir, pulling clean, and re-applying only the `ghpr`-specific hunk to the freshly-pulled `main.go` rather than my own stale copy. **Next:** the mutating subset (`pr-checkout`, `pr-amend-push`, `pr-comment`, `pr-label`, `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `backport-commit`, `xx-make-pr.sh`) — pick up if free, claim the row; each of those needs parity-proof **and** an explicit praetor go-ahead before any cutover, per the standing rule. **Claimed by Constellation, 2026-07-06 (fleet directive m0046) — mutating subset DONE, pushed, cut over 2026-07-06 (m0060).** All nine implemented in `internal/ghpr/` (`mpbt-hq/starfleetctl` commit `e77934c`, rebased cleanly onto `f351823`): `pr-comment` (incl. the `--bot-review` banner prepend, ship name from `$AGENT_ID`), `pr-label` (add/remove/set-review), `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `pr-checkout` (same-repo and fork-PR paths, the advisory `pr-claim --who` cross-agent check, wired through `workspaceRoot()` since it needs `scripts/mk-agent-clone` + `_WORK_/...` paths — unlike the stateless read-only group), `pr-amend-push`, `backport-commit` (incl. the path-remap fallback for the `Xext/<ext>/` <-> `<ext>/` reorg), and `xx-make-pr` (both single- and multi-commit paths). Used an isolated `scripts/worktree add` throughout, not the shared checkout, per the Phase-1/Phase-2 collision notes above — zero collisions this time. **Verification approach, deliberately risk-scaled to each command's blast radius** (no real spurious PRs/pings created against `X11Libre/xserver`): `pr-label`/`pr-set-body`/`pr-append-body`/`pr-comment` verified byte-identical to their bash originals against **live** repo data (idempotent `set-review passed` re-apply on #3257; round-tripped **and exactly restored** PR #3103's body via append-then-set-body, diffed byte-for-byte after restore; bot-review banner text diffed directly against the bash version's output, both via a stubbed `gh`); `pr-request-reviewers` verified via a stubbed `gh` only (its real effect — pinging humans — isn't something to trigger just to test); `pr-checkout` run **live** end-to-end against real open fork PR #3253 (correctly detected the fork, wired the `fork` remote, checked out tracking `fork/<branch>`); `pr-amend-push` exercised against a scratch bare+clone repo (amend preserved Signed-off-by, force-with-lease pushed correctly, both the detached-HEAD and nothing-staged rejections fire); `xx-make-pr` run end-to-end against a scratch repo with a stubbed `gh`, both single- and multi-commit paths — confirmed it **faithfully reproduces** the documented `[PR #N]`-prefix-leak-onto-the-pushed-branch bug (`AGENTS.md` "PR workflow") rather than silently fixing it, since this is a parity port, not a redesign; `backport-commit`'s path-remap fallback covered by two new Go tests (`TestApplyViaPathRemap`, simulating the real `Xext/dpms/` -> `dpms/` reorg shape, and `_NotAReorg` for the genuine-conflict bail path) rather than a live run against `mk-agent-clone` (out of scope to fire a real cherry-pick/PR-create cycle just to test). One disclosed, deliberate divergence: the bash original's diff-path remap uses `sed` (so a `.` in a path is a BRE wildcard); the Go port uses a literal `strings.ReplaceAll` instead — behaviourally identical for every real path in this tree, intentionally not reproducing that edge case. **Cut over 2026-07-06 (Constellation, praetor go-ahead via Enterprise m0060):** `AGENTS.md`'s Key-commands row updated (subcommand list + a new dated bullet in the "Cut over to production" summary, `starfleetctl@e77934c`) to list all nine as preferred, same treatment as every prior round. **Bash originals intentionally NOT deleted** — `scripts/pr-checkout`/`scripts/backport-commit` still call bash `scripts/mk-agent-clone` (no Go port of that one yet) and `scripts/backport-commit` also calls bash `scripts/xx-make-pr.sh` internally; per the just-established "delete only once genuinely dependency-free" precedent (the `pr-view`/`pr-ci`/`show-pr-conflict`/`backport-applies` cleanup earlier this session), removal is a separate later step gated on `mk-agent-clone` itself getting a Go port. **`mk-agent-clone` ported, 2026-07-06 (Constellation, directive m0064).** `EnsureAgentClone()` in `internal/ghpr/mkagentclone.go` (`starfleetctl@221cabd`) — same config-section-copy approach as the bash original (drop+re-add `make-pr.*`/`remote.*` verbatim from the reference clone) and a new `mk-agent-clone` subcommand wrapping it. **`pr-checkout`/`backport-commit` now call it directly as a Go function — no more bash `scripts/mk-agent-clone` shell-out from either.** `backport-commit` already called `ghpr.RunXXMakePR` directly (a Go function call, not a shell-out) rather than bash `scripts/xx-make-pr.sh` — so with this change **both `pr-checkout` and `backport-commit` have zero remaining bash dependencies**. Verified against live workspace data: bash vs. Go clone of the same release (fresh + idempotent refresh) produce byte-identical git config and matching HEAD/branch state; `pr-checkout`'s stdout-contract (clone dir only, all progress on stderr) still holds after threading an `io.Writer` through `EnsureAgentClone` so callers can redirect the whole subtree's progress output like the bash original's differing `>&2` usage per call site. **Not added to `AGENTS.md`'s "preferred" list as a bare subcommand** — it's git-mutating (creates/fetches/checks-out local clones), same risk class as `dashboard`/`ws-commit` which both waited for an explicit go-ahead before their own cutover, so leaving that call to the praetor rather than assuming the m0064 go-ahead (which was for the port itself) extends to it; its logic is already in real production use today via the two already-cut-over callers. **Next:** `scripts/mk-agent-clone` (bash) can likely be deleted once the dependency audit confirms nothing else still calls it directly (only `pr-checkout`/`backport-commit` ever did, and both are now ported) — someone should double check with the same `grep -rl` audit used for the read-only cleanup before removing it.
  - **Phase 3 — fully automatic bootstrap, both fresh-project and re-init-after-clone.** Builds on the existing `./bootstrap` script and the already-parked "starfleetctl self-bootstrap command" TODO above (tool-authorization + file/dir setup from nothing). Must also handle **re-initializing an existing, previously-bootstrapped checkout after a fresh `git clone`** (i.e. idempotent from either a truly empty or a git-fresh-but-not-yet-`_WORK_`-populated state) — not just first-time setup. **Claimed by Farragut, 2026-07-06** (picked up autonomously per the standing "grab an open DASHBOARD theme" invitation, m0065). **Mostly DONE — one part genuinely blocked, flagged below rather than decided.** Built `starfleetctl bootstrap [--fix]` (`mpbt-hq/starfleetctl` commit `73ae87e`): idempotent self-check/self-repair of the fleet-management-specific one-time setup (`_WORK_/agent-bus`/`_WORK_/agent-claims` dir tree, the `starfleetctl` allowlist entries in `.claude/settings.json`) — deliberately narrow, doesn't duplicate `./bootstrap`'s broader mpbt-build-system job. The `.claude/settings.json` fix is a targeted text insertion after the `"allow": [` marker (not a full JSON re-marshal, which would reformat the whole shared/hand-edited file into a noisy diff), validated to still parse as JSON before writing. Verified against a mock fresh checkout: report-only correctly flags everything missing; `--fix` repairs cleanly; re-running either mode afterward is a true no-op (including a second `--fix` producing no duplicate settings.json entries); the specific "re-init after fresh clone" case (wipe just the gitignored `_WORK_`, leave tracked `settings.json` alone) detected and repaired correctly without re-touching the already-fine file. Ran `--fix` for real against this live checkout too — found and closed one genuine gap (`_WORK_/agent-bus/notify/.popup-once` hadn't been created). Wired into `./bootstrap` as a new step 4a, right after starfleetctl itself is built (commit `0b8b1ee`) — `./bootstrap` re-run end to end afterward, confirmed the new step fires and passes. **Side finding while testing this (documented in `AGENTS.md`'s starfleetctl section, not just here): `./run-fetch.starfleetctl` doesn't fast-forward the local checkout to new upstream commits** — only updates the remote-tracking ref, so `./run-build.starfleetctl` can silently build stale code right after a fetch (`git branch -vv` shows e.g. "2 behind"). Not root-caused (generic mpbt behavior vs. this solution's specific config — unconfirmed), just flagged as a gotcha; worked around locally with `git merge --ff-only origin/master` in the shared clone before rebuilding. **The genuinely blocked part, NOT decided here — needs the praetor's call:** the roadmap's premise of "fully automatic bootstrap from a truly fresh clone" runs into a real structural wall — confirmed via `gh repo view --json defaultBranchRef` that `master` is this repo's actual default branch (what a plain `git clone` checks out), and confirmed via `git cat-file -e origin/master:<path>` that `master` is **missing** `./bootstrap`, `AGENTS.md`, `DASHBOARD.md`, `LICENSE`, `scripts/agent-bus`, `scripts/starfleetctl`, and (per an earlier session's finding) the rest of the fleet tooling — all of it lives only on `mtx/agent-config`. `master`'s own `.claude/settings.json` is also a stale, much smaller snapshot (no fleet/agent-bus/starfleetctl entries, no hooks, no ship-name `statusLine`). So a genuinely fresh `git clone` with no prior knowledge has *nothing* to bootstrap with — not even `./bootstrap` itself exists there. Fixing this crosses the explicit, repeatedly-stated `AGENTS.md` "Licensing policy" line that `mtx/*` → `master` promotion is **"a deliberate, separate, later decision the [praetor] makes per item — don't propose or perform that promotion unprompted."** Did not act on this. Options for the praetor to choose from, not chosen here: (a) generalize some minimal subset onto `master` (e.g. just enough of a pointer/instructions to `git checkout mtx/agent-config`); (b) leave it as tribal knowledge / document it only in a human-facing onboarding message; (c) something else entirely. Everything this session's `bootstrap` subcommand does assumes you're already on `mtx/agent-config` — it solves the *re-init* half of Phase 3 completely, not the *branch-discoverability* half.
  - **Phase 5 — move `metux/starfleetctl` to the `mpbt` GitHub org** (https://github.com/mpbt-hq — praetor-provided URL, check it's the right org/that it exists first). Update every reference once moved: `cf/starfleetctl/*`, `run-fetch.starfleetctl`/`run-build.starfleetctl`, `scripts/starfleetctl`, this row, any local clone remotes. This is a real, visible GitHub repo-transfer action — explicitly authorized by the praetor in this same directive, so no separate confirmation needed, but do it carefully and document the before/after state here. **Claimed by Endeavour, 2026-07-06** — confirmed `mpbt-hq` org exists and `metux` (the `gh` identity) has admin role there, so the transfer is executable; proceeding. **Done, 2026-07-06 (Endeavour):** transferred via `gh api --method POST repos/metux/starfleetctl/transfer -f new_owner=mpbt-hq` (before: `metux/starfleetctl`, private; after: `mpbt-hq/starfleetctl`, private, same `master` branch/history — verified `gh api repos/mpbt-hq/starfleetctl` resolves post-transfer). Updated every reference: local clone's `origin` remote (`_WORK_/starfleetctl/sources/xlibre/starfleetctl`), `cf/starfleetctl/solutions/default.yaml`'s `xlibre_git:` base URL, the repo-path mentions in `cf/starfleetctl/config.sh` and `scripts/starfleetctl`'s comments, and `AGENTS.md`'s starfleetctl section + Key-commands row. GitHub redirects the old `metux/starfleetctl` URL transparently, but nothing in this workspace still points at it directly. Phase 5 complete. **Standing design constraint, praetor 2026-07-06: everything under the `mpbt-hq` GitHub org (starfleetctl and any future sister project placed there) must stay fully independent of XLibre/xserver specifics.** XLibre/mpbt-workspace is only the **first** major application of this tooling, not something it's allowed to be coupled to — a second, unrelated one ("sonicde") is already planned. Concretely: no hardcoded `X11Libre/xserver` repo slug, no assumptions baked in about xserver's release-line naming or directory layout, etc. — anything project-specific must stay a configurable default (env var / flag / config file), never a literal in the Go code. Worth a quick audit next time someone touches `internal/ghpr/` or similar: check for stray hardcoded `X11Libre`/`xserver`/`xlibre` strings that should instead be parameters. Not urgent enough to interrupt current in-flight work for — fold into the next natural touch of that code.
  - **Unnumbered idea (optional, "evtl." per praetor — not a commitment):** restructure `DASHBOARD.md` itself into a structured format (YAML? a directory of per-theme files?) with matching `starfleetctl` frontend commands to read/write it, instead of the current single hand-edited markdown table. Explore only if a ship has spare capacity; this is explicitly lower-priority than phases 1/2/3/5.
  - **README for the `mpbt-hq/starfleetctl` repo itself** (directive m0061, Enterprise, 2026-07-06) — **done, Potemkin.** The repo had only code+`LICENSE`+`Makefile`, no docs of its own; all prior knowledge lived in *this* file's prose, written workflow-first, not as something someone could read to understand the tool standalone. Added `README.md` (commit `5ff1736`): what it is + why a Go rewrite, build (`make`/`go build`, plus the mpbt-solution build path), standalone vs. `scripts/starfleetctl` wrapper usage, and a full subcommand reference across all **19** current subcommands grouped into fleet-coordination (`agent-bus`, `dashboard`, `pr-claim`, `ws-commit`, `ship-names`, `with-clone-lock`), read-only GitHub interaction (`pr-view`, `pr-ci`, `show-branch-file`, `backport-applies`, `show-pr-conflict`), and mutating GitHub interaction (`pr-comment`, `pr-label`, `pr-request-reviewers`, `pr-set-body`, `pr-append-body`, `pr-checkout`, `pr-amend-push`, `backport-commit`, `xx-make-pr` — Constellation's Phase-2 work, checked against their current `e77934c` state first per the directive so nothing got documented twice). Also surfaces, in one place, the three parity caveats that were previously only in scattered doc comments: `agent-bus monitor-loop`/`fleet-watch`'s known Monitor-tool incompatibility (m0047/Farragut), `backport-commit`'s disclosed `sed`-BRE-vs-`strings.ReplaceAll` divergence, and `xx-make-pr`'s deliberately-preserved `[PR #N]`-onto-PR-branch bug (parity port, not a redesign). Written in English, per the standing rule that project artifacts (docs, code, PRs) stay English regardless of chat/status language.
  - **m0047 (Enterprise, 2026-07-06) — check whether the agent-bus "ecosystem" scripts still left on bash after the core cutover (`agent-bus-watch`, `agent-bus-monitor-loop`, `agent-bus-fleet-watch`, `agent-bus-boot-prompt`, `agent-bus-monitor-hint`, `agent-bus-auto-id.sh`, the `SessionStart`/`SessionEnd` hooks) can now move to Go too. Investigated by Farragut, 2026-07-06 — result: mostly NO, with one real, unresolved blocker found.** Built Go ports of the three pollers (`internal/agentbus`'s `DoMonitorLoop`/`DoFleetWatch`/`DoWatch`, `mpbt-hq/starfleetctl` commit `f351823`) directly on the existing `Bus` helpers. All three are functionally correct standalone (verified via plain bash-backgrounded runs: correct detection + dedup + text output matching the bash originals). **But: reproducibly BROKEN when `monitor-loop`/`fleet-watch` are spawned via the Claude Code `Monitor` tool specifically** — both correctly catch a *backlog* match (present before the process starts, which is all a `Monitor` arm's own first pass exercises) but never detect a message/status change that arrives *while already running* under `Monitor`, despite the identical binary working fine backgrounded via plain bash `&`. Isolated with three separate minimal Go reproductions of the same shape (bare sleep-loop; directory-listing poll loop; that loop plus a held append-mode file handle mirroring the real seen-file handling) — **all three worked correctly under `Monitor`**, ruling out directory-cache staleness, held-fd side effects, and workspace-root resolution as the cause. Root cause NOT identified despite this systematic isolation — a genuine open mystery, not a shrugged-off guess. **Decision: do NOT wire `monitor-loop`/`fleet-watch` into any `Monitor` arm.** `scripts/agent-bus-monitor-loop`/`scripts/agent-bus-fleet-watch` (bash) remain the only `Monitor`-armed implementation; the Go code stays in the repo (documented prominently in its doc comment + `--help` text) for whoever wants to dig further, but is not safe to flip. `DoWatch` (a *different* execution model — `setsid`-detached daemon, not `Monitor`-tool-managed) was not separately stress-tested against this same failure mode and is likewise not cut over, out of caution rather than a confirmed problem. **Also deliberately left unchanged:** `agent-bus-boot-prompt`/`agent-bus-monitor-hint` (their emitted text still names the bash commands — correct, since bash is what's actually armed); `agent-bus-auto-id.sh` (structurally can't delegate its `PS1`/env-var mutation to any subprocess — must run in-process in the interactive shell; also decided *not* to swap even its internal `ship-names` calls to the Go path, since this dotfile is sourced into every interactive shell on the host, not just Claude Code sessions, and a go-toolchain dependency there buys nothing); the `SessionStart`/`SessionEnd` hooks (unchanged for the original reason — hooks run outside the permission system, switching buys nothing there — now doubly so given the unresolved `Monitor`-reliability question above). No broadcast-before-switching was needed since nothing was actually switched; broadcast the negative finding instead so nobody re-attempts this blind.
  **Also per this directive: other open themes in this file (Aktive Themen + Parkplatz) may be picked up autonomously by any idle ship while the praetor is away** — normal judgment call on what's worth doing, still bound by every other standing constraint in `AGENTS.md` (isolation via `mk-agent-clone`/`pr-claim`, `Signed-off-by`-only commits, no release-branch automerge, HW-domain-expert routing, NVIDIA-ABI caution, etc.). |
| NetBSD CI: scoped pkgsrc/X11-sets mirror on GitHub (insulate `xserver-build-netbsd` from ftp/cdn.netbsd.org flakes) | **Built + PR opened, not merged** | **PR #3243** (branch off `rfc/backport-master`, agent clone `_WORK_/xserver-master/agent/netbsd-mirror/xserver`); new `.github/scripts/netbsd/{mirror-conf.sh,sync-pkg-mirror.sh,publish-mirror.sh}` + `.github/workflows/netbsd-pkg-mirror.yml`; hosted as GitHub Release `netbsd-pkgsrc-mirror` in X11Libre/xserver | Motivated by PR #3225 flake (2026-07-03): `pkg_add: Can't process https://ftp.netbsd.org...: Undefined error: 0`, 3 retries, unrelated to the (Ubuntu-only) diff. Scoped mirror ≈230MB (165MB pkgsrc closure + 65MB X11 sets) vs full pkgsrc 65GB. Weekly cron workflow boots the same `vmactions/netbsd-vm@v1.2.3` (10.1) the build job uses, runs `pkgin -d install` for the authoritative closure (no hand-rolled resolver), builds a **trimmed** `pkg_summary.gz` (subset of the official one, so sizes match), and `gh release upload --clobber`s + prunes stale assets. `install-pkg.sh` now tries the GitHub mirror first, falls back to official on any failure (not a hard cutover). Package list factored into shared `mirror-conf.sh` sourced by both. **Open design Qs flagged in PR:** (1) unverified without a live VM: does pkgin's libfetch follow GitHub's 302 release-asset redirect for `pkg_summary.gz`/`.tgz`? (2) sync-VM-image == build-VM-image assumption for closure matching; (3) optional `libtool`→`libtool-base` swap (~78MB gcc10 drop) left undone to avoid unverifiable build break. Infra blast radius (release hosting + cron cost + staleness) → praetor review before merge. |
| FlyingTux (sister project, separate repo) — rebuild on Go + restructure; adopt `xnamespace` for X11 app isolation now | **✅ PR #3103 merged 2026-07-03 (`5f85319`) — X-NAMESPACE wire protocol now on `xserver-master`.** Python `master` pipeline builds+deploys+runs a containerized app confined to its own X-NAMESPACE (see below, still best-effort/optional in `x11.py`); **Go rewrite done in an earlier session** — full port, Python removed, builds+tests pass, still not run against real docker/X11 | `_WORK_/flyingtux/sources/xlibre/flyingtux` (mpbt solution, `master`, pushed `c80bffc..32dfca5`); `/home/nekrad/src/flyingtux-go` (separate worktree, branch `wip/golang-rewrite`); `src/imagebuilder/flyingtux/services/x11.py`; xserver PR #3103 (**merged** into `master` as `5f85319`, was branch `draft/xns-proto`) | Praetor request, 2026-07-01, continued 2026-07-01/02/03 (multiple sessions). **2026-07-03: first real E2E run (the actual ask — "get it running", not just isolated component tests).** Built a containerized `glxgears` app via FlyingTux's real `flyingtux-app build/deploy/run` against the `draft/xns-proto` Xvfb + Docker, no shortcuts. Found the Python `master` codebase had never actually been run under Python 3 at all — fixed across 4 commits on `flyingtux` `master`: implicit relative imports (~15 files), removed Python 2 builtins (`iteritems`/`has_key`/`basestring`/`unicode`/comma-`except`), `docker.py`/`x11.py` bytes-vs-str bugs, a missing `mkdir` before first-ever `deploy`, and stale Alpine signing keys + a renamed `mesa-dri-*` package (blocked `apk` entirely on current `edge`). Then found and fixed the **real bug in `x11.py`'s X-NAMESPACE wiring itself**: the per-app `xauth` cookie was written `FamilyLocal` keyed to the *host's* hostname, so a client inside the container (different hostname) never matched it and silently fell back to unauthenticated → landed in `anon`, not its own namespace — isolation was **silently not happening at all** despite every prior isolated-component check passing. Fixed by rewriting the entry `FamilyWild` (xauth nlist/nmerge trick). That fix then **exposed two real xserver-side bugs in PR #3103** neither previous testing round had hit (both only reachable via the actual runtime `CreateNamespace` protocol path, not the static config file or by staying superPower): (1) a namespace created via the protocol never got a virtual root window → **NULL-deref segfault crashing the whole server** on the confined client's first `GetProperty` (i.e. any authorized management client could trivially crash the server for everyone just by using the feature) — fixed by factoring `XnsCreateVirtualRoot()` out of the boot-time-only `hookInitRootWindow()` and calling it from `XnsCreate()` too; (2) `CreateColormap` against the real root wasn't in `hookResourceAccess()`'s allowlist (unlike sibling `CreateGC`/`CreatePixmap`) → any confined GL/visual-using client got `BadAccess` and could never render — fixed by adding it. Both pushed to `draft/xns-proto` + reported as a PR comment. **Verified end to end after all fixes:** `xnamespace -s list` shows the running container's client with `refcnt=1` under `app-glxgears` (not `anon`/`root`), full GL rendering works (`glxgears` FPS output). **Next step unchanged:** praetor decision on merging #3103 — code is now proven correct by an actual full-stack run, not just isolated tests. Two separate efforts: (1) **full Go rewrite — done, verified 2026-07-02:** ran in its own session on branch `wip/golang-rewrite` / worktree `flyingtux-go` (7 commits, `acd3e5d`..`242df9f`+license). Ports every Python module 1:1 (`internal/spec`, `internal/{log,fs,util}`, `internal/dockercli`, `internal/{buildjail,pkg}`, `internal/services` incl. the X-NAMESPACE probe/setup below, `internal/container`, `internal/app` orchestration, `cmd/flyingtux-app` CLI), then **removes** `src/`/`vendor/`/`run-zim` (last commit) — no Python left on that branch. Verified this session: `go build ./...` and `go vet ./...` clean, `go test ./...` passes (2 suites: `internal/spec` config-load test against the example target, `internal/app` a docker-free end-to-end deploy test that catches real bugs per its own commit message — a spec-serialization wrapper bug and a null-service-entry mismatch). Not yet merged to `master`, not yet tested against real docker/X11 (only the docker-free unit-level path). (2) **X11 isolation via X-NAMESPACE, implemented optional/best-effort as requested** (on `master`, and ported into the Go tree too): `x11.py` now probes `xnamespace -s version` against the app's `DISPLAY` before doing anything; on failure it falls back unchanged to the old full-access bind-mount. On success it would `create`+`addtoken` a per-app namespace (`app-<name>`, transient, caps from a new `x11-namespace-caps` setting) and hand the container a matching per-app `xauth` cookie (`XAUTHORITY=/etc/x11-app.xauth`) instead of full access. **Blocker found:** stock `xserver-master` never calls `AddExtension()` for X-NAMESPACE (`Xext/namespace/namespace.c` only wires the ACE enforcement hooks) — `QueryExtension` always fails regardless of config. **Blocker resolved, 2026-07-01:** the missing wire-protocol registration is exactly **PR #3103** (`draft/xns-proto`, open, adds `Xext/namespace/proto.c`). Built `xserver-master` from that branch in an isolated agent clone (`_WORK_/xserver-master/agent/xns-proto/xserver` + `.../build`, meson `-Dxephyr -Dxnest -Dxvfb -Dxfbdev` against the already-built install prefix) and verified end to end: `xnamespace -s version` → `1.0`, `-s list` shows `root`/`anon`, and go-x11proto's `run-xnamespace-test.sh` conformance suite passes **40/40** (20 tests × little/big-endian) against the built `Xvfb`. So the client tooling and `x11.py` wiring are correct; the only remaining dependency is **building/running xserver from PR #3103 (or its eventual merge)** instead of stock master. Told `flyingtux` agent (agent-bus directive m0003) it's clear to commit the `x11.py` probe+fallback as-is and to document the PR #3103 build dependency. **PR #3103 follow-up, 2026-07-01:** probono had asked for "an opcode for the WM to find out which NS a window is in" (PR comment thread) — turned out no new opcode was needed. `GetClientNamespace` already accepts a `clientResource` (any resource ID owned by the target client, e.g. a window), resolved via `dixLookupResourceOwner()`; the actual gap was that `hookResourceAccess()` (`Xext/namespace/hook-resource.c`) was the only Xace hook in the extension missing the `superPower` bypass every sibling hook has, so a privileged (WM-like) client's cross-namespace resource lookup was rejected with `BadAccess`. Fixed with a 2-line bypass (commit `92830a3851`, pushed to `draft/xns-proto`), verified end to end (`xnamespace -s whoami <window-id-in-other-ns>` now returns the correct namespace; go-x11proto conformance suite still 40/40, no regression), and posted as a PR comment to probono/metux. **PR #3103 merged 2026-07-03 (Constellation, fleet directive m0023, praetor-authorized auto-merge on this master PR):** rebased `draft/xns-proto` onto current `origin/master` (clean, all 8 commits applied, new head `e537608648`), build-verified `Xvfb`/`Xnest`/`Xephyr`/`Xorg` (xfree86), re-ran both documented E2E tests green — go-x11proto conformance `run-xnamespace-test.sh` 40/40 both byte orders against Xvfb **and** Xephyr/Xnest (CI parity), and the FlyingTux containerized-`glxgears` run (`flyingtux-app build/deploy/run` against a real Docker container) confirming `app-glxgears` namespace shows `refcnt=1` while `root`/`anon` stay untouched, with working GL rendering (llvmpipe, FPS output) — pushed the rebase (`--force-with-lease`), CI came back all-green (22 pass/0 fail/27 skip), merged via `gh pr merge 3103 --rebase` → `5f85319`. X-NAMESPACE protocol registration is therefore on `master` now; FlyingTux's `x11.py` isolation path is live against stock `xserver-master`, no more PR-3103-branch build dependency. |
| NVIDIA open DDX (replace closed X-side driver, keep proprietary `libGL`) | Planning + tooling; nothing run on real NVIDIA HW yet | `NVIDIA-OPEN-DDX.md`, RE tooling in `nvglx-re/` | Next action needs an NVIDIA box, driver ≥ ~550 (Tier A) |
| x86emu `INTR_BOOL_TO_U64`/carry-macro cleanup | Paused mid-cleanup — found real semantic differences between the two code paths | `X86EMU-CLEANUP.md` | Both DIV/IDIV paths have independent correctness bugs (see Parkplatz); needs its own reviewed fix, not a drop-in |
| Extension request-handler vulnerability scan (`wip/ext-handler-vulns`) | 20 fixes committed on a **local, unpushed** branch off `release/25.1`; disclosure path not yet decided | `VULN-SCAN-FULL.md` / `VULN-SCAN-extension-handlers.md` (findings), `VULN-FIX-PR.md` (push/PR recipe), `VULN-FIX-BACKPORT.md` (per-branch applicability: 10 apply to 25.0, 18 to master) | Public PR = public disclosure — needs a praetor call before pushing |
| Fleet auto-scaling — spawn ships on demand when queued parallel work exceeds idle capacity | **Built + locally tested 2026-07-06 (Pegasus), per praetor request m0037/Enterprise m0038. Manual/on-demand path works now; unattended background auto-spawn deliberately NOT armed yet, pending praetor/Enterprise sign-off (explicit requirement in m0038).** | `scripts/fleet-autoscale` (`need <N> --reason "..." [--release <rel>] [--client claude\|opencode] [--max <cap>] [--dry-run]`, `status`) | **Design choices made (m0038 left these to the implementer, to be documented before starting — doing that here):** (1) **demand signal = explicit trigger, not a background daemon/heuristic scan of DASHBOARD.md.** The caller (human or a control session like Enterprise) states `need <N>` + a mandatory `--reason` directly; nothing polls the board or parses DASHBOARD.md's markdown to infer demand itself (that would be fragile — no structured "unclaimed" markup exists today — and defeats the point of keeping this manual-only for now). (2) **hard cap** on TOTAL non-stale fleet size (not just auto-spawned ships): `--max`/`$FLEET_AUTOSCALE_MAX`, default 6 — chosen conservative given real per-ship API-token cost; spawn count = `min(need - idle, max - current_total)`, never negative, never exceeds the cap regardless of `need`. Stale board entries (past `agent-bus`'s own `$BUS_TTL`) don't count against the cap or as available idle capacity. (3) **loud + auditable**: every invocation (spawn *and* no-op decisions alike) appends to a gitignored `_WORK_/agent-bus/autoscale-events.log`, and an actual spawn also fires `agent-bus broadcast` naming the new ship(s), the reason, and the triggering `AGENT_ID` — ties directly into the just-surfaced "agent-bus has no message authentication" gap (see Parkplatz row below): a broadcast at least makes the spawn visible/attributable fleet-wide even though it can't be cryptographically verified. Implementation reuses existing primitives as instructed — `scripts/ship-names assign` for the name, `scripts/agent-run <release> --client X --name <name>` for the actual tmux-detached launch — no new spawn mechanism invented. **Locally tested (all via Bash, no code changes needed after)**: `status` (idle/total counts vs. cap), `need` dry-runs covering all three branches (no-op when idle already covers demand, cap-limited partial spawn, missing `--reason` rejected), and **one real (non-dry-run) spawn+stop cycle** as m0038 explicitly required. **Real-spawn finding, flagged back to Enterprise (agent-bus m0041 thread) rather than guessed past:** `--client claude` spawned via `fleet-autoscale`/`agent-run` from *inside this own sandboxed Claude-Code-session's Bash tool* died immediately (tmux server exited right after creation, ship-name reservation leaked in `scripts/ship-names` until manually released) — isolated the cause by re-running plain `scripts/agent-run master --client shell --name zzz-test` from the same shell, which persisted correctly and stopped cleanly via `agent-run --stop`. So the tmux/agent-run/ship-names plumbing itself is solid; the failure is specific to launching a *nested* `claude` process from within another Claude Code agent's own sandboxed tool-call environment (auth/resource/session conflict of some kind — not diagnosed further, out of scope for this pass). **Consequence for whoever eventually arms real automation:** triggering `need ... --client claude` needs to happen from a normal human-launched terminal (or at least a non-nested-sandboxed context) to actually get a working claude worker — if a future daemon/cron/Monitor-loop invocation runs *inside* another agent's own sandbox, expect the same failure. `--client shell`/`--client opencode` were not affected by this and should be unaffected in the same context (opencode not itself tested this session). **Explicitly NOT done, by design, per m0038:** no cron/Monitor-loop/SessionStart-hook wiring that would spawn ships unattended — the on-demand `need` call is the only entry point right now. **Next:** (a) get a `--client claude` real-spawn test run from a genuinely separate terminal/session to confirm the nested-sandbox theory and clear the last "does it actually produce a working worker" question; (b) praetor/Enterprise decide whether/how to arm unattended invocation (e.g. from Enterprise's own board-watching loop) and what demand heuristic would drive it then (the DASHBOARD/directive-count idea from m0038 is still on the table for that later step, just not built now); (c) coordinate with Farragut's starfleetctl Phase 1 (fleet-management coverage) — `fleet-autoscale` deliberately stayed a separate bash script for now rather than a new starfleetctl subcommand, since m0038 asked for design+implementation now and a starfleetctl port can follow the same later-cutover pattern as `pr-claim`/`ws-commit`/etc. if that's wanted. **2026-07-06 (Pegasus): follow-up request (agent-bus m0051, from Enterprise) — PAUSED, not implemented, pending direct confirmation.** Ask: a two-tier permission model — ships spawned by `fleet-autoscale` ("lower tier") would launch with Claude Code's `--permission-mode dontAsk` (anything outside `permissions.allow` is auto-rejected instead of blocking, the documented mode for unattended operation — explicitly *not* `--dangerously-skip-permissions`), tagged via env vars (proposed `AGENT_TIER=worker` / `AGENT_SUPERVISOR=<name>`, default supervisor `Enterprise`); on a rejected/blocked action the worker would report exactly what it tried and why via `agent-bus tell <supervisor>` instead of hanging, then move to other work or wait — the supervisor (a human at a terminal, or Enterprise) could then either fix the gap or do the step itself interactively. Explicitly the *active* counterpart to Potemkin's m0050 (a passive `PreToolUse` hook that only logs would-be-denied commands for later pattern-mining — same underlying goal, different mechanism, coordinate before either implements). **Declined to build (1)-(3) from m0051 without independent confirmation first** — this lowers the human-confirmation floor for unattended agents, a materially bigger and harder-to-reverse safety-relevant change than the `maintainer`→`praetor` vocabulary question that Constellation/Endeavour already correctly refused to take on bus-say-so alone (see the `agent-bus` message-authentication Parkplatz row below). Asked directly in my own session; no reply within the timeout (praetor reportedly away). Told Enterprise (m0053) I'm holding off until confirmed directly in-session or otherwise independently verifiable — not a refusal of Enterprise specifically, same bar applied to m0031. **Next:** needs explicit praetor (or independently-verified) go-ahead before any of (1) `agent-run`/`fleet-autoscale` gaining a `--permission-mode`-passthrough flag, (2) the `agent-bus-boot-prompt` worker-tier branch, or (3) `fleet-autoscale` actually setting `AGENT_TIER`/`AGENT_SUPERVISOR` on spawn. **2026-07-06 (Pegasus): m0051 CONFIRMED directly by the praetor in-session (after Enterprise's relay via m0066 wasn't enough on its own — asked twice directly, second time confirmed) and BUILT (commit `7eb8546`).** Standing policy going forward per the praetor: this specific category (further tuning of the already-approved dontAsk/worker-tier mechanism) no longer needs a fresh direct round-trip — indirect confirmation relayed via the flagship (Enterprise) is now sufficient for it; a genuinely *new* category of higher-impact/permission-loosening change would still warrant asking directly again. Implementation, all three parts from m0051's ask: (1) `scripts/agent-run` gained `--permission-mode <mode>` (restricted to `--client claude`, forwarded verbatim to `claude --permission-mode <mode>`), `--tier <tier>`, `--supervisor <name>` (both exported into the session as `$AGENT_TIER`/`$AGENT_SUPERVISOR`) — a plain interactive `agent-run` call (human or control ship at a real terminal) sets none of these and is completely unaffected, normal confirmation as always. (2) `scripts/agent-bus-boot-prompt` gained a third branch (`AGENT_TIER=worker`, alongside the existing Enterprise/default ones): tells the worker it runs under `dontAsk` (nobody is watching to answer a prompt), and on a rejected/blocked action to *not* keep retrying or silently give up but `agent-bus tell $AGENT_SUPERVISOR` exactly what it tried and why, then continue other queued work or wait for a reply. (3) `scripts/fleet-autoscale need` gained `--supervisor <name>` (default `Enterprise`) and `--permission-mode <mode>` (default `dontAsk`, only forwarded for `--client claude`) and now passes `--tier worker --supervisor "$supervisor"` (+ `--permission-mode` for claude) to every ship it spawns; the broadcast and audit-log lines were extended to record supervisor/permission-mode too. Per m0066's approved simplification: **no owner/flotilla/reassignment tracking** — any control ship (interactive session) may address any free worker-tier ship, `AGENT_SUPERVISOR` is just the default escalation target, not an exclusive claim. **Locally verified**: the worker-tier boot-prompt branch (both explicit and default-Enterprise supervisor) and the normal/Enterprise branches are unaffected (ran `agent-bus-boot-prompt` standalone with different env combos); a real `--client shell --tier worker --supervisor Enterprise` spawn correctly exported both env vars inside the live tmux session (checked via `tmux send-keys`+`capture-pane`), then stopped cleanly; `--permission-mode` with a non-claude client is correctly rejected. **Re-hit the already-documented nested-claude-in-sandbox limitation** with `--client claude --permission-mode dontAsk` too (dies immediately, same as the plain-`claude` case above) — confirms it's the pre-existing sandbox issue, not something the new flags introduced; command construction verified correct by code inspection instead (`claude --permission-mode dontAsk <prompt>`). Coordination with Potemkin's m0050 (passive `PreToolUse` denial-logging hook, `scripts/confirm-log-hook`, already landed and unrelated/complementary — see its own note in this file) confirmed no overlap: that hook logs *all* sessions' would-be-denied commands as telemetry; this escalation is specifically the worker-tier's *active* self-report of a `dontAsk`-caused hard failure to its supervisor. **Still NOT done, unchanged from before:** no daemon/cron/Monitor-loop wiring — `fleet-autoscale need` remains the only spawn trigger; a genuinely unattended *autoscale-decides-on-its-own-when-to-spawn* mode is a separate, later, still-unapproved step. |
| `xf86-input-hurd` native input driver (Mach device ports) | WIP, open | PR #3194 | — |
| `submit/*` stale-branch cleanup | In progress — most already cleared | 6 `submit/*` branches remain (was ~150; 79 cleared as of the June 2026 pass, see `AGENTS.md` "submit/* branch hygiene") | Remaining ones likely need the zipper-rebase or manual conflict resolution (the easy patch-id/merge-tree wins are done) |
| xorg-upstream tracking (`tracking/xorg/main-on-<rel>`) | Ongoing, recurring — **delta fully dispositioned + all 4 trackers advanced, 2026-07-02** | `AGENTS.md` "xorg upstream tracking"; master PR #3217 (udev NULL-deref fix) + backports #3221 (25.2) / #3222 (25.1) / #3223 (25.0); master PR #3219 (xquartz GLX_ARB_create_context + deprecation silencing) | 2026-07-02: 3 new `xorg/main` commits found (all 4 trackers were at the same old tip). `9babe7e7f6` (config/udev: guard against NULL subsystem in fallback bus id — real SIGSEGV crash fix, `config_udev_get_fallback_bus_id()`) → master PR #3217; `config_udev_get_fallback_bus_id()` was byte-for-byte identical (unguarded) on `release/25.2`/`25.1`/`25.0` (checked via `show-branch-file`), so on praetor go-ahead it was also cherry-picked straight from the upstream SHA onto all three release incubators → **#3221/#3222/#3223 opened** (cross-linked to #3217's new "Backport dashboard" table; per policy these are **opened only, not merged** — release-line merges stay manual/praetor-only). `609e7d9421`+`31060c9506` (xquartz/GL: advertise `GLX_ARB_create_context`/profile + silence deprecation warnings, macOS-only, not backport-relevant) → bundled master PR #3219 (one real conflict in `hw/xquartz/GL/meson.build` — XLibre already dropped the stale `glx_inc` include in `dd4c8d4ecb`, resolved by keeping our `include_directories` and only taking the new `c_args`; not locally buildable, macOS CI lane will exercise it). All 4 PRs carry the `-x`-style `(cherry picked from commit ...)` provenance trailer per the workflow. All 4 trackers (master + 25.2/25.1/25.0) advanced to `9babe7e7f6`. |
| Bool → bool (stdbool.h) phase-out | Ongoing, opportunistic (no dedicated branch) | `AGENTS.md` C-booleans note | Apply only in new/rewritten code touching a declaration — not a standalone sweep |
| Issue #3136 (veloren fullscreen crash) — **two unrelated bugs** | Bug1 workaround PR open (draft); Bug2 investigating + 2 side-leaks fixed | issue #3136; **PR #3228** (draft, glamor CopyArea OOB workaround); leak PRs **#3226** (glx — **MERGED** `f045ee09`; backports #3236/#3237/#3238) + **#3227** (panoramiX — **⇒ REBASE DUE**, #3231 merged) + **#3231** (xinerama macros→variadic, **MERGED**); agent clones `.../glamor-wa`, `.../glamor-bisect`, `.../leakfix`, `.../xinerama-macro` | **Bug 1 — glamor OOB source read → crash (the issue's subject):** `glamor_upload_boxes` back-projects dest-clamped box coords into the source buffer without bounding to the source allocation; fullscreen Present geometry reads past `bits` → SIGSEGV in mesa. **Bisect finding:** the unclamped-source design is **pre-21.1** (not the regression); the real regression suspect is the 2023 upstream depth-24-on-32 alpha-fixup path (`5371690891`+`3e044b1e64`, absent in 21.1) with a wrongly-sized `tmp_bits` — but reporter's crash used the *plain* path (bits==src), so needs runtime gdb data (metux requested it) to pin. **PR #3228** = provably-safe defensive clamp in the plain CPU-copy path (crash→glitch), **draft/RFC pending reporter confirmation + HW review**; does NOT cover the tmp_bits path. **Backport-applicable to 25.2/25.1/25.0** (identical unclamped call at `glamor_copy.c:272` on all three; reporter saw all crash) — flagged only, not opened (draft + release-merge-manual policy). **Bug 2 — uninitialised memory → client (info-leak), unrelated:** valgrind flagged uninit bytes written via `__write_reply_hdr_and_rpcbuf`. Audited all ~14 non-zeroing `x_rpcbuf_reserve` callers → **all fully fill** (arrays lückenlos, padding-free structs, or compound-literal assigns that zero padding); rpcbuf is likely NOT the source. Two genuine *side* leaks found instead (bare reply struct + field-by-field fill via `X_SEND_REPLY_SIMPLE`): **glx `dispatch_GLXQueryVersion`** (→ **PR #3226**, designated-init) + **panoramiX `PanoramiXAllocColor`** (→ **PR #3227**, designated-init). The enabling `XINERAMA_FOR_EACH_SCREEN_*`→variadic change was split into **PR #3231 — now MERGED**. **NEXT ACTION (pending):** rebase #3227 onto current `origin/master` (`pr-checkout 3227` → `git rebase origin/master` → the now-duplicate `panoramiX.h` hunk drops as already-applied, leaving only the `panoramiXprocs.c` reply-init → `pr-amend-push`/force-push). #3227 still carries both files (built standalone); cross-linked via a comment. Backport: **glx leak MERGED to master (#3226) + backports opened 2026-07-02** → **#3236** (25.2) / **#3237** (25.1) / **#3238** (25.0, adapted to the older `SetReplyHeader`/`WriteToClient` idiom; 25.2/25.1 applied via `backport-commit`), all build-verified, cross-linked (dashboard on #3226 + back-links), **not merged** (release-merge-manual); panoramiX leak on **25.2/25.1** (on 25.0 `PanoramiXAllocColor` delegates to the core handler, no reply built → N/A) — flagged, not opened. Reporter's exact WITH_RPCBUF leak still unpinned → asked reporter for `valgrind --track-origins=yes`. Note: `= {…}` zeroes omitted *fields* (std) + padding (gcc/clang, not std-guaranteed). **panoramiX leak backported, 2026-07-02:** praetor asked to check #3227 for backport-worthiness — confirmed (real client-triggerable info disclosure) and independently re-verified applicability by reading the actual code on each branch (25.2/25.1 both still have the bare `xAllocColorReply reply;` + field-by-field fill; 25.0 confirmed delegates to `SavedProcVector[X_AllocColor]`, no reply built there → N/A, matching the earlier assessment). Backported **both** #3227 and its prerequisite #3231 to each applicable release (in dependency order, macro-enabler first): **25.2** → PR #3232 (macro) + PR #3233 (fix, depends on #3232); **25.1** → PR #3234 (macro) + PR #3235 (fix, depends on #3234). All 4 build-verified (`meson setup` + `ninja hw/vfb/Xvfb hw/xnest/Xnest`, clean). Cherry-picks applied via 3-way merge with no manual conflict resolution needed on either branch (git's merge correctly dropped the redundant macro-header hunk when applying the fix commit after the macro commit was already present). All 6 PRs (#3227, #3231 masters + #3232-3235 backports) cross-linked (backport dashboards on the masters, back-links + depends-on notes on the backports). Not merged — release-line merges are manual/praetor-only. **Side finding, not mine to resolve:** while creating the 25.2 and 25.1 agent clones, their local `rfc/backport-25.2`/`rfc/backport-25.1` incubators carried extra commits beyond what's pushed to `origin` (e.g. a DGA QueryMode padding fix, a `glamor_egl.c`/`xorgGlxCreateVendor` fix on 25.1) that aren't on `origin/rfc/backport-<rel>` — looks like other in-flight session(s)' pending work on those incubators, not pushed yet. Left untouched (reset my own clones to `origin`'s state before cherry-picking, to avoid clobbering it) — flagging in case those sessions lost track of it. **Macro-enablers merged + fixes rebased, 2026-07-02:** praetor asked to merge the macro-enabler PRs; flagged the AGENTS.md "release merges are manual-praetor-only, agents stop after opening" rule first (master auto-merge-on-request is fine, release-branch auto-merge is not) — praetor merged **#3232** (25.2) and **#3234** (25.1) themselves. (**#3231** on master was already merged earlier.) With all three macro-enablers landed, rebased the three dependent fix PRs onto their now-updated targets: **#3227** (master, via `pr-claim`+`pr-checkout`) and **#3233**/**#3235** (25.2/25.1, in the existing backport clones) — each rebase cleanly dropped the now-redundant macro hunk (git recognized it as already-applied), leaving each PR as a single-file, ~15-line `panoramiXprocs.c` diff. All three re-verified (`meson setup` + `ninja hw/vfb/Xvfb hw/xnest/Xnest`, clean) and force-pushed. All 6 PR bodies updated to reflect the merged/rebased state. **#3227/#3233/#3235 are now ready for review/merge** — master (#3227) could be merged on request; the two release PRs still need the praetor's manual merge per policy. |
| PR review backlog (`/bot-review`) | Ongoing, recurring — large backlog | `gh pr list --repo X11Libre/xserver --state open` | 2026-07-01 snapshot: **84 of 140** open PRs carry no `bot-review-*` label yet (32 of those are drafts). No dedicated tracking doc — this row *is* the tracker; re-check the count periodically rather than listing PRs individually. **2026-07-06 (Constellation, self-assigned during maintainer-away autonomy window, m0030):** re-checked count — still >100 unlabeled open PRs (many old/community/drive-by). Claiming a batch of non-backport, substantive master PRs to review this session: **#3257** (hurd OS-support+kbd driver), **#3253** (kdrive/fbdev refresh-rate), **#3228** (glamor CopyArea clamp, workaround for issue #3136 already tracked above), **#3216** (xfree86 ddc HDR metadata parsing), **#3199** (os: drop Linux abstract-socket support). Posting review comments + pass/changes-requested labels only, per the normal bot-review process — no merging (not the maintainer's own PRs, not master-auto-merge-authorized). Other free ships: pick a different slice to avoid duplicate reviews. **Batch done, all 5 `bot-review-passed`:** #3257 (advisory: no CI yet on fork, sequencing note vs #3194), #3253 (advisory: still draft, recommend @cepelinas9000 second look on shared `kmode.c`), #3228 (**flagged as a backport candidate** — client-triggerable OOB-read crash/DoS via fullscreen Present, applies once merged; still draft pending reporter confirmation), #3216 (deep-dove the tail-appended `xf86Monitor::hdr` field per its own ABI note — confirmed safe, all instances server-side `calloc`'d, never embedded by value), #3199 (new `unix_socket_is_live()` liveness check is a genuine hardening improvement beyond the abstract-socket removal itself; not clearly backport-worthy — feature removal, not a vuln fix, flagged as maintainer's judgment call). **Second batch done, all 3 `bot-review-passed`:** #3218 (verified the new `HAVE_SYS_*_H` conf_data keys need no template wiring — `configure_file` with no `input:` auto-generates defines); #3213 (**build-verified, not just CI-trusted** — checked out, built `libglx.so`+`libglamoregl.so`, confirmed via `nm -D` the previously-duplicated GLX symbols are now defined once in `libglx.so` and correctly `U`/dynamic in `libglamoregl.so`; this completes the incomplete #3174 fix `AGENTS.md`'s glx/glamor duplicate-symbol note already flagged); #3210 (traced `WalkTree`/`TraverseTree` to confirm the root window is visited too, not just descendants; `dix_priv.h` confirmed not in any SDK/install header list). |
| `xf86bigfont` resurrection & cleanup | Step 1 **done** — builds on all lanes again (#3202 **merged**: sysmacros + mingw fixes). #3205 **merged**: meson default-**off** restored + bigfont enabled only on the CI lanes (praetor: "meson defaults nicht anfassen"). #3206 backports both compile fixes to **release/25.2** (25.1/25.0 = older bigfont.c, N/A). #3201 (pagesize/CSRG cleanup) **merged** (rebased, all bigfont lanes green). | `BIGFONT.md`; PRs #3202 + #3205 (merged: CI builds bigfont on all lanes, default off), #3203 (dix ARRAY_SIZE guard, merged), #3201 (pagesize/CSRG, **merged**); branch `xlibre/bigfont-consolidate-cleanup` (ResetProc drop, no commit yet) | Root causes: `sys/sysmacros.h` (5 BSD/macOS), `dix.h` `ARRAY_SIZE` redef (solaris), `geteuid`/`getegid`+unused-var (mingw). BSD/macOS compile-break → backport candidate. Later ideas in Parkplatz. **Competing proposal in flight, see next row:** praetor also wants a draft PR to remove `xf86bigfont` entirely — the two are deliberately parallel options to put before the community, not a contradiction to resolve here |
| Propose removing `xf86bigfont` extension entirely | **Draft PR opened + rebased** (conflicts resolved after the cleanup PRs merged), awaiting community discussion | PR #3208 (draft), branch `xlibre/remove-xf86bigfont` | Praetor request, 2026-07-01. Runs **in parallel** with the resurrection/cleanup effort above (same extension, opposite direction) — intentional, lets the community weigh "fix the bit-rot" vs "just drop it" with a concrete PR to react to, not just discussion in the abstract. Removed the whole `Xext/xf86bigfont/` dir, its meson option/wiring, the `mi/miinitext.c` init hook, both direct dix call-sites (`dix/dixfonts.c` `CloseFont()`, `os/utils.c` `AbortServer()`), the reserved extension-opcode slot, protocol-version constants, and other stray refs (full sweep, see PR body). Build-verified: `meson setup` configures clean, `ninja hw/vfb/Xvfb hw/xnest/Xnest hw/xfree86/Xorg` links. Note: PR #3202 (enable-by-default) is **already merged** into master, so this removal supersedes/reverts its effect as part of the same diff; #3201/#3205/#3206/consolidate-cleanup become moot if this direction is chosen — left untouched per instructions, not a decision made here |
| Propose removing XFS (X Font Server client) support | **Investigated, closed — no in-tree removal target exists.** No draft PR opened | `AGENTS.md`/this row (no dedicated doc; findings below) | Praetor request, 2026-07-01. Full sweep of `dix/`, `os/`, `hw/`, `include/`, `meson.build`, man pages, and git history found **zero XFS-protocol-specific in-tree code**. `dix/dixfonts.c` implements only a generic, backend-agnostic FPE (Font Path Element) dispatch layer: `SetFontPathElements()`/`DetermineFPEType()` (`dix/dixfonts.c:1609,1655`) dispatch through a vtable (`fpe_functions[]`, type `xfont2_fpe_funcs_rec`) that is populated entirely by **libXfont2's own callback** (`register_fpe_funcs`, wired into `xfont2_client_funcs_rec` at `dix/dixfonts.c:2084-2108`) when the server calls `xfont2_init()`. The server never special-cases a `tcp/`/`fs/` path prefix anywhere — `hw/xfree86/common/xf86Config.c:190-236` only validates local-directory paths (`stat()` + `fonts.dir` check) and passes everything else through opaquely with just a comment ("Either an OK directory, or a font server name"). The `_init_fs_handlers`/`fs_block_handler`/`add_fs_fd`-style functions (`dix/dixfonts.c:2040-2076`, "fs" = "font system") are generic async-I/O/block-handler glue for *any* FPE backend, not X-Font-Server-protocol code. `meson.build`/`meson_options.txt` have no font-server option — `xfont2_dep` (`meson.build:126`) is a plain external pkg-config dep, not vendored (no `.wrap`/subproject). `man/Xserver.man` mentions "font servers" only generically, documents no `tcp/` syntax. No XLibre-authored commit ever touched font-server code (`git log --all -i --grep`: only pre-fork 2008 XFree86/X.org history, e.g. `6c78f1b736` removing `NOFONTSERVERACCESS`, which shows the FS backend was *already* external via libXfont even then). **Conclusion: "drop XFS support" is a `libXfont2`-side decision (drop its own `fs` FPE backend), entirely out of scope for an xserver PR — there is nothing removable here.** No draft PR opened per the task's explicit instruction not to fabricate one; recorded as closed/no-action instead. Contrast with the `xf86bigfont` row above, which *does* have real in-tree code and got its own removal-candidate PR. **Follow-up, 2026-07-01:** is the `Suspended`/`ClientSleep` async-completion plumbing in `dixfonts.c` (`doOpenFont`, `doListFontsAndAliases`, `_init_fs_handlers`) still needed without XFS? **Yes — it's required by libXfont2's generic FPE API, not XFS-specific.** Verified against `libxfont2` 1:2.0.6 source (`apt-get source libxfont2`): `Suspended` (`<X11/fonts/font.h>:72`, value 84) is a generic status code in the `FPEFunctions` vtable (`<X11/fonts/fontstruct.h>`, plain-`int`-returning callbacks every FPE backend implements) — `grep -rl Suspended src/` in libXfont2 hits **only** `src/fc/{fserve.c,fsconvert.c,fservestr.h}` (the font-server client backend); `src/{bitmap,builtins,fontfile,FreeType,util}/` — i.e. every backend xserver actually uses for local fonts — never return it. libXfont2's own `configure.ac` even has `--disable-fc` ("Support connections to xfs servers", default enabled) to compile the fs backend out entirely, without touching the vtable shape. So xserver must keep honoring `Suspended` generically (any FPE backend *could* return it per the API contract it links against), even though none of the backends it actually ships with ever do — this is libXfont2's ABI, not something XLibre can prune unilaterally |
| go-x11proto library + tools + Debian packaging (**separate repo/project**) | **v0.0.7 released** (tag pushed 2026-07-01, ships `xnamespace -s`+manpage+JSON-removal) | repo `X11Libre/go-x11proto`, now an mpbt solution @ `_WORK_/go-x11proto/sources/xlibre/go-x11proto` (moved 2026-07-02 from the old `/home/nekrad/src/xorg/go-x11`); packaging `X11Libre/deb-pkg` `cf/repos/devuan/go-x11.yml` | eventCh `send on closed channel` race fix + `%w` error-format fix (CI now shows the real socket errno instead of `%!w(...)`); Debianized into `golang-…-dev`/`xnamespace`/`tetris64`. **Tools get script-parseable output:** `xnamespace` has `-s/--short` (terse tab-separated, header-less, for shell/C `system()` — e.g. a DE launching a client into its own namespace); old `-json` mode dropped (−330 KB binary), `xnamespace-test` emits TAP. `xnamespace` also got a manpage (verified shipping in the `.deb`). `-s`+manpage+JSON-removal shipped in **v0.0.7**. The go-xts break on **PR #3199** is xserver-side (`-displayfd` socket collision), owned by agent `fix-pr3199-socket-race` — **not this repo**. Separate project/agent, coordinate via `agent-bus`. **xserver CI bump done:** `PKG_GOXPROTO_REF`/`GOXPROTO_REF` v0.0.4→**v0.0.7** (both sites, `.github/scripts/conf.sh` + `build-xserver.yml`) → **PR #3207** (2026-07-01, awaiting CI/merge) |
| Agent control plane ("1st officer" — one central contact for many workers) | Steps 1 **and** 2 **done** (ask/reply + notify watcher + permission-forward hook); tested | `AGENT-CONTROL-PLANE.md`; `scripts/agent-bus` (`ask`/`reply`/`asks`), `scripts/agent-bus-watch`, `scripts/agent-permission-hook` | Worker `agent-bus ask "<q>"` blocks on a local file-poll (no API) for the controller's reply; controller uses `agent-bus asks` / `reply <qid>`. Notify watcher auto-starts per session (SessionStart hook). Tool approvals route to the controller via the opt-in `PreToolUse` hook `agent-permission-hook` (per-worker `settings.local.json`; fail-closed deny on no answer). **Longer term:** MCP push-bus instead of file polling |
| `xephyr-glamor / XTS` CI hang, all-3-retries case | **Investigated — looks like a one-off, not a new persistent regression** | PR #3203 job `xserver-build-ubuntu-debug` run 28522256520; PR #3204 (NULL-deref fix, see below) | 2026-07-01: `xserver:xephyr-glamor / XTS` hit the full `1200s` `TIMEOUT` on all 3 retries (~64min) on PR #3203 (unrelated `ARRAY_SIZE` guard). Checked ~60 recent `build-xserver.yml` runs: this is the **only** one showing "all 3 retries exhausted" — not widespread. Root mechanism confirmed: `test/meson.build`'s `timeout: 1200` on the `XTS` test is the *only* timeout in play — the piglit invocation (`xephyr-glamor-piglit.sh` → `run-piglit.sh`) passes no per-test `-t` flag, so a single wedged XTS subtest blocks silently for the full 1200s with zero progress output (meson buffers all test stdout until completion/kill) — **which specific subtest wedged could not be identified** from available CI logs. Plausibly related-but-distinct from the crash (not hang) that motivated PR #3204's `hostx.c` NULL-deref fix — two other same-day runs hit an actual `Xephyr … Segmentation fault` in `go-xts` (not the piglit `XTS` test) under `-glamor +byteswappedclients`, i.e. same general "Xephyr+glamor concurrent-load fragility" area, different code path/symptom. No suspicious recent glamor/kdrive/piglit-wrapper commit or runner/mesa version bump found. **Follow-up worth doing but not yet started:** give piglit an actual per-test timeout (`-t`) so a future wedge fails fast with a subtest name instead of blocking the whole suite for 1200s blind |
| Vendor `libXfont2` in-tree (like `os/Xtrans*.c` already is), then strip the dead `fc/` backend | Idea stage — sized/scoped 2026-07-01, nothing written yet | precedent: `cd81370e9d` ("os: incorporate xtrans xtrans-1.6.0") | Follows from the XFS investigation above. **Other consumers:** none outside the X-server family — Debian reverse-deps of `libxfont2` are only `nxagent`/`xwayland`/`xvfb`/`xserver-xspice`/`xserver-xorg-core`/`xserver-xephyr`/`x2gokdrive`/`xnest`/`tigervnc-standalone-server`; the historical other consumer, the `xfs` font-server daemon, no longer exists as a Debian package. Vendoring costs no one else anything. **Size vs. the xtrans precedent** (that commit: 7 files, 6,969 lines, one shot, then a long tail of small cleanup commits over months — same pattern to repeat here): libXfont2 is **~4x bigger** — ~26,164 lines under `src/` (`fc/` 5,320, `bitmap/` 5,836, `fontfile/` 5,086, `FreeType/`-glue 5,388, `builtins/` 1,882, `util/` 2,227, `stubs/` 425) + ~1,370 lines of public headers. Upstream is autotools-only (no meson.build), same situation xtrans was in — vendoring means hand-writing the meson `static_library()` wiring, no reusable upstream build file. **New direct deps `meson.build` would gain** (currently hidden behind `dependency('xfont2')`'s transitive `Requires`, absent from xserver's own build file today): freetype2, zlib (+optional bz2), and `fontenc` (a separate small lib, ~2,144 lines — vendorable too or kept external). **The payoff, already proven this session:** `fc/` (the font-server client backend, 5,320 lines ≈ 20% of the codebase) is confirmed dead code for this fork (see the XFS row above) — an immediate, justified first cut right after vendoring, unlike xtrans's cleanup which was many small incremental cuts over time. **Real trade-off to weigh:** vendoring means XLibre must track libXfont2 upstream fixes/CVEs manually from then on (same trade-off already accepted for xtrans). **In-tree consumers to keep in mind when scoping this** (what actually uses libXfont2's output, i.e. what any refactor must not break): the core-protocol font requests in `dix/dispatch.c` (`OpenFont`/`CloseFont`/`QueryFont`/`QueryTextExtents`/`ListFonts(WithInfo)`/`PolyText8/16`/`ImageText8/16`), font-based cursors (`XCreateGlyphCursor` → `AllocGlyphCursor` → `dix/glyphcurs.c`), and the rendering backends that consume the resulting `FontPtr`/`CharInfoPtr` structs — `mi/mipolytext.c`, `mi/miglblt.c`, `fb/fbglyph.c`, `glamor/glamor_text.c`+`glamor_font.c`, the `miext/shadow/sh*.c` variants, `exa/exa_accel.c`, `hw/xnest/Font.c`. Startup wiring: `dix/main.c` → `xfont2_init_glyph_caching()` + `InitFonts()` |
| Convince other contributors to adopt `mpbt-workspace` | Idea stage — direction being discussed, nothing written yet | this DASHBOARD.md entry (no doc yet) | Goal (praetor, 2026-07-01): move contributors off "clone xserver standalone + manual meson" onto mpbt (multi-release-line builds, shared tooling). Checked 2026-07-01: **no existing docs anywhere** — `X11Libre/xserver` wiki (`Building-XLibre.md`) only describes the manual single-clone build, no mpbt mention in any of its 16 pages; `X11Libre/mpbt-workspace` wiki is enabled but has **zero pages** (wiki git repo doesn't exist yet); upstream `metux/mpbt` wiki likewise empty. Landing spot should probably be `Building-XLibre.md` (a pointer, not a rewrite), linking to real content. **Open question, not decided:** does that content live as a *wiki page* (which repo's wiki — xserver's, since that's what contributors already browse, or mpbt-workspace's?) or as a plain doc/README section **inside `mpbt-workspace` itself**, linked from the xserver README/wiki instead of duplicating another wiki? Related: the earlier `.vscode/tasks.json`-in-xserver discussion (a multi-root `.code-workspace` in mpbt-workspace only pays off once this migration is real) |
| Platform-specific `#ifdef` cleanup — grab-bag OS macros → concrete feature flags | **In progress** — `CSRG_BASED` phase **merged**, `SVR4` phase 2 PRs open | **Merged:** #3209 / #3211 / #3212 (CSRG_BASED). **Open:** #3218 (`SVR4` socket headers), #3220 (dead `<sys/utsname.h>`), #3224 (dead `#define termio termios`); agent clones `_WORK_/xserver-master/agent/ifdef-cleanup/xserver` + `.../termio/xserver` | Praetor idea 2026-07-01: replace vague "which-OS-family" macros with specific `HAVE_<feature>`/meson checks. All build-verified `-Dwerror` on Linux, base master. **CSRG_BASED — DONE (merged):** `os/connection.c`+`os/access.c` `<sys/param.h>`→`HAVE_SYS_PARAM_H` (#3209), `xf86Helper.c` dead `HAS_SETPRIORITY` deleted (#3211), `xf86Globals.c`+`xf86Privstr.h` consType→`CONFIG_BSD_CONSOLE` (#3212); bigfont one via merged #3201. **SVR4 — clean wins done (open PRs):** `<sys/filio.h>`/`<sys/sockio.h>`/`<sys/stropts.h>`→`HAVE_SYS_*_H` (#3218), dead `<sys/utsname.h>` in access.c removed — uname is behind `xhostname()` now (#3220). **Dead-code bonus:** the `#define termio termios` in `xf86_OSlib.h` (BSD block) removed — `termio` type unused treewide (#3224). **Deliberately left (NOT mislabels — the macro is used correctly, would need a restructure not a swap):** `include/xf86_OSlib.h` BSD/`__SVR4&&__sun` header blocks (structural per-OS-family header cascade, now minus the dead termio alias); `hw/xfree86/os-support/bus/xf86Sbus_priv.h` (niche SPARC SBUS fbio/openprom header selection); `os/backtrace.c` `__sun&&__SVR4`→`HAVE_PSTACK` (already a clean OS→named-capability map; pstack is Solaris-only); `os/Xtranssock.c:231` `SOCKLEN_T` typedef fallback (compat quirk, already partly `HAVE_SOCKLEN_T`-based). `SYSV`/`BSD44`/`SYSV386` don't occur. Inventory: `git grep -n 'CSRG_BASED\|SVR4\|__SVR4'` |
| HDR (High Dynamic Range) display support | **Analysis done, first prep extraction built and pushed** — `wip/hdr-prep` has 2 build-verified commits pulled out of cepelinas9000's WIP, pushed to `X11Libre/xserver` (plain branch, no PR yet) for cross-clone review; the actual rendering pipeline is **not** extracted (see why below) | Discussion `X11Libre/misc#251`; external WIP fork `cepelinas9000/xserver@wip/x11hdrV2`; branch `wip/hdr-prep` **pushed to `X11Libre/xserver`** (https://github.com/X11Libre/xserver/tree/wip/hdr-prep), tracked locally in agent clone `_WORK_/xserver-master/agent/hdr-prep/xserver` (remote `cepelinas` added there for fetching his branches; extraction base was master commit `cb5f2cd67e`, Feb 17 2026, 871 commits behind current master when analyzed) | Praetor 2do, 2026-07-01, continued 2026-07-01/07-02: "look at his branches, what exactly he did, pull out prep pieces as separate PRs." **What's actually in `wip/x11hdrV2`:** one giant unsplit commit ("Reworked X11 HDR", 271 files / +46.5k lines) against a Feb-17 master base. The huge line count is misleading — **~44k of those lines are the vendored `cglm` MIT-licensed C math library** (`glamor/hdrext/cglm/`, ~150 header files, wholesale drop-in, not a system dep) plus a stray `.gdb_history` debug artifact that must never be carried over. The real HDR code is concentrated in `glamor/hdrext/` (new subdir: `hdrext.c/.h`, shader sources, a draft wire-protocol doc `HDRProto.txt` mirroring Vulkan structs over X11, `applyvulkanproperties.c`), the **modesetting** driver (`driver.c`, `drmmode_display.c`/`.h`, `drmmode_bo.c`/`.h` — DRM connector colorspace/HDR-metadata property plumbing), deep **glamor** shader/program refactoring (`glamor_program.c`, `glamor_render.c`, `glamor_text.c`, etc. — new colorimetry-aware shader variants per draw op), and EDID/DDC extended-block parsing. **Extracted into `wip/hdr-prep` (2 commits, build-verified `meson setup` + `ninja hw/vfb/Xvfb test/tests`, unit test passes via `XLIBRE_TEST=ddc_tests`):** (1) `xfree86: ddc: parse CTA-861-G HDR static metadata + colorimetry data blocks` — new `xf86DoInterpretHDRMetadata()` walking the EDID CTA extension's Colorimetry (tag 5) and HDR Static Metadata (tag 6) data blocks into a new tail-appended `xf86Monitor::hdr` field, with a dedicated unit test (`test/ddc-tests.c`) using 4 real captured hardware EDIDs (Samsung Q800T, MSI MAG321CURV, HP 27 QD, QEMU-virtual-as-negative-case) — this is exactly the "clean extended-EDID-parsing prerequisite" metux asked for back in January (superseding/complementing the earlier #1881→#1933/#1934/#1935 DDC EDID entry-point work, which didn't cover CTA HDR/colorimetry blocks). (2) `test: add XLIBRE_TEST env var to select which unit test suites run` — a small, fully HDR-unrelated test-harness dev-QoL improvement found incidentally in the same commit. Both commits keep cepelinas9000 (`Tautvis <gtautvis@gmail.com>`) as `--author`, with a light typo/whitespace cleanup pass (e.g. `tf_hlf`→`tf_hlg`, `tradinional`→`traditional`) noted in the commit body — logic untouched. **Why the rendering pipeline itself was NOT extracted as "prep":** it's genuinely inseparable from the full feature (glamor shader/struct plumbing only makes sense wired end-to-end), and it surfaces several real design/ABI concerns that need a praetor/upstream call, not a quiet fixup: (a) `include/misc.h` bumps `MAXFORMATS` 8→10 with cepelinas's own comment *"XXXX breaking ABI XXXX"* — a genuine, self-admitted ABI break (grows a fixed array inside `ScreenInfo`/related structs); (b) `dix/dispatch.c`+`dix/tables.c` repurpose core protocol **opcode 125** (previously `ProcBadRequest`) for a "latch visual/depth for this client" hack (`XXXProcLatchUnlatchVisual`, triggered by sending a sentinel-valued `xCreateWindowReq`-shaped packet) to work around Vulkan WSI blindly copying the parent window's visual — bypasses the extension mechanism entirely, likely to draw strong review pushback; (c) `include/glamor.h` bumps the minimum required OpenGL core profile from 3.1 to **4.6** with the comment *"I didn't check which minimum version required"* — a potentially serious HW-compatibility regression if it ever landed as-is; (d) additive-only (tail-appended, lower-risk per our ABI precedent) new fields on `ScreenRec`/`ScrnInfoRec` (the latter correctly placed *after* the existing `reserved*` padding arrays — the one part of the patch that shows real ABI awareness) plus `GCRec`/`PictureRec`/`ClientRec`, and a new `HDRColor` visual class constant (`include/colormap.h`, value 6) that doesn't exist in the core X11 protocol; (e) glamor and the whole `hdrext/` subtree gain a **new build dependency on Vulkan headers** (`#include <vulkan/vulkan.h>` in `hdrext.h`) that isn't currently a server-side dependency anywhere. None of (a)-(e) are prep-extractable — they need to be resolved as part of a real design pass on the feature itself. **Other prior-session findings still stand:** `wip/x11hdr`(v1)/`wip/x11hdrV2` progression, the `__X11HDR_SDR_PARAMS` per-window property, the `VK_xlibre_layer` companion repo, the open PQ-vs-HLG / per-window-colorspace design threads, the TimothyLottes 8-bit-passthrough objection, and the `HaplessIdiot`-authored-posts caution — see git history of this row for the full writeup. **Next step:** praetor review of the two `wip/hdr-prep` commits (pushed as a plain branch, not a PR, per "erstmal in Ruhe anschauen" — deliberately not opening a PR until reviewed); separately, a praetor/cepelinas conversation on (a)-(e) above before any of the actual pipeline work is prep-extractable. **2026-07-02: full reconstruction built, local-only, not yet pushed — branch `wip/hdr-full`** in the same agent clone, one commit on top of `wip/hdr-prep`, reconstructing everything else from `wip/x11hdrV2` (i.e. what his branch would look like rebased onto the two prep commits + current master) — kept as faithful as possible, none of (a)-(e) fixed. Needed hand-adaptation for ~870 commits of drift since his Feb-17 base: the `Xext/<ext>/` reorg (`composite/`→`Xext/composite/`, `dri3/`→`Xext/dri3/`), `dixstruct.h`/`scrnintstr.h` struct fields re-anchored to the true tail (both structs grew new trailing members upstream since Feb — `saveSets`, an `xfixes` block), and `glamor_egl.c`'s depth→format helper (renamed/reshaped by the unrelated DRM_FORMAT/GBM_FORMAT consolidation, issue #2142/PR #2296) — his `bpp`-aware HDR format special-casing was ported onto the new function shape. Dropped the stray `glamor/hdrext/tests/.gdb_history` debug artifact for good. **Build-verified clean from scratch** (`meson setup -Dxorg=true -Dtests=false` + full `ninja`, 684/684 targets: `Xorg`, `Xnest`, `Xvfb`, `modesetting_drv.so`, `libglamoregl.so`, `libglx.so`) — checked `libglamoregl.so`/`modesetting_drv.so`/`libglx.so` for the known double-compile-into-two-loaded-modules hazard (see "static lib linked into two loadable modules" in `AGENTS.md`): clean, `HDRExtensionInit` only appears in `libglamoregl.so`. `-Dtests=false` was needed because `glamor/hdrext/tests/` pulls in `criterion`, unavailable/not installable (no root) in this sandbox — cepelinas's own unit tests are therefore **not yet verified**, only the main build. **Found a real bug while reconstructing** (left in, not fixed, per "faithful reproduction" — flagging instead): `drmmode_output_get_modes()` in `drmmode_display.c` has `if (mon && edid_blob->length > 128)\n    mon->flags |= ...;\n    xf86DoInterpretHDRMetadata(mon);` — missing braces mean the HDR-metadata call is **not actually guarded by the `if`**, so it runs even when `xf86InterpretEDID()` returned NULL for a malformed EDID, and `xf86DoInterpretHDRMetadata()` dereferences unconditionally — a likely NULL-deref crash on a bad EDID. Caught by GCC's `-Wmisleading-indentation` while building the modesetting driver. |
| Wire valgrind (memcheck) into CI, hunt memleaks/UAF/OOB | **Done — PR open + fully green, awaiting praetor merge decision** | PR #3225 (`ci: run pyxtest under valgrind memcheck on Ubuntu`, master, single clean commit `77b2d64733`, open, not draft, mergeable) | Praetor request, 2026-07-02 (see git history of this row for the full initial research/dispatch). **Gap confirmed:** `pytest` wasn't installed in the Ubuntu CI image at all, so `pyxtest`'s existing-but-unused valgrind harness was silently never built/run. **Shipped:** `install-pkg.sh` gains `python3-pytest`/`-timeout`/`-xdist`/`valgrind`; new `.github/scripts/ubuntu/run-pyxtest-valgrind.sh` (builds `xorgproto` from source first, then a debug Xvfb-only config, then `pytest --valgrind test/pyxtest/`); new dedicated CI job `xserver-build-ubuntu-valgrind` (kept separate from the existing ubuntu lanes — valgrind is slow, pyxtest only needs Xvfb). Side effect (intentional, in the commit message): the existing `xserver-build-ubuntu*` jobs now also run plain (non-valgrind) pyxtest for real, since `pytest` stops being silently missing there. **One CI iteration needed:** first push failed fast — the new job's meson configure skipped the `xorgproto`-from-source prereq step the other jobs rely on, so it saw the Ubuntu image's too-old system `presentproto` (1.3 vs required ≥1.4); fixed by adding that same build step, confirmed via job logs (`presentproto found: YES 1.4`). **Final CI: fully green** — 23 passed, 28 non-blocking skips (gating/dedup), 0 pending, 0 failing, including the slow `xserver-build-solaris` (18m29s). The valgrind lane itself: 29 passed, 19 skipped, ~2min, real valgrind 1:3.22.0-0ubuntu3 on the runner. **Valgrind findings: none** — clean baseline, no leaks/invalid-access/UAF found on this run; documented as such in the PR body rather than silently omitted. No new `@pytest.mark.valgrind` markers added — the job's `--valgrind` CLI flag already forces the whole suite under valgrind regardless of per-test markers, so this already covers more than just the one previously-marked test. **Not merged** — master-PR auto-merge needs an explicit user request, which wasn't given; PR is ready and waiting on that call. |
| Request-handler conversion to `X_REQUEST_*()`/`X_REPLY_*()`/rpcbuf macros | **Xinerama family DONE (2 PRs open); broader dix/Xext sweep pending praetor OK on scope/granularity** | PR #3229 (`panoramiXprocs.c`, 51 handlers), PR #3230 (`rrxinerama.c` + `pseudoramiX.c`); agent clone `_WORK_/xserver-master/agent/xreq/xserver`, branch `wip/xreq-panoramix` | Praetor request 2026-07-02: convert **all** request handlers to the `dix/request_priv.h` macros (`X_REQUEST_HEAD_STRUCT`/`_AT_LEAST`/`_NO_CHECK`, `X_REQUEST_FIELD_*`, `X_REPLY_FIELD_*`, `X_SEND_REPLY_*`/rpcbuf), **especially Xinerama**; "erstmal nur PRs" (open only, don't merge). **Done:** the whole Xinerama family. `panoramiXprocs.c` was the big one (51 handlers) — those hook only `ProcVector[]` and do no request-field swapping (core dispatch's `SwappedProcVector` handles that), so it's a pure `REQUEST()`+`REQUEST_SIZE_MATCH/AT_LEAST` → `X_REQUEST_HEAD_*` conversion, swap blocks untouched; `-141/+51` lines. `rrxinerama.c`/`pseudoramiX.c` are the extension's *own* handlers (they swap their own req/reply fields) so they got the full HEAD+FIELD+REPLY_FIELD treatment, mirroring the already-converted `panoramiX.c`. Both build-verified (Xvfb/Xnest link; `pseudoramiX.c` is macOS-only → syntax-checked with real build flags). Dispatch demuxes (`Proc*Dispatch`, raw `REQUEST(xReq)` to read the minor opcode) correctly left as-is. **Gotcha hit:** `xx-make-pr.sh` on the 2nd PR exceeded the 120s foreground Bash timeout mid-run — it had already pushed the branch (clean, no `[PR #…]` marker since the rewrite step never ran) but hadn't done `gh pr create`; recovered by creating #3230 manually with `gh pr create`. **Remaining sweep (NOT started — needs praetor go-ahead on how granular/which files, and dix/dispatch.c is sensitive):** `git grep -c REQUEST_SIZE_MATCH\|REQUEST_AT_LEAST_SIZE dix Xext` ranks the rest — `dix/dispatch.c` (~70, core protocol, most sensitive), `dix/swapreq.c` (~47, these are the *swap* side — different pattern), `Xext/glx/*` (glxcmds/vnd/glxcmdsswap, complex generated dispatch), `dix/events.c` (16), `dri2ext.c` (14), `dix/devices.c` (13), `present_request.c` (12), `randr/*` (rrcrtc 9, rrscreen/rrproperty/… 3-6 each), `record.c` (8), plus smaller ones. Recommend doing the clean extension files first (present, dri2, randr/*, record) as per-file PRs, and treating `dix/dispatch.c` + GLX as their own carefully-reviewed passes. |
| if/while/for brace-everywhere conversion (xserver coding style) | **Claimed by Potemkin, 2026-07-06 — first batch open, CI running** | PR #3258 (`os/Xtranssock.c` `set_sun_path()`, master, single commit, build-verified via `meson setup` + `ninja hw/vfb/Xvfb hw/xnest/Xnest`) | Praetor confirmed 2026-07-03: always brace `if`/`while`/`for`/`else` bodies, even single-statement, in **all new/touched code from now on** (already the rule since PR #3199, now made permanent policy) — **and** convert existing unbraced bodies successively, in small self-contained batches (one file or a handful of functions per PR, brace-only diffs, no mixed changes). **First batch (Potemkin):** braced `set_sun_path()`'s 4 previously-unbraced `if`/`else if` bodies, matching the file's already-dominant same-line brace style; scope deliberately kept to exactly this one function (the candidate this row itself named earlier), not the whole file, to keep the diff trivially reviewable. Update this row each time a further batch lands so the initiative doesn't stall silently. |
| Xinerama/PanoramiX refactor — replace proc-vector hooking with a frontend/backend split | **Idea stage — plan specified by praetor, no branch yet** | no doc yet; planned as a **fully separate branch** (not created) | Praetor request, 2026-07-02: successively get rid of the current detour via request-handler proc-vector hooking. **Plan:** split each affected proc into a **frontend** (owns all protocol handling — request parsing, checks, etc. — and stays the single `ProcVector` entry) and a **backend** doing the actual execution (e.g. calling into the screen's ops); one backend for normal/single-screen mode, one for Xinerama mode. Xinerama-init then just flips a flag the frontend checks to pick which backend to call, instead of swapping `ProcVector` entries wholesale. Praetor has done this split for some procs before (precedent referenced, not yet located in current in-tree history — worth asking for pointers before starting). **Current-state survey (2026-07-02, via Explore agent, not manually re-verified line-by-line):** today's hooking lives in `Xext/panoramiX/panoramiX.c` — a static `SavedProcVector[256]` (line 108) stashes the original handlers, and `PanoramiXExtensionInit()` (lines 422-570) directly overwrites `ProcVector[X_*]` with Xinerama-aware wrappers for roughly **50 opcodes** across three groups: window/geometry (CreateWindow, ChangeWindowAttributes, DestroyWindow, DestroySubwindows, ChangeSaveSet, ReparentWindow, MapWindow, MapSubwindows, UnmapWindow, UnmapSubwindows, GetGeometry, TranslateCoords, ConfigureWindow, CirculateWindow), pixmap/GC/drawing (CreatePixmap, FreePixmap, CreateGC, ChangeGC, CopyGC, SetDashes, SetClipRectangles, FreeGC, ClearArea, CopyArea, CopyPlane, PolyPoint, PolyLine, PolySegment, PolyRectangle, PolyArc, FillPoly, PolyFillRectangle, PolyFillArc, PutImage, GetImage, PolyText8, PolyText16, ImageText8, ImageText16), and colormap (CreateColormap, FreeColormap, CopyColormapAndFree, InstallColormap, UninstallColormap, AllocColor, AllocNamedColor, AllocColorCells, AllocColorPlanes, FreeColors, StoreColors, StoreNamedColor). The wrapper implementations live in `Xext/panoramiX/panoramiXprocs.c`, mostly looping over screens via the `XINERAMA_FOR_EACH_SCREEN_BACKWARD` macro and calling `SavedProcVector[X_*]` per screen (re-dispatching into the saved original single-screen proc, not a dedicated backend function). The controlling flag is `noPanoramiXExtension` (panoramiX.c:68), checked only once at init time to decide whether to install the hooks — not per-request by the wrappers, which is exactly the indirection this refactor removes. **One partial precedent for a frontend/backend-like split:** `PanoramiXCreateWindow` (panoramiXprocs.c:158) calls `DoCreateWindowReq()` directly instead of `SavedProcVector[X_CreateWindow]` — the closest thing today to a dedicated backend entry point, but incidental (dix already had `DoCreateWindowReq` as an internal helper) rather than a deliberately designed pattern; the other wrappers have no equivalent. **Next step:** praetor to point at the past procs this split was already done for (as a template), then start in a fresh branch — no code written yet, this row exists purely so the plan survives between sessions. **See also** the Parkplatz row "Xinerama-Umbau: interner Proxy-Screen" — a same-day, related-but-distinct praetor idea for the same subsystem (route default-screen traffic through one hidden internal `ScreenRec` rather than a per-proc frontend/backend split); the frontend/backend split here could plausibly be a stepping stone toward that (once every proc explicitly picks a backend via a flag, that flag could later generalize into "does this screen dispatch land on the real screen or the internal Xinerama-proxy"). Not reconciled — two independent praetor asks to two sessions the same day, not yet compared against each other by the praetor. |
| Starfleet ↔ Telegram integration — send & receive Telegram messages | Idea stage — just requested, nothing started | no doc/branch yet | Praetor request, 2026-07-03. Goal: bridge the fleet control plane (`agent-bus`/`starfleetctl`) with Telegram so directives/notifications can be sent and received via a Telegram bot, not just desktop notify (`agent-bus-watch`) or in-context `Monitor` surfacing (see "Agent control plane" row). Scope/design not yet discussed with praetor — bot token handling, which side initiates (agent-bus → Telegram push, and/or Telegram → agent-bus `tell`), self-hosted-only per [[prefers-self-hosted-no-cloud]] preference (a Telegram bot itself is a cloud API dependency, so this should be flagged/confirmed with the praetor before building, not assumed in conflict with that preference). |
| Split `DASHBOARD.md` into per-theme files (`dashboard/themes/<slug>.md`) + a thin generated index | **Claimed by Potemkin, 2026-07-06 — design proposal complete, awaiting fleet/praetor review** | `DASHBOARD-RESTRUCTURE.md` (full proposal) | Directive m0048 (Enterprise + praetor), 2026-07-06: this file scales poorly (see the starfleetctl row above — now a wall-of-text single cell) and concurrent per-theme edits keep colliding on the whole file. Explicitly design-only, no implementation yet. Full answers to the four posed questions live in `DASHBOARD-RESTRUCTURE.md`: (1) **Markdown + small YAML frontmatter** per theme file (slug/title/status/since/owner), mirroring Claude Code's own memory-file format — not pure YAML (prose doesn't fit cleanly) and not plain Markdown (the thin index needs a few structured fields without prose-parsing heuristics); (2) new `starfleetctl dashboard theme list|show|write|commit|new` (scoped to one file) + `dashboard reindex` (mechanically regenerates the thin index from all themes' frontmatter — this is the actual concurrency fix, since two ships touching two different theme files never collide, and two racing `reindex` runs converge to the same byte-identical output); (3) migration as **one mechanical copy-out commit**, `migrated_from: <sha>` recorded in each theme file's frontmatter since a content split can't carry per-row git history the way `git mv` would for a whole-file rename — stated as an honest limitation, not glossed over; (4) `[[slug]]` cross-refs, generalizing a convention this file *already* informally uses (the Telegram row above links `[[prefers-self-hosted-no-cloud]]`, a Claude-memory slug) — textual/grep-able, no auto-resolving link tooling. Explicitly excludes ship/agent live status (stays in `agent-bus board`, per directive). **Next:** praetor/fleet review of the proposal; implementation (once approved) should happen from an isolated `scripts/worktree` checkout, not the shared interactive one, since it's one big mechanical diff touching every row at once. |
| Permission-confirmation telemetry — log Bash calls that would need an interactive prompt, mine for new `starfleetctl`/`scripts/*` wrapper candidates | **Claimed by Potemkin, 2026-07-06 — built, tested live, shipped to shared `.claude/settings.json`** | `scripts/confirm-log-hook`, `scripts/confirm-log-report` | Directive m0050 (Enterprise + praetor), 2026-07-06. Researched the exact `PreToolUse` contract first (via the `claude-code-guide` agent): input is `{session_id, hook_event_name, tool_name, tool_input:{command}, cwd, permission_mode}`; exit 0 with empty stdout (no `hookSpecificOutput` at all) is unconditionally neutral — deny/ask rules still evaluate exactly as without the hook; real `Bash(...)` matching splits compound commands on `&&`/`\|\|`/`;`/`\|`/`\|&`/`&`/newline and strips `timeout`/`time`/`nice`/`nohup`/`stdbuf`/bare-`xargs` before matching each sub-command independently (confirmed against AGENTS.md's own documented "export AGENT_ID=Foo && scripts/agent-bus board prompts" gotcha — that's exactly a sub-command-independent-match failure, not whole-line prefix matching as originally assumed there). Built `scripts/confirm-log-hook`: reimplements that matching against the union of project/local/user `permissions.allow\|deny\|ask`, logs one JSONL line per unmatched sub-command to `_WORK_/agent-bus/tooling-gaps/events.jsonl` (`flock`-serialized). **Two real bugs caught during live testing** (not just unit tests with crafted stdin): (1) the compound-splitter initially split `2>&1`/`>&2` FD-redirects at the bare `&`, corrupting the sub-command (`cat f 2>&1` → `cat f 2>` + `1`) — fixed by not splitting a `&` immediately preceded by `>`/`<`; (2) a hardcoded "only log when `permission_mode == default`" turned out wrong — live-wiring into the (per-host-shared, gitignored) `settings.local.json` showed a real fleet-worker session running `acceptEdits` executes an unlisted Bash command with **no visible prompt at all**, so that assumption would have silently under-collected everywhere except `default` mode. Fixed by logging every mode except `bypassPermissions` (where nothing ever prompts by definition) and recording the mode on each entry instead of filtering by it — push the "what does no-match mean in this mode" judgment call to analysis time, not collection time. **Coordinated with Pegasus per Enterprise's m0052 nudge** (their m0051 active-escalation-on-deny effort, for `dontAsk`-mode autoscaled ships, overlaps in intent): agreed on a shared `_WORK_/agent-bus/tooling-gaps/` directory + a `source: "hook"\|"escalation"` field so both mechanisms feed one collection point once m0051 actually ships (currently paused per m0053, pending direct praetor sign-off since it lowers the unattended-operation confirmation bar — not this task's blocker). **Verified live, in-session, twice** (once accidentally fleet-wide via the shared `settings.local.json`, which is how bug #2 above was even caught — Constellation's own no-match commands showed up in the log without any disruption to their session; once deliberately after cutting over to the committed `settings.json`): allowed commands produce zero log entries and zero behavior change, unlisted commands (`whoami`, `pwd`, `cat …`) get logged correctly with no new prompt observed and no perceptible latency (~30ms/call measured, mostly Python startup). Added `scripts/confirm-log-report [--top N] [--words N] [--category …]` (requirement 3) — ranks the most common non-matched prefixes; empty-log-safe. **Not done, by design (requirement 4):** no new wrapper scripts or allowlist entries proposed from findings yet — needs the log to actually accumulate real fleet signal first (bare shell utilities like `cat`/`ls`/`pwd`/`echo` are unlisted and already showed up as noise in testing, but that's a "broaden the baseline allowlist" finding, not a "build a wrapper" one — a distinction worth keeping straight when this gets analyzed later). **Next:** let it run and accumulate across the fleet, periodically `scripts/confirm-log-report`, propose whatever clear pattern emerges as a new Parkplatz row (not built here). |

## Parkplatz

Angefangen/aufgefallen, aber (noch) nicht weiterverfolgt — kurze Notiz statt Verlust.

| Thema | Notiert | Seit | Notiz |
|---|---|---|---|
| **`xserver-build-freebsd` CI lane currently red fleet-wide — NOT a transient flake, a real OS/package version-skew that won't self-clear on retry** | Potemkin, 2026-07-06 | 2026-07-06 | Found while chasing a FreeBSD failure on PR #3258 (a trivial brace-only style change). **Correction of an earlier, too-hasty read of this same log:** initially misdiagnosed as a generic pkg-mirror network flake (the visible symptom, `'pkg update -f' failed (attempt N/3)`, looks exactly like one) — the *actual* error a few lines earlier in the log is `pkg: repository FreeBSD contains packages for wrong OS version: FreeBSD:14:amd64` / `Newer FreeBSD version for package zycore-c: package: 1404000, running userland: 1403000` / `Ignore the mismatch and continue? [y/N]:`. `.github/workflows/build-xserver.yml`'s three `vmactions/freebsd-vm@v1` blocks pin `release: "14.3"`, but FreeBSD's official package repo has rolled its `FreeBSD:14:amd64` ABI branch forward to (at least) 14.4-built packages — `pkg` refuses the mismatch non-interactively (no TTY in CI to answer the prompt) and the whole `pkg update -f` dies, so the job never reaches the actual build. **This will NOT clear itself on retry** (confirmed: reran twice, identical failure both times) — reruns only make sense for genuine transient flakes, not a fixed version mismatch. **Confirmed systemic** (checked two unrelated concurrent runs — `fbdev-explicit-rate` PR run 28787951202, plain `master` run 28740407536 — both red the same way), i.e. **every FreeBSD job on every PR and on `master` itself is currently broken** until the workflow is updated. **Fix opened: PR #3259** (`ci: freebsd: set IGNORE_OSVERSION=yes`, master, single commit) — chose the `IGNORE_OSVERSION=yes` stopgap over bumping the `release: "14.3"` pins (lower risk, doesn't change what OS version is tested, matches pkg's own suggested remedy from the error message). CI running on the fix PR itself as verification — the real proof is whether `xserver-build-freebsd` actually goes green there. |
| `starfleetctl` CLI structure: group subcommands instead of one flat namespace | praetor, 2026-07-06 | 2026-07-06 | With 11+ subcommands already (and Phase 2's mutating pr-* subset about to add ~9 more), `starfleetctl <name>` is becoming a flat, undifferentiated list. Idea: split into subcommand **groups** — e.g. pure fleet-coordination (`agent-bus`, `pr-claim`, `ws-commit`, `ship-names`, `with-clone-lock`, `dashboard`) under one group, GitHub-interaction (`pr-view`, `pr-ci`, `show-branch-file`, `backport-applies`, `show-pr-conflict`, and the Phase-2 mutating set) under another — something like `starfleetctl fleet <cmd>` vs `starfleetctl gh <cmd>` (exact names TBD). Explicitly a **later** consideration, not urgent — do not restructure the CLI surface while Phase 2's cutover and the README (m0061) are still in flight, since that would churn the exact commands/docs being written right now. Revisit once the current subcommand set stabilizes; would need a compatibility thought (keep flat aliases during transition, or a clean breaking change since every caller is internal to this workspace anyway — no external consumers to preserve compat for). |
| `agent-bus` has no message authentication — any ship identity is pure self-report | Constellation (m0032) + Endeavour (m0033), independently, 2026-07-06 | 2026-07-06 | Both correctly refused to adopt the `maintainer`→`praetor` vocabulary change (broadcast m0031) pending verification, since a `tell`/`broadcast` claiming to be from `Enterprise` cannot be cryptographically distinguished from a spoofed one — `AGENT_ID` is just an env var, and commit-author metadata proves nothing either (every `mtx/*`-grant session commits as the same git identity). Enterprise confirmed the specific instance was legitimate (given live, in the same continuous chat as the praetor's away-announcement and 5-phase roadmap — internal consistency, not cryptographic proof) via m0035/m0036; both ships adopted it after. **The underlying gap is real and worth fixing before agent-bus ever carries higher-stakes directives** (e.g. merge-authorization, destructive actions) — some kind of signing/shared-secret/out-of-band confirmation for directives from the flagship. Not scoped/designed yet. Good sign: this is exactly the skepticism the fleet should apply to unauthenticated bus traffic — encourage, don't suppress. |
| **RESOLVED — Vokabel-Änderung "maintainer" → "praetor"** (Commit `9c7d33f`), bestätigt legitim | Constellation, 2026-07-06 | 2026-07-06 | Während der Autonomie-Phase (m0030, Praetor/Maintainer laut Enterprise ein paar Stunden nicht erreichbar) hat Commit `9c7d33f` in `AGENTS.md`/`DASHBOARD.md` jede Erwähnung von "maintainer" durch "praetor" ersetzt (~62 Stellen, rein mechanisches Such-Ersetzen, keine Regelinhalte geändert — geprüft per `git show 9c7d33f`). Passend dazu kam agent-bus-Direktive **m0030** von "Enterprise" mit der Aufforderung, "Praetor" fleet-weit auch in eigener Kommunikation zu verwenden. **Warum das Vorsicht verdient, nicht blinde Übernahme:** (1) die Änderung geschah *während* der erklärten Abwesenheit des echten Maintainers, nicht auf seine direkte Anweisung im Chat; (2) sie betrifft, wie die Flotte die menschliche Autorität selbst bezeichnet/erkennt — eine Identitäts-/Grundsatzentscheidung, die laut AGENTS.md an anderer Stelle explizit **nicht** unaufgefordert getroffen werden soll (vgl. "Generalize onto master" / Lizenz-Retrofit: "deliberate, separate, later decision the maintainer makes"); (3) sie war **nicht** Teil der in m0030 aufgezählten Autonomie-Leitplanken (mk-agent-clone, pr-claim, Signed-off-by, kein Automerge auf release/*, HW-Routing, NVIDIA-ABI, Sprachregel); (4) agent-bus-Nachrichten sind nicht kryptografisch authentifiziert — jede Session mit `AGENT_ID=Enterprise` könnte m0031 verschickt haben, ob nun der echte Enterprise-Agent es war oder nicht ist von hier aus nicht verifizierbar. **Kein Hinweis auf böswillige Absicht** (der Diff ist inhaltlich neutral, keine Sicherheits-/Autorisierungsregel wurde inhaltlich verändert, nur benannt) — aber genau deshalb reicht ein kurzer Check beim echten Maintainer, sobald er wieder erreichbar ist, statt dass die Flotte das unternehmenden Fakt einfach weiterträgt. **Bis dahin (Constellation, 2026-07-06):** verwende "maintainer" weiter in eigener Kommunikation, habe den Commit nicht revertiert (Revert wäre ebenso unilateral), und habe Enterprise per agent-bus informiert, dass ich abwarte. **Auflösung (m0035, Enterprise, 2026-07-06):** mindestens ein weiteres Schiff (Endeavour, m0033) hatte denselben Einwand unabhängig erhoben. Enterprise erklärte: die Umbenennung wurde direkt im selben durchgehenden Live-Chat mit dem echten Menschen (Enrico) angefragt, in dem kurz zuvor auch die Außeneinsatz-Ankündigung und die 5-Phasen-Roadmap kamen — keine Bus-Weiterleitung einer dritten Quelle. Enterprise räumte offen ein, das nicht kryptografisch beweisen zu können, bestätigte die reale Lücke (agent-bus hat keine Nachrichten-Authentifizierung, jede Ship-Identität ist Selbstauskunft) und bekräftigte, dass Skepsis bei künftigen höher-riskanten Bus-Direktiven (Merges, destruktive Aktionen) weiterhin der richtige Reflex bleibt — nur für diesen konkreten, folgenlosen Vokabel-Fall sei die Anfrage verifiziert legitim. Constellation und Endeavour übernehmen "Praetor" ab jetzt. |
| **Uncommitted security-fix worktrees at `/home/nekrad/src/xorg/xserver-rel-25.0` and `-25.1`** — outside mpbt-workspace, never tracked in agent-bus/DASHBOARD | Enterprise, 2026-07-03 | 2026-07-03 | Found while checking "who uses these dirs" for the praetor. Both are **linked git worktrees** of `/home/nekrad/src/xorg/xserver` (main clone, on `master`), on **unborn** branches `rfc/security-fixes-release-25.0`/`-25.1` (no commits, but full tree staged — classic `git checkout --orphan` artifact). No untracked files beyond `build/`, no reflog, no running process/tmux session touches them. **Real, unpublished work confirmed** — do NOT delete without extracting first: both worktrees carry, identically, (1) an integer-overflow/negative-size guard added to `render/filter.c`'s `convolutionFilterValidateParams` (rejects `w<0`\|\|`h<0`, uses `int64_t` for the product) and (2) an OOB-read guard added to `Xi/xiselectev.c`'s `ProcXISelectEvents` (bounds-checks before dereferencing `evmask->mask_len`) — **neither exists anywhere on `master`** (checked both at the current path and the `Xext/render/`\|`Xext/xinput/` reorg path), so this looks like original hardening authored directly on the release lines, never upstreamed. **Needs scrutiny before landing:** both worktrees also re-add a `xorgGlxCreateVendor();` call in `glamor/glamor_egl.c` — the *inverse* of PR #3174 (which removed that exact call on master to fix the `libglx.so`/`libglamoregl.so` duplicate-symbol issue, see AGENTS.md); could reintroduce that bug, verify before use. The `25.1` worktree's base is stale (meson.build shows 25.1.7/2026-06-14 vs current `origin/release/25.1` at 25.1.8/2026-06-21) — its extra-looking diffs in `Xext/panoramiX.h` (macro `__LAMBDA__`→`__VA_ARGS__`), `Xi/xiproperty.c` (switch bracing) and `hw/xfree86/common/xf86DGA.c` (reverts PR #3173's `x_rpcbuf_write_string_0t_pad` fix) are **just staleness noise**, not deliberate edits — confirmed those fixes are already on current `origin/release/25.1`, so a rebase onto the current tip drops them cleanly. `25.0`'s worktree base already matches current `origin/release/25.0` exactly (only the 3 genuine files differ) — no staleness issue there. **Next step:** rebase 25.1's staged tree onto current `origin/release/25.1`, re-verify the `glamor_egl.c` hunk, then commit + run through the normal backport-workflow (`mk-agent-clone` + PR) instead of leaving it in an ad-hoc worktree outside the fleet's tracking. **Update 2026-07-03 (Potemkin):** confirmed **both** vulnerable patterns exist verbatim on current `master` too (`Xext/render/filter.c` still has plain `w * h > nparams`; `Xext/xinput/xiselectev.c`'s non-swapped path still reads `evmask->mask_len` before bounds-checking it — the swapped path right above already has the check, so it's a real asymmetry) — filed as a **master PR first, per normal workflow: #3244** (`render/Xi: fix integer-overflow and OOB-read input-validation bugs`, 2 commits, build-verified via `ninja hw/vfb/Xvfb`), CI running. **`glamor_egl.c` hunk: discarded, not carried into #3244 or any backport.** Verification: `nm` on the currently-built `libglamoregl.so`/`libglx.so` (from `_WORK_/xserver-master/target/…`) shows `xorgGlxCreateVendor` already present as a **local** symbol in *both* modules even on unmodified current master (`Xext/render/meson.build`'s `glamoregl` target still `link_with: [glamor, libxserver_glx]`) — so #3174's fix (removing the explicit call) didn't fully eliminate cross-module duplication at the object-file level, and the actual PR #3174 concern was specifically about the *duplicated static-guard flag* (`vendor_initialized` et al.) firing per-module-copy, not the mere symbol existing twice (harmless for `static`/local symbols by itself). Given that ambiguity plus the task's explicit "im Zweifel verwerfen", **not** re-adding the explicit `xorgGlxCreateVendor()` call is the safe call — it's exactly the line #3174 removed on purpose, and nothing in this batch of work depends on it. **Next step:** once #3244 is merged, backport both commits to `release/25.2`/`25.1`/`25.0` via `scripts/backport-commit`, then delete the two ad-hoc worktrees (only after the backport PRs exist). **DONE 2026-07-06 (Potemkin) — fully landed, closing this row:** #3244 merged to master as `6a97f6d38e` (filter.c) + `bf6b62648d` (xiselectev.c). Backported to all three release lines via `scripts/backport-commit` from an isolated `mk-agent-clone master security-fixes` clone: `release/25.2` → **#3245**/**#3246**, `release/25.1` → **#3247**/**#3248**, `release/25.0` → **#3249**/**#3250** (all 🔄 open, awaiting praetor's manual release-line merge per the standing no-auto-merge-on-release rule). Backport dashboard table added to #3244's body; each backport PR links back to #3244. Before deleting, re-verified both ad-hoc worktrees file-by-file against their release tips (`git diff --stat origin/release/<rel> -- .`): `25.0` differed only in the 3 known files; `25.1` additionally showed `panoramiX.h`/`xiproperty.c`/`xf86DGA.c`/`meson.build` diffs, all confirmed to be pure staleness (worktree predates upstream changes already merged, e.g. still had the pre-#3173 unpadded `xf86DGA.c` write) — no real extra work lost. Both worktrees + their unborn branches removed (`git worktree remove --force`). |
| **FlyingTux + go-x11proto in mpbt integrieren** (in die Fleet holen, statt Außenposten) | praetor, 2026-07-02 | 2026-07-02 | Ziel: die beiden Schwester-Projekte **vollständig in die mpbt-Fleet** aufnehmen, statt sie nur als externe Repos „von außen" mitzubauen — damit die Arbeit daran denselben orchestrierten Build/CI/Tooling-Rahmen bekommt wie die xserver-Release-Linien. **FlyingTux** (`/home/nekrad/src/flyingtux`, eigenes Repo — siehe Aktive-Themen-Zeile) und **go-x11proto** (`/home/nekrad/src/xorg/go-x11`, `X11Libre/go-x11proto` — siehe dessen Zeile) je **ggf. als komplett eigene mpbt-Solution** (analog `cf/<release>/solutions/…`), mit eigenen packages/config, statt Ad-hoc-Builds. **✅ go-x11proto DONE (2026-07-02):** eigene Solution `cf/go-x11proto/` (config + `solutions/default.yaml` + `packages/xlibre/go-x11proto.yaml`) + Wrapper `run-{fetch,build,opencode}.go-x11proto`, **separat vom xserver-Build**. Clone **verschoben** (nicht neu geklont — erhält 6 Stashes + lokale Branches) nach `_WORK_/go-x11proto/sources/xlibre/go-x11proto`; `run-fetch` (fetch + `make-pr.*`-config, non-destruktiv) und `run-build` (`buildsystem: exec` → `make` → `go build`) grün getestet. Doku: AGENTS.md „go-x11proto and FlyingTux are their own mpbt solutions". **✅ FlyingTux DONE
(2026-07-02):** eigene Solution `cf/flyingtux/` (config + `solutions/default.yaml` +
`packages/xlibre/flyingtux.yaml`) + Wrapper `run-{fetch,build,opencode}.flyingtux`, **separat vom
xserver-Build**. **Branch-Entscheidung:** getrackt wird `master` (die aktuell produktive
Python-Codebasis), **nicht** der fertige, aber noch unmerged Go-Rewrite auf
`wip/golang-rewrite`/`/home/nekrad/src/flyingtux-go` (analog zur go-x11proto-Entscheidung, den
tatsächlichen Produktiv-Branch zu tracken) — Merge des Rewrites bleibt eine separate
Praetor-Entscheidung, hier nicht angefasst. **Worktree-Falle gefunden+gelöst:**
`/home/nekrad/src/flyingtux` (`master`) und `/home/nekrad/src/flyingtux-go`
(`wip/golang-rewrite`) waren **ein Repo als zwei verlinkte git-worktrees**, kein zweites
unabhängiges Klon — ein simples `mv` des Hauptworktrees hätte den Rücklink von `flyingtux-go`
gebrochen. Gelöst mit `mv` + `git worktree repair` aus dem verschobenen Verzeichnis heraus;
`flyingtux-go` danach verifiziert unangetastet (gleicher Commit, sauberer Baum, alle Branches
intakt) nach `_WORK_/flyingtux/sources/xlibre/flyingtux` verschoben (nicht neu geklont — kein
Stash vorhanden, sauberer Baum vor dem Move). Kein Build-Schritt nötig (reines Python, kein
Compile-Artefakt) — `buildsystem: exec` ganz ohne `commands:`-Block ist laut mpbt-Quellcode
(`core/workflow/build/exec.go`) ein dokumentierter No-op pro Stage; `run-build` schreibt nur einen
Source-Tarball. Ein `python -m compileall`-Smoketest wurde erwogen und verworfen: der `master`-Baum
enthält alte Python-2-Syntax (`chmod(scriptname, 0755)` in
`src/imagebuilder/flyingtux/app/deploy.py`), die einen Compile-Smoketest von Anfang an rot machen
würde — nicht Teil dieser Migration, siehe Parkplatz. Kein `make-pr.*`-Config (persönliches
`metux/flyingtux`-Repo, nicht `X11Libre` — die xserver-PR-Konventionen greifen hier nicht).
`run-fetch`/`run-build` beide grün getestet. |
| FlyingTux `master`: Python-2-only Syntax in `deploy.py` | agent, 2026-07-02 (bei der mpbt-Solution-Integration entdeckt) | 2026-07-02 | `src/imagebuilder/flyingtux/app/deploy.py` hat `chmod(scriptname, 0755)` — ein alter Oktal-Literal ohne `0o`-Präfix, unter Python 3 ein `SyntaxError` (nicht nur eine Warnung). Verhindert jeden `python -m compileall`/`ast.parse`-Smoketest über den Baum. Nicht Teil der mpbt-Integration gefixt (out of scope) — eigener kleiner Python-3-Kompatibilitäts-Fix, wenn `master` mal angefasst wird. |
| `panoramiXprocs.c` noch nicht auf neue `X_REQUEST*`-Makros umgestellt | praetor, 2026-07-02 (bei #3136 Bug-2-Audit aufgefallen) | 2026-07-02 | `Xext/panoramiX/panoramiXprocs.c` benutzt noch das alte Request-Handling (manuelles Header-Parsing, Byteswap, Längen-Checks) statt der neuen `X_REQUEST*`-Makros. **Außerdem prüfen:** sind die Replies dort schon auf den `x_rpcbuf_t`-Pfad (`X_SEND_REPLY_WITH_RPCBUF`) umgestellt, oder noch alte `WriteToClient`/`X_SEND_REPLY_SIMPLE`-Handler? (Beim Audit gesehen: `PanoramiXAllocColor` nutzt noch bare `xAllocColorReply` + `X_SEND_REPLY_SIMPLE` — Info-Leak, separat gefixt.) Eigener Umstell-Pass, wenn Zeit. |
| Xinerama-Umbau: interner (unsichtbarer) Proxy-Screen statt Screen-0-Sonderrolle | praetor, 2026-07-02 | 2026-07-02 | **Zukunftsaufgabe, noch nichts begonnen — nur Architektur-Idee + erste Ist-Stand-Recherche.** Idee (Praetor): Clients, die den Default-Screen ansprechen, landen nicht mehr auf dem ersten echten Screen, sondern auf einem internen, nach außen unsichtbaren Xinerama-`ScreenRec`, das Requests dann intern auf die echten Screens verteilt — kein Hooken des primären Screens mehr, keine direkten dix→Xinerama-Calls mehr, alles nur noch indirekt über diesen internen Screen. Später soll das Screen-Setup zur Laufzeit änderbar sein (z.B. via XRandR-artiges Interface). **Fernziel:** dynamische Konfiguration mehrerer *verschiedener* Karten ohne gemeinsamen Framebuffer soll genauso einfach/transparent werden wie heute Multi-Output innerhalb einer Karte via XRandR. **Ist-Stand-Recherche (2026-07-02, `Xext/panoramiX/`, ~4125 Zeilen, aktiv gepflegt — letzter Commit 2026-04-29):** Screen 0 ist heute fest der "primäre" Screen — beim Connection-Setup wird `screenInfo.numScreens` kurz auf `1` gesetzt (`panoramiX.c:614`), sodass Clients de facto auf Screen 0 landen; die anderen Screens existieren nur geometrisch (Xinerama-Protokoll `GetScreenSize` etc.), nicht als eigene Root-Fenster. **Kein sauberer Screen-Hook-Mechanismus** — Umleitung passiert per **ProcVector-Interception auf Request-Dispatch-Ebene** (`panoramiX.c:510-565`, 23+ Handler wie `PanoramiXCreateWindow`/`PanoramiXConfigureWindow` ersetzen die core-Dispatcher direkt), nicht per Wrapping von `ScreenRec`-Funktionszeigern (einzige Ausnahme: `CreateGC` wird gewrapped, `panoramiX.c:156,172-186`). **dix ruft heute direkt/unconditional in Xinerama-Zustand rein**, nicht nur hinter `#ifdef`: `dix/main.c:248-249,271-272` (`PanoramiXConsolidate()`/`PanoramiXCreateConnectionBlock()`), plus verstreute `!noPanoramiXExtension`- und `pScreen->myNum`-Checks in `dix/events.c`, `dix/window.c` (Zeilen ~2276/2360/2506/2768/2996), `dix/colormap.c`, `dix/resource.c:1179` — **genau das direkte Koppeln, das laut Aufgabenstellung weg soll**. Xinerama-Screen-Anzahl/-Geometrie ist heute reines 1:1-Mapping auf `screenInfo.screens[]` (`PanoramiXNumScreens = screenInfo.numScreens`, `panoramiX.c:442`), keine Indirektion, kein synthetischer Screen. **Multi-GPU-Verträglichkeit schon teilweise vorhanden:** Screen-Offsets (`pScreen->x/y`) werden für Koordinatentransformation genutzt (`panoramiXprocs.c:143-150`), keine GPU/Device-Identity-Checks gefunden (kein `xf86ScrnToScreen`/DRM-Device-Lookup) — aber **harte Annahme gleicher Pixel-Formate/Depths über alle Screens** (`panoramiX.c:598`: `rootDepth`-Mismatch → Fehler; unterschiedliche Backing-Store-Unterstützung deaktiviert Xinerama komplett für alle Screens, `panoramiX.c:602-610`). **Für die Umsetzung relevant:** ein echter Proxy-Screen bräuchte entweder eine Erweiterung des Screen-Hook-Mechanismus (`dixScreenHook*`-Familie, siehe `dix/screen_hooks_priv.h`) auf die fehlenden Fälle, oder einen kompletten Ersatz der ProcVector-Interception-Technik; die verstreuten `myNum`/`noPanoramiXExtension`-Direktchecks in dix müssten durch den neuen indirekten Pfad ersetzt werden; die Depth/Backing-Store-Kompatibilitätsannahmen müssten für "verschiedene Karten" gelockert werden. Nächster Schritt: kein Code, sondern eine echte Design-Skizze (wie sieht der interne Screen aus, wie registriert/dispatcht er, wie interagiert er mit RandR für die spätere Laufzeit-Rekonfiguration) — erst dann Machbarkeit wirklich beurteilbar. **Siehe auch** Aktive-Themen-Zeile "Xinerama/PanoramiX refactor — replace proc-vector hooking with a frontend/backend split" — eine am selben Tag von einer anderen Session aufgenommene, verwandte aber andere Praetor-Idee für dieselbe Subsystem-Baustelle. Beide Ideen noch nicht miteinander abgeglichen |
| x86emu `mul_long`/`idiv_long`/`div_long` correctness bugs | `X86EMU-CLEANUP.md` | 2026 (fuzz run) | Manual-path high-word carry bug in `mul_long`; u64-path overflow check wrong in `idiv_long`/`div_long`, `INT64_MIN / -1` SIGFPEs. Needs a dedicated, separately-reviewed correctness commit + the fuzz harness (`scratchpad/llcmp.c`, not checked in) as evidence |
| x86emu `types.h` u8/u16 kludge → stdint rename | `X86EMU-CLEANUP.md` | 2026 | Treewide mechanical rename, own large pass |
| x86emu `#ifdef DEBUG` / `DB()` macro cleanup | `X86EMU-CLEANUP.md` | 2026 | Low priority |
| Hurd kdrive backend for `xfbdev` | `AGENTS.md` Hurd CI section | 2026 (PR #3193 probe) | The one *real* open Hurd port gap (DRI/glamor are fundamentally blocked by no DRM kernel iface, not worth chasing) |
| `TODO.md` infra backlog (git-autopick, CI apt-caching x3, duplicate CI pipelines on PR push, cygwin repo build, real-Xserver-in-CI feasibility, gh-api helper scripts) | `TODO.md` | initial workspace upload | Never triaged since import — re-check which are still relevant before acting on any |
| `xx-make-pr.sh` leaks `[PR #NNNN]`/`PR:` markers onto the **PR branch** (not just the incubator) | `AGENTS.md` "PR workflow" | 2026 (found via PR #3162) | Root cause: `DEFAULT_MODE="rebase"` applies the marker rewrite to `$BRANCH_NAME` too, because the clean `incubator`-only mode needs interactive `git rebase -i` (unsupported here). Fix: scope the marker `--exec` to the incubator rebase only, make that path non-interactive (`GIT_SEQUENCE_EDITOR=true`, no `-i`). Until fixed, every `xx-make-pr.sh`-created PR needs a manual clean-subject check before merge |
| Legacy backport-queue tracker PRs `#2170` ("backport WIP queue onto 25.0") / `#2171` ("...onto 25.1") | `gh pr view 2170/2171` | 2026-07-01 | Old-style catch-all PRs, each just a checklist of ~25 sub-PR numbers (#2196–#2816 range) — predate the `tracking/xorg/main-on-<rel>` workflow. Check with praetor whether these are superseded/closeable now that the tracker-branch workflow covers the same job |
| ABI verdict already recorded in `AGENTS.md`/`NVIDIA-ABI.md`, but the PR itself is still open | `AGENTS.md` "Automated reviews" (NVIDIA-ABI section) | 2026-07-01 | PR #1786 (`Draft: drop MI overlay code` — verdict: breaks all tested blobs, deletes 6 `miOverlay*` symbols), #2070 (`unexport xf86CursorScreenKeyRec` — verdict: resolved by runtime lookup in all blobs, unsafe), #808 (`RFC: drop XvMC` — verdict: safe, no tested blob references it) all still sit open on GitHub with no label/close action taken on the verdict |
| NVIDIA ABI check version coverage is only 4 versions (390/470/550/570) | `NVIDIA-ABI.md` "Versions checked" (inline `TODO:`) | 2026 | Widen via `scripts/fetch-all-nvidia-drivers --per-branch` (77 branches) for exhaustive coverage before relying on "no tested blob references it" as a strong safety verdict |
| bigfont: hard-wired dix→extension call in `CloseFont()` | `BIGFONT.md` | 2026-07-01 | `dix/dixfonts.c:539` directly calls `XF86BigfontFreeFontShm` under `#ifdef XF86BIGFONT`. Consider decoupling (per-font free-callback / registration) so dix doesn't hard-reference the extension |
| bigfont: does core logic belong in dix? | `BIGFONT.md` | 2026-07-01 | The shm/font-metrics-sharing machinery is arguably dix-level; the extension could shrink to the pure protocol/wire interface. Bigger redesign, after the build resurrection |
| bigfont: `xlibre/bigfont-consolidate-cleanup` branch has no actual commit | agent clone `_WORK_/xserver-master/agent/rhel-ci/xserver` | 2026-07-01 | `BIGFONT.md` calls this branch "ready, not pushed" (drop redundant `XF86BigfontResetProc`), but its tip is identical to `origin/master` — the intended commit was never made (or got lost). Needs to be (re)authored before it can be pushed/PR'd |
| Pure-Go diagnosis tool (xdpyinfo-like) on go-x11proto | praetor idea, 2026-07-01 | 2026-07-01 | Idea only, deprioritized. A pure-Go server-inventory dumper (setup: vendor/release/proto/byteorder; screens+depths+visuals; extensions+versions; pixmap-formats) built on go-x11proto — no libX11/libxcb, so it works in minimal/container/CI envs (where the go-xts socket issue lives). Would ship with `-json`/`-s` from day one. Revisit if a pure-Go `xdpyinfo` is wanted |
| Generalize parts of `mtx/agent-config` onto `master` | praetor decision, 2026-07-01 | 2026-07-01 | `mtx/agent-config` is the praetor's personal staging branch (docs/lessons/tooling accumulate freely there, see `AGENTS.md`). Praetor intends to later review and selectively promote durable, team-useful parts to `master`. No selection made yet — revisit when the branch has settled |
| `ARRAY_SIZE` defined 9× in-tree, inconsistently guarded | `include/dix.h` + 8 others (`grep -rn 'define ARRAY_SIZE'`); dix.h guarded (#3203 **merged**; no backport — Solaris not in release CI) | 2026-07-01 | Non-standard macro **some platforms provide** (illumos `<sys/sysmacros.h>`), others don't (Linux glibc userspace) → redefinition clashes. Only `pci_id_driver_map.h` + (now) `dix.h` use `#ifndef`. Look at properly: one canonical guarded def (or a dedicated cross-platform macro header), possibly a non-clashing name; **also audit the in-tree `xf86-*` drivers** (separate repos) for their own copies. Fits the MIN/MAX-dedup theme (#3142) |
| Retrofit the new licensing policy onto *existing* files | praetor, 2026-07-02 | 2026-07-02 | `AGENTS.md` "Licensing policy" only covers **new** files so far (final-delivery code → X11 OR MIT OR AGPL-3.0-or-later; non-delivery helper/CI tooling → AGPL-3.0-or-later). Praetor wants this eventually retrofitted onto **existing** files too, but **only ones written solely by the praetor** — never touching a file that contains code originally authored by someone else. Explicitly deferred: don't start scoping/relicensing existing files until the praetor gives a fresh go-ahead |
| Hurd CI job (`xserver-build-hurd`) can hang with no timeout | run 28600347199 / job 84806756748 (PR #3231), cancelled by praetor after ~33m; `.github/scripts/hurd/run-vm-build.sh` (QEMU), see `AGENTS.md` "Hurd CI" | 2026-07-02 | Step "Boot GNU/Hurd VM and build (Xvfb+Xnest)" wedged ~33min with **no per-job/step timeout** → had to be cancelled by hand (otherwise it burns to the 6h runner cap and blocks the PR's other jobs). Fix later: add a `timeout-minutes` to the job/step (or wrap the QEMU run in `timeout` inside `run-vm-build.sh`) so a wedged boot fails fast instead of hanging. Same "no timeout → silent hang" pattern as the xephyr-glamor/XTS row above |
| Scripts „rund machen" + Agent-Doku in eigenes `scripts/`-File, eigener Commit → **master** | praetor, 2026-07-02 | 2026-07-02 | Explizite Später-Aufgabe. Drei Teile: **(1)** alle `scripts/*` nochmal überarbeiten/robust machen — einheitlicher Stil (`set -euo pipefail`, `--help`-Banner, sane defaults, `REPO`-Override), Fehlerpfade, konsistente Ausgaben; die neueren `scripts/json` + `scripts/pr-ci` taugen als Muster. **(2)** Die agent-bezogenen Nutzungs-/Anweisungstexte (heute verstreut in `AGENTS.md` Key-commands-Tabelle + Prosa) in ein **eigenes File unter `scripts/`** ziehen (z.B. `scripts/README.md`), sodass die Skript-Doku bei den Skripten liegt. **(3)** Das Ganze als **eigener, sauberer Commit nach `master`** (nicht nur `mtx/agent-config`) — bewusste Promotion, damit alle Contributors Tooling + Doku bekommen. Verwandt mit „Generalize parts of `mtx/agent-config` onto `master`" (oben); dies ist die konkrete Skript/Doku-Teilmenge. Nichts gestartet. |
| opencode: keine Entsprechung zu Claude Codes `Monitor`-Tool → nur Notify, kein In-Context-Auto-Surfacing von agent-bus-Direktiven | praetor, 2026-07-03 | 2026-07-03 | Seit heute nutzen Claude-Code-Sessions einen `Monitor`-Watch auf die eigene agent-bus-Inbox, damit `tell`/`broadcast` direkt im Gespräch als Event auftaucht statt nur als Desktop-Notification (siehe `AGENTS.md`, agent-bus-Abschnitt „Auto-surfacing directives via Monitor"). opencode-Sessions (`run-opencode.xserver-*`) haben kein äquivalentes In-Context-Event-Mechanismus und bleiben auf reines Notify angewiesen (aktuell nicht mal das — `agent-bus-watch` ist nur per Claude-Code-Hook verdrahtet). Aufgabe: opencode so anpassen, dass zumindest Desktop-Notifications (`agent-bus-watch`) auch dort laufen — z.B. `run-opencode.*` startet `scripts/agent-bus-watch` selbst detached, analog zum Claude-Code-`SessionStart`-Hook. Volles In-Context-Surfacing bräuchte ein opencode-seitiges Äquivalent zu `Monitor`, falls es sowas gibt — noch nicht recherchiert. Nichts gestartet. |
| Better model for the flagship/top-level coordination role (Enterprise) — faster interactive turnaround | praetor, 2026-07-06 | 2026-07-06 | Praetor wants to look for a model better suited to the flagship's job specifically: it only needs to coordinate/delegate (read the board, `tell`/`broadcast`, triage inbox directives, decide what to hand to which worker) — it does **not** do coding itself, so raw coding capability isn't the deciding factor there. Current setup uses whatever model the session was started with (no distinction between flagship and worker model choice today). Goal: pick a model/config for `run-flagship` that responds faster in back-and-forth interactive use, since coordination is latency-sensitive (the praetor is directly chatting with it) in a way batch worker sessions aren't. Nothing started — idea stage, explicitly "for later". |

---

*Not tracked here on purpose (already covered elsewhere, would just go stale):* individual open
PRs (`gh pr list --repo X11Libre/xserver`), who's-online-now (`scripts/agent-bus board`), PR-branch
locks (`scripts/pr-claim --list`), per-PR backport-dashboard tables (each master PR's own
description).
